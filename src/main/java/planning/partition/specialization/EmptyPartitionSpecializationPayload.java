package planning.partition.specialization;

/**
 * Empty structured payload for specialization candidates whose metadata is fully represented by common fields.
 */
public enum EmptyPartitionSpecializationPayload implements PartitionSpecializationPayload {
    INSTANCE
}
