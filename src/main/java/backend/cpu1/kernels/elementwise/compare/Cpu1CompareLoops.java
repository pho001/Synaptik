// GENERATED by scripts/generate-cpu1-elementwise.js. Do not edit by hand.
package backend.cpu1.kernels.elementwise.compare;

import backend.cpu1.exec.Cpu1KernelArgs;
import backend.cpu1.exec.Cpu1TensorView;
import backend.cpu1.offset.Cpu1GenericOffsetPlan;
import jdk.incubator.vector.DoubleVector;
import jdk.incubator.vector.FloatVector;
import jdk.incubator.vector.VectorOperators;
import jdk.incubator.vector.VectorSpecies;
import tensor.dtype.TensorDTypeOps;
import utils.FastTranscendentals;
import utils.SpecialFunctions;

import java.lang.foreign.MemorySegment;
import java.nio.ByteOrder;

import static java.lang.foreign.ValueLayout.JAVA_DOUBLE;
import static java.lang.foreign.ValueLayout.JAVA_FLOAT;
import static java.lang.foreign.ValueLayout.JAVA_BYTE;
import static java.lang.foreign.ValueLayout.JAVA_SHORT;

/**
 * Static gt/ge/lt/le/eq/ne loops selected by Cpu1KernelId at prepare time.
 */
public final class Cpu1CompareLoops {
    private static final VectorSpecies<Float> F32 = FloatVector.SPECIES_PREFERRED;
    private static final VectorSpecies<Double> F64 = DoubleVector.SPECIES_PREFERRED;
    private static final ByteOrder ORDER = ByteOrder.nativeOrder();
    private static final int BROADCAST_CONTIGUOUS = 0;
    private static final int BROADCAST_SCALAR = 1;
    private static final int BROADCAST_INNER = 2;

    private Cpu1CompareLoops() {
    }

    private static int broadcastVectorMode(Cpu1TensorView view) {
        if (isBroadcastScalar(view)) {
            return BROADCAST_SCALAR;
        }
        if (isBroadcastInner(view)) {
            return BROADCAST_INNER;
        }
        return BROADCAST_CONTIGUOUS;
    }

    private static int broadcastVectorInnerSize(Cpu1TensorView outputView) {
        int rank = outputView.rank();
        return rank == 0 ? 1 : outputView.shape(rank - 1);
    }

    private static boolean isBroadcastScalar(Cpu1TensorView view) {
        for (int dim = 0; dim < view.rank(); dim++) {
            if (view.stride(dim) != 0) {
                return false;
            }
        }
        return true;
    }

    private static boolean isBroadcastInner(Cpu1TensorView view) {
        int rank = view.rank();
        if (rank == 0 || view.contiguous()) {
            return false;
        }
        for (int dim = 0; dim < rank - 1; dim++) {
            if (view.stride(dim) != 0) {
                return false;
            }
        }
        return view.stride(rank - 1) == 1;
    }

    public static void eqBf16ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) == TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void eqBf16ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        short[] left = args.input(0).bfloat16Array();
        short[] right = args.input(1).bfloat16Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset.offset(i)]) == TensorDTypeOps.fromBFloat16Bits(right[rightOffset.offset(i)]) ? 1 : 0);
        }
    }

    public static void eqBf16ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) == TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void eqBf16ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) == TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void eqBf16ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) == TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void eqF32ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] == right[rightOffset] ? 1 : 0);
        }
    }

    public static void eqF32ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        float[] left = args.input(0).float32Array();
        float[] right = args.input(1).float32Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] == right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void eqF32ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] == right[rightOffset] ? 1 : 0);
        }
    }

    public static void eqF32ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] == right[rightOffset] ? 1 : 0);
        }
    }

    public static void eqF32ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] == right[rightOffset] ? 1 : 0);
        }
    }

    public static void eqF64ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] == right[rightOffset] ? 1 : 0);
        }
    }

    public static void eqF64ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        double[] left = args.input(0).float64Array();
        double[] right = args.input(1).float64Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] == right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void eqF64ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] == right[rightOffset] ? 1 : 0);
        }
    }

    public static void eqF64ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] == right[rightOffset] ? 1 : 0);
        }
    }

    public static void eqF64ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] == right[rightOffset] ? 1 : 0);
        }
    }

    public static void geBf16ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) >= TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void geBf16ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        short[] left = args.input(0).bfloat16Array();
        short[] right = args.input(1).bfloat16Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset.offset(i)]) >= TensorDTypeOps.fromBFloat16Bits(right[rightOffset.offset(i)]) ? 1 : 0);
        }
    }

    public static void geBf16ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) >= TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void geBf16ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) >= TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void geBf16ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) >= TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void geF32ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] >= right[rightOffset] ? 1 : 0);
        }
    }

    public static void geF32ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        float[] left = args.input(0).float32Array();
        float[] right = args.input(1).float32Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] >= right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void geF32ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] >= right[rightOffset] ? 1 : 0);
        }
    }

    public static void geF32ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] >= right[rightOffset] ? 1 : 0);
        }
    }

    public static void geF32ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] >= right[rightOffset] ? 1 : 0);
        }
    }

    public static void geF64ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] >= right[rightOffset] ? 1 : 0);
        }
    }

    public static void geF64ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        double[] left = args.input(0).float64Array();
        double[] right = args.input(1).float64Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] >= right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void geF64ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] >= right[rightOffset] ? 1 : 0);
        }
    }

    public static void geF64ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] >= right[rightOffset] ? 1 : 0);
        }
    }

    public static void geF64ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] >= right[rightOffset] ? 1 : 0);
        }
    }

    public static void gtBf16ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) > TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void gtBf16ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        short[] left = args.input(0).bfloat16Array();
        short[] right = args.input(1).bfloat16Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset.offset(i)]) > TensorDTypeOps.fromBFloat16Bits(right[rightOffset.offset(i)]) ? 1 : 0);
        }
    }

    public static void gtBf16ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) > TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void gtBf16ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) > TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void gtBf16ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) > TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void gtF32ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] > right[rightOffset] ? 1 : 0);
        }
    }

    public static void gtF32ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        float[] left = args.input(0).float32Array();
        float[] right = args.input(1).float32Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] > right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void gtF32ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] > right[rightOffset] ? 1 : 0);
        }
    }

    public static void gtF32ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] > right[rightOffset] ? 1 : 0);
        }
    }

    public static void gtF32ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] > right[rightOffset] ? 1 : 0);
        }
    }

    public static void gtF64ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] > right[rightOffset] ? 1 : 0);
        }
    }

    public static void gtF64ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        double[] left = args.input(0).float64Array();
        double[] right = args.input(1).float64Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] > right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void gtF64ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] > right[rightOffset] ? 1 : 0);
        }
    }

    public static void gtF64ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] > right[rightOffset] ? 1 : 0);
        }
    }

    public static void gtF64ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] > right[rightOffset] ? 1 : 0);
        }
    }

    public static void leBf16ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) <= TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void leBf16ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        short[] left = args.input(0).bfloat16Array();
        short[] right = args.input(1).bfloat16Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset.offset(i)]) <= TensorDTypeOps.fromBFloat16Bits(right[rightOffset.offset(i)]) ? 1 : 0);
        }
    }

    public static void leBf16ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) <= TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void leBf16ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) <= TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void leBf16ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) <= TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void leF32ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] <= right[rightOffset] ? 1 : 0);
        }
    }

    public static void leF32ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        float[] left = args.input(0).float32Array();
        float[] right = args.input(1).float32Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] <= right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void leF32ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] <= right[rightOffset] ? 1 : 0);
        }
    }

    public static void leF32ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] <= right[rightOffset] ? 1 : 0);
        }
    }

    public static void leF32ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] <= right[rightOffset] ? 1 : 0);
        }
    }

    public static void leF64ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] <= right[rightOffset] ? 1 : 0);
        }
    }

    public static void leF64ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        double[] left = args.input(0).float64Array();
        double[] right = args.input(1).float64Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] <= right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void leF64ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] <= right[rightOffset] ? 1 : 0);
        }
    }

    public static void leF64ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] <= right[rightOffset] ? 1 : 0);
        }
    }

    public static void leF64ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] <= right[rightOffset] ? 1 : 0);
        }
    }

    public static void ltBf16ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) < TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void ltBf16ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        short[] left = args.input(0).bfloat16Array();
        short[] right = args.input(1).bfloat16Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset.offset(i)]) < TensorDTypeOps.fromBFloat16Bits(right[rightOffset.offset(i)]) ? 1 : 0);
        }
    }

    public static void ltBf16ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) < TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void ltBf16ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) < TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void ltBf16ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) < TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void ltF32ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] < right[rightOffset] ? 1 : 0);
        }
    }

    public static void ltF32ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        float[] left = args.input(0).float32Array();
        float[] right = args.input(1).float32Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] < right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void ltF32ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] < right[rightOffset] ? 1 : 0);
        }
    }

    public static void ltF32ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] < right[rightOffset] ? 1 : 0);
        }
    }

    public static void ltF32ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] < right[rightOffset] ? 1 : 0);
        }
    }

    public static void ltF64ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] < right[rightOffset] ? 1 : 0);
        }
    }

    public static void ltF64ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        double[] left = args.input(0).float64Array();
        double[] right = args.input(1).float64Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] < right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void ltF64ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] < right[rightOffset] ? 1 : 0);
        }
    }

    public static void ltF64ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] < right[rightOffset] ? 1 : 0);
        }
    }

    public static void ltF64ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] < right[rightOffset] ? 1 : 0);
        }
    }

    public static void neBf16ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) != TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void neBf16ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        short[] left = args.input(0).bfloat16Array();
        short[] right = args.input(1).bfloat16Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset.offset(i)]) != TensorDTypeOps.fromBFloat16Bits(right[rightOffset.offset(i)]) ? 1 : 0);
        }
    }

    public static void neBf16ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) != TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void neBf16ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) != TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void neBf16ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        short[] left = leftView.bfloat16Array();
        short[] right = rightView.bfloat16Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (TensorDTypeOps.fromBFloat16Bits(left[leftOffset]) != TensorDTypeOps.fromBFloat16Bits(right[rightOffset]) ? 1 : 0);
        }
    }

    public static void neF32ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] != right[rightOffset] ? 1 : 0);
        }
    }

    public static void neF32ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        float[] left = args.input(0).float32Array();
        float[] right = args.input(1).float32Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] != right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void neF32ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] != right[rightOffset] ? 1 : 0);
        }
    }

    public static void neF32ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] != right[rightOffset] ? 1 : 0);
        }
    }

    public static void neF32ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        float[] left = leftView.float32Array();
        float[] right = rightView.float32Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] != right[rightOffset] ? 1 : 0);
        }
    }

    public static void neF64ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int leftOffset = leftBase + i;
            int rightOffset = rightBase + i;
            int outputOffset = outputBase + i;
            output[outputOffset] = (byte) (left[leftOffset] != right[rightOffset] ? 1 : 0);
        }
    }

    public static void neF64ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        double[] left = args.input(0).float64Array();
        double[] right = args.input(1).float64Array();
        byte[] output = args.output().boolArray();
        Cpu1GenericOffsetPlan leftOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan rightOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            output[outputOffset.offset(i)] = (byte) (left[leftOffset.offset(i)] != right[rightOffset.offset(i)] ? 1 : 0);
        }
    }

    public static void neF64ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            output[outputOffset] = (byte) (left[leftOffset] != right[rightOffset] ? 1 : 0);
        }
    }

    public static void neF64ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            output[outputOffset] = (byte) (left[leftOffset] != right[rightOffset] ? 1 : 0);
        }
    }

    public static void neF64ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView leftView = args.input(0);
        Cpu1TensorView rightView = args.input(1);
        Cpu1TensorView outputView = args.output();
        double[] left = leftView.float64Array();
        double[] right = rightView.float64Array();
        byte[] output = outputView.boolArray();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int leftBase = leftView.storageOffset();
        int rightBase = rightView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int leftOffset = leftBase + c0 * leftView.stride(0) + c1 * leftView.stride(1) + c2 * leftView.stride(2) + c3 * leftView.stride(3);
            int rightOffset = rightBase + c0 * rightView.stride(0) + c1 * rightView.stride(1) + c2 * rightView.stride(2) + c3 * rightView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            output[outputOffset] = (byte) (left[leftOffset] != right[rightOffset] ? 1 : 0);
        }
    }
}
