// GENERATED by scripts/generate-cpu1-elementwise.js. Do not edit by hand.
package backend.cpu1.kernels.elementwise.where;

import backend.cpu1.exec.Cpu1KernelArgs;
import backend.cpu1.exec.Cpu1TensorView;
import backend.cpu1.offset.Cpu1GenericOffsetPlan;
import jdk.incubator.vector.DoubleVector;
import jdk.incubator.vector.FloatVector;
import jdk.incubator.vector.VectorOperators;
import jdk.incubator.vector.VectorSpecies;
import tensor.dtype.TensorDTypeOps;
import utils.FastTranscendentals;
import utils.SpecialFunctions;

import java.lang.foreign.MemorySegment;
import java.nio.ByteOrder;

import static java.lang.foreign.ValueLayout.JAVA_DOUBLE;
import static java.lang.foreign.ValueLayout.JAVA_FLOAT;
import static java.lang.foreign.ValueLayout.JAVA_BYTE;
import static java.lang.foreign.ValueLayout.JAVA_SHORT;

/**
 * Static where loops selected by Cpu1ElementwiseKernelId at prepare time.
 */
public final class Cpu1WhereLoops {
    private static final VectorSpecies<Float> F32 = FloatVector.SPECIES_PREFERRED;
    private static final VectorSpecies<Double> F64 = DoubleVector.SPECIES_PREFERRED;
    private static final ByteOrder ORDER = ByteOrder.nativeOrder();
    private static final int BROADCAST_CONTIGUOUS = 0;
    private static final int BROADCAST_SCALAR = 1;
    private static final int BROADCAST_INNER = 2;

    private Cpu1WhereLoops() {
    }

    private static int broadcastVectorMode(Cpu1TensorView view) {
        if (isBroadcastScalar(view)) {
            return BROADCAST_SCALAR;
        }
        if (isBroadcastInner(view)) {
            return BROADCAST_INNER;
        }
        return BROADCAST_CONTIGUOUS;
    }

    private static int broadcastVectorInnerSize(Cpu1TensorView outputView) {
        int rank = outputView.rank();
        return rank == 0 ? 1 : outputView.shape(rank - 1);
    }

    private static boolean isBroadcastScalar(Cpu1TensorView view) {
        for (int dim = 0; dim < view.rank(); dim++) {
            if (view.stride(dim) != 0) {
                return false;
            }
        }
        return true;
    }

    private static boolean isBroadcastInner(Cpu1TensorView view) {
        int rank = view.rank();
        if (rank == 0 || view.contiguous()) {
            return false;
        }
        for (int dim = 0; dim < rank - 1; dim++) {
            if (view.stride(dim) != 0) {
                return false;
            }
        }
        return view.stride(rank - 1) == 1;
    }

    public static void bf16FromBf16Bf16ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        short[] falseValues = falseView.bfloat16Array();
        short[] output = outputView.bfloat16Array();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            float selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = TensorDTypeOps.toBFloat16Bits(selected);
        }
    }

    public static void bf16FromBf16Bf16ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        byte[] condition = args.input(0).boolArray();
        short[] trueValues = args.input(1).bfloat16Array();
        short[] falseValues = args.input(2).bfloat16Array();
        short[] output = args.output().bfloat16Array();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            float selected = condition[conditionOffset.offset(i)] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset.offset(i)]) : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset.offset(i)]);
            output[outputOffset.offset(i)] = TensorDTypeOps.toBFloat16Bits(selected);
        }
    }

    public static void bf16FromBf16Bf16ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        short[] falseValues = falseView.bfloat16Array();
        short[] output = outputView.bfloat16Array();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            float selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = TensorDTypeOps.toBFloat16Bits(selected);
        }
    }

    public static void bf16FromBf16Bf16ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        short[] falseValues = falseView.bfloat16Array();
        short[] output = outputView.bfloat16Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            float selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = TensorDTypeOps.toBFloat16Bits(selected);
        }
    }

    public static void bf16FromBf16Bf16ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        short[] falseValues = falseView.bfloat16Array();
        short[] output = outputView.bfloat16Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            float selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = TensorDTypeOps.toBFloat16Bits(selected);
        }
    }

    public static void bf16FromBf16Bf16SegmentContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_SHORT, (long) (outputOffset) * Short.BYTES, TensorDTypeOps.toBFloat16Bits(selected));
        }
    }

    public static void bf16FromBf16Bf16SegmentStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        MemorySegment condition = args.input(0).segment();
        MemorySegment trueValues = args.input(1).segment();
        MemorySegment falseValues = args.input(2).segment();
        MemorySegment output = args.output().segment();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset.offset(i)) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset.offset(i)) * Short.BYTES)) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset.offset(i)) * Short.BYTES));
            output.set(JAVA_SHORT, (long) (outputOffset.offset(i)) * Short.BYTES, TensorDTypeOps.toBFloat16Bits(selected));
        }
    }

    public static void bf16FromBf16Bf16SegmentStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_SHORT, (long) (outputOffset) * Short.BYTES, TensorDTypeOps.toBFloat16Bits(selected));
        }
    }

    public static void bf16FromBf16Bf16SegmentStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_SHORT, (long) (outputOffset) * Short.BYTES, TensorDTypeOps.toBFloat16Bits(selected));
        }
    }

    public static void bf16FromBf16Bf16SegmentStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_SHORT, (long) (outputOffset) * Short.BYTES, TensorDTypeOps.toBFloat16Bits(selected));
        }
    }

    public static void f32FromBf16F32ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        float[] falseValues = falseView.float32Array();
        float[] output = outputView.float32Array();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            float selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f32FromBf16F32ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        byte[] condition = args.input(0).boolArray();
        short[] trueValues = args.input(1).bfloat16Array();
        float[] falseValues = args.input(2).float32Array();
        float[] output = args.output().float32Array();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            float selected = condition[conditionOffset.offset(i)] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset.offset(i)]) : falseValues[falseOffset.offset(i)];
            output[outputOffset.offset(i)] = selected;
        }
    }

    public static void f32FromBf16F32ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        float[] falseValues = falseView.float32Array();
        float[] output = outputView.float32Array();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            float selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f32FromBf16F32ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        float[] falseValues = falseView.float32Array();
        float[] output = outputView.float32Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            float selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f32FromBf16F32ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        float[] falseValues = falseView.float32Array();
        float[] output = outputView.float32Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            float selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f32FromBf16F32SegmentContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromBf16F32SegmentStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        MemorySegment condition = args.input(0).segment();
        MemorySegment trueValues = args.input(1).segment();
        MemorySegment falseValues = args.input(2).segment();
        MemorySegment output = args.output().segment();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset.offset(i)) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset.offset(i)) * Short.BYTES)) : falseValues.get(JAVA_FLOAT, (long) (falseOffset.offset(i)) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset.offset(i)) * Float.BYTES, selected);
        }
    }

    public static void f32FromBf16F32SegmentStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromBf16F32SegmentStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromBf16F32SegmentStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32Bf16ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        short[] falseValues = falseView.bfloat16Array();
        float[] output = outputView.float32Array();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            float selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = selected;
        }
    }

    public static void f32FromF32Bf16ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        byte[] condition = args.input(0).boolArray();
        float[] trueValues = args.input(1).float32Array();
        short[] falseValues = args.input(2).bfloat16Array();
        float[] output = args.output().float32Array();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            float selected = condition[conditionOffset.offset(i)] != 0 ? trueValues[trueOffset.offset(i)] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset.offset(i)]);
            output[outputOffset.offset(i)] = selected;
        }
    }

    public static void f32FromF32Bf16ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        short[] falseValues = falseView.bfloat16Array();
        float[] output = outputView.float32Array();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            float selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = selected;
        }
    }

    public static void f32FromF32Bf16ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        short[] falseValues = falseView.bfloat16Array();
        float[] output = outputView.float32Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            float selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = selected;
        }
    }

    public static void f32FromF32Bf16ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        short[] falseValues = falseView.bfloat16Array();
        float[] output = outputView.float32Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            float selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = selected;
        }
    }

    public static void f32FromF32Bf16SegmentContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32Bf16SegmentStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        MemorySegment condition = args.input(0).segment();
        MemorySegment trueValues = args.input(1).segment();
        MemorySegment falseValues = args.input(2).segment();
        MemorySegment output = args.output().segment();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset.offset(i)) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset.offset(i)) * Float.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset.offset(i)) * Short.BYTES));
            output.set(JAVA_FLOAT, (long) (outputOffset.offset(i)) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32Bf16SegmentStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32Bf16SegmentStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32Bf16SegmentStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32F32ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        float[] falseValues = falseView.float32Array();
        float[] output = outputView.float32Array();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            float selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f32FromF32F32ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        byte[] condition = args.input(0).boolArray();
        float[] trueValues = args.input(1).float32Array();
        float[] falseValues = args.input(2).float32Array();
        float[] output = args.output().float32Array();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            float selected = condition[conditionOffset.offset(i)] != 0 ? trueValues[trueOffset.offset(i)] : falseValues[falseOffset.offset(i)];
            output[outputOffset.offset(i)] = selected;
        }
    }

    public static void f32FromF32F32ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        float[] falseValues = falseView.float32Array();
        float[] output = outputView.float32Array();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            float selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f32FromF32F32ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        float[] falseValues = falseView.float32Array();
        float[] output = outputView.float32Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            float selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f32FromF32F32ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        float[] falseValues = falseView.float32Array();
        float[] output = outputView.float32Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            float selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f32FromF32F32SegmentContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32F32SegmentStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        MemorySegment condition = args.input(0).segment();
        MemorySegment trueValues = args.input(1).segment();
        MemorySegment falseValues = args.input(2).segment();
        MemorySegment output = args.output().segment();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset.offset(i)) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset.offset(i)) * Float.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset.offset(i)) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset.offset(i)) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32F32SegmentStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32F32SegmentStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f32FromF32F32SegmentStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            float selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_FLOAT, (long) (outputOffset) * Float.BYTES, selected);
        }
    }

    public static void f64FromBf16F64ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromBf16F64ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        byte[] condition = args.input(0).boolArray();
        short[] trueValues = args.input(1).bfloat16Array();
        double[] falseValues = args.input(2).float64Array();
        double[] output = args.output().float64Array();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition[conditionOffset.offset(i)] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset.offset(i)]) : falseValues[falseOffset.offset(i)];
            output[outputOffset.offset(i)] = selected;
        }
    }

    public static void f64FromBf16F64ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromBf16F64ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromBf16F64ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        short[] trueValues = trueView.bfloat16Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition[conditionOffset] != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues[trueOffset]) : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromBf16F64SegmentContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromBf16F64SegmentStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        MemorySegment condition = args.input(0).segment();
        MemorySegment trueValues = args.input(1).segment();
        MemorySegment falseValues = args.input(2).segment();
        MemorySegment output = args.output().segment();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset.offset(i)) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset.offset(i)) * Short.BYTES)) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset.offset(i)) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset.offset(i)) * Double.BYTES, selected);
        }
    }

    public static void f64FromBf16F64SegmentStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromBf16F64SegmentStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromBf16F64SegmentStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? TensorDTypeOps.fromBFloat16Bits(trueValues.get(JAVA_SHORT, (long) (trueOffset) * Short.BYTES)) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF32F64ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF32F64ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        byte[] condition = args.input(0).boolArray();
        float[] trueValues = args.input(1).float32Array();
        double[] falseValues = args.input(2).float64Array();
        double[] output = args.output().float64Array();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition[conditionOffset.offset(i)] != 0 ? trueValues[trueOffset.offset(i)] : falseValues[falseOffset.offset(i)];
            output[outputOffset.offset(i)] = selected;
        }
    }

    public static void f64FromF32F64ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF32F64ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF32F64ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        float[] trueValues = trueView.float32Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF32F64SegmentContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF32F64SegmentStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        MemorySegment condition = args.input(0).segment();
        MemorySegment trueValues = args.input(1).segment();
        MemorySegment falseValues = args.input(2).segment();
        MemorySegment output = args.output().segment();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset.offset(i)) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset.offset(i)) * Float.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset.offset(i)) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset.offset(i)) * Double.BYTES, selected);
        }
    }

    public static void f64FromF32F64SegmentStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF32F64SegmentStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF32F64SegmentStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_FLOAT, (long) (trueOffset) * Float.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64Bf16ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        short[] falseValues = falseView.bfloat16Array();
        double[] output = outputView.float64Array();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64Bf16ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        byte[] condition = args.input(0).boolArray();
        double[] trueValues = args.input(1).float64Array();
        short[] falseValues = args.input(2).bfloat16Array();
        double[] output = args.output().float64Array();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition[conditionOffset.offset(i)] != 0 ? trueValues[trueOffset.offset(i)] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset.offset(i)]);
            output[outputOffset.offset(i)] = selected;
        }
    }

    public static void f64FromF64Bf16ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        short[] falseValues = falseView.bfloat16Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64Bf16ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        short[] falseValues = falseView.bfloat16Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64Bf16ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        short[] falseValues = falseView.bfloat16Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : TensorDTypeOps.fromBFloat16Bits(falseValues[falseOffset]);
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64Bf16SegmentContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64Bf16SegmentStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        MemorySegment condition = args.input(0).segment();
        MemorySegment trueValues = args.input(1).segment();
        MemorySegment falseValues = args.input(2).segment();
        MemorySegment output = args.output().segment();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset.offset(i)) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset.offset(i)) * Double.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset.offset(i)) * Short.BYTES));
            output.set(JAVA_DOUBLE, (long) (outputOffset.offset(i)) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64Bf16SegmentStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64Bf16SegmentStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64Bf16SegmentStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : TensorDTypeOps.fromBFloat16Bits(falseValues.get(JAVA_SHORT, (long) (falseOffset) * Short.BYTES));
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F32ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        float[] falseValues = falseView.float32Array();
        double[] output = outputView.float64Array();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64F32ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        byte[] condition = args.input(0).boolArray();
        double[] trueValues = args.input(1).float64Array();
        float[] falseValues = args.input(2).float32Array();
        double[] output = args.output().float64Array();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition[conditionOffset.offset(i)] != 0 ? trueValues[trueOffset.offset(i)] : falseValues[falseOffset.offset(i)];
            output[outputOffset.offset(i)] = selected;
        }
    }

    public static void f64FromF64F32ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        float[] falseValues = falseView.float32Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64F32ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        float[] falseValues = falseView.float32Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64F32ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        float[] falseValues = falseView.float32Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64F32SegmentContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F32SegmentStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        MemorySegment condition = args.input(0).segment();
        MemorySegment trueValues = args.input(1).segment();
        MemorySegment falseValues = args.input(2).segment();
        MemorySegment output = args.output().segment();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset.offset(i)) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset.offset(i)) * Double.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset.offset(i)) * Float.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset.offset(i)) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F32SegmentStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F32SegmentStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F32SegmentStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : falseValues.get(JAVA_FLOAT, (long) (falseOffset) * Float.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F64ArrayContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64F64ArrayStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        byte[] condition = args.input(0).boolArray();
        double[] trueValues = args.input(1).float64Array();
        double[] falseValues = args.input(2).float64Array();
        double[] output = args.output().float64Array();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition[conditionOffset.offset(i)] != 0 ? trueValues[trueOffset.offset(i)] : falseValues[falseOffset.offset(i)];
            output[outputOffset.offset(i)] = selected;
        }
    }

    public static void f64FromF64F64ArrayStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64F64ArrayStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64F64ArrayStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        byte[] condition = conditionView.boolArray();
        double[] trueValues = trueView.float64Array();
        double[] falseValues = falseView.float64Array();
        double[] output = outputView.float64Array();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition[conditionOffset] != 0 ? trueValues[trueOffset] : falseValues[falseOffset];
            output[outputOffset] = selected;
        }
    }

    public static void f64FromF64F64SegmentContiguousScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int conditionOffset = conditionBase + i;
            int trueOffset = trueBase + i;
            int falseOffset = falseBase + i;
            int outputOffset = outputBase + i;
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F64SegmentStridedGenericScalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        MemorySegment condition = args.input(0).segment();
        MemorySegment trueValues = args.input(1).segment();
        MemorySegment falseValues = args.input(2).segment();
        MemorySegment output = args.output().segment();
        Cpu1GenericOffsetPlan conditionOffset = args.inputGenericOffsetPlan(0);
        Cpu1GenericOffsetPlan trueOffset = args.inputGenericOffsetPlan(1);
        Cpu1GenericOffsetPlan falseOffset = args.inputGenericOffsetPlan(2);
        Cpu1GenericOffsetPlan outputOffset = args.outputGenericOffsetPlan();
        for (int i = startInclusive; i < endExclusive; i++) {
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset.offset(i)) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset.offset(i)) * Double.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset.offset(i)) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset.offset(i)) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F64SegmentStridedRank2Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c0 = i / d1;
            int c1 = i - c0 * d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F64SegmentStridedRank3Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c2 = i % d2;
            int rem = i / d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }

    public static void f64FromF64F64SegmentStridedRank4Scalar(Cpu1KernelArgs args, int startInclusive, int endExclusive) {
        Cpu1TensorView conditionView = args.input(0);
        Cpu1TensorView trueView = args.input(1);
        Cpu1TensorView falseView = args.input(2);
        Cpu1TensorView outputView = args.output();
        MemorySegment condition = conditionView.segment();
        MemorySegment trueValues = trueView.segment();
        MemorySegment falseValues = falseView.segment();
        MemorySegment output = outputView.segment();
        int d1 = outputView.shape(1);
        int d2 = outputView.shape(2);
        int d3 = outputView.shape(3);
        int conditionBase = conditionView.storageOffset();
        int trueBase = trueView.storageOffset();
        int falseBase = falseView.storageOffset();
        int outputBase = outputView.storageOffset();
        for (int i = startInclusive; i < endExclusive; i++) {
            int c3 = i % d3;
            int rem = i / d3;
            int c2 = rem % d2;
            rem /= d2;
            int c1 = rem % d1;
            int c0 = rem / d1;
            int conditionOffset = conditionBase + c0 * conditionView.stride(0) + c1 * conditionView.stride(1) + c2 * conditionView.stride(2) + c3 * conditionView.stride(3);
            int trueOffset = trueBase + c0 * trueView.stride(0) + c1 * trueView.stride(1) + c2 * trueView.stride(2) + c3 * trueView.stride(3);
            int falseOffset = falseBase + c0 * falseView.stride(0) + c1 * falseView.stride(1) + c2 * falseView.stride(2) + c3 * falseView.stride(3);
            int outputOffset = outputBase + c0 * outputView.stride(0) + c1 * outputView.stride(1) + c2 * outputView.stride(2) + c3 * outputView.stride(3);
            double selected = condition.get(JAVA_BYTE, (long) (conditionOffset) * Byte.BYTES) != 0 ? trueValues.get(JAVA_DOUBLE, (long) (trueOffset) * Double.BYTES) : falseValues.get(JAVA_DOUBLE, (long) (falseOffset) * Double.BYTES);
            output.set(JAVA_DOUBLE, (long) (outputOffset) * Double.BYTES, selected);
        }
    }
}
