# Task 0008E: Bounded Multi-Input Materialization and Representation Reuse

## Status

Complete

## Goal

Finish the existing CPU-private bounded representation capability without making an unproved
static performance choice. Preserve every complete direct, single-copy, and eligible two-copy
topology/representation candidate; exact graph-identity-free facts; explicit generated affine
copy units; dedicated resources; compatible copy-once reuse; finalization; and generated-versus-
optimal-clean-Java requirements. Ordinary CPU preparation must always select the unchanged direct
topology already selected by CPU 0008D. Materialized forms remain complete inspectable and
executable candidate alternatives for future opaque Prepare/model-autotuning work, but the current
static estimate cannot promote them.

The newly retained honest production-selectable FLOAT64 heap-carrier result requires this
correction. Generated affine copy remains equivalent to optimal clean Java at median
`1.000326680`, but the selected materialized complete plan is `2.605250934x` the equal-work direct
plan, with samples `2.615038667`, `2.606896076`, `2.498279430`, `2.583623032`, and
`2.613269873`. Preserve
`/private/tmp/synaptik-cpu-0008e-schema53-authoritative-evidence-v3-final2-20260827` and fork
SHA-256 `c55d07272272fb484b4a6580312aba34aa67483e404f42ecddaf8a717d7f4612`.
Do not retry, replace, relabel, omit, or change that carrier.

The older same-instruction two-copy failure and `CO_CONSUMED_PAIR` correction also remain
authoritative. A pair consumed together by one represented instruction stays a complete typed
rejection; both corresponding singles and eligible disjoint-consumer pairs remain retained. CPU
0008E completes the truthful candidate/execution foundation and safe ordinary fallback. Config
0006A, Prepare 0004, CPU 0016, and Tuning 0001–0002 later own explicit compatible end-to-end
promotion, so this boundary does not keep 0008E incomplete forever.

The first full CPU rerun after the narrow direct-selection correction remains discovery evidence:
`./gradlew :backends:cpu:test --rerun-tasks` executed 544 tests, skipped 3, and failed 9 because
exactly seven existing test owners retained stale selected-copy assumptions. The corrective
implementation migrated those owners to explicit retained-candidate oracles. Its focused matrix
passed 115 tests, and its final full CPU run passed 544 tests across 104 suites with 3 expected
skips and no failures or errors. Implementation and executable validation are complete. Final
clean documentation context `01a04317-b784-76e3-a93b-ff35106284b9` corrected the CPU guide and
the three authorized glossary entries, finalized affected Javadocs, and completed documentation
validation.

## Scope

### Preserved candidate and execution end-state

- Preserve the schema-53 `CpuRepresentationDecision` model, complete topology/representation
  identity, source/access/carrier geometry, consumer positions and use counts, reuse facts,
  workspaces, copy strategy, structural keys, specialization, costs, margins, rejection, and
  selection.
- Preserve direct plus every bounded eligible single-copy and two-distinct-copy identity for each
  legal CPU 0008D topology: direct first, singles in stable source order, and pairs in
  lexicographic source order. Do not invent, split, merge, legalize, or reorder topology.
- Preserve external-`READ` eligibility for already executable FLOAT64, FLOAT32, INT32, INT64, and
  canonical BOOL pointwise work. BFLOAT16 and all existing forbidden boundary, access, mapping,
  alias, lifetime, unresolved, overflowed, and unsupported cases remain excluded.
- Preserve exact-value deduplication and one generated copy reused across all compatible uses.
  Cross-unit repeated mapping/reuse stays a typed candidate fact, not a static promotion proof.
- Preserve zero/one/two explicit CPU-private `CONTIGUOUS` affine-copy units, workspaces 8 and 9,
  exact declarations, copy-before-consumer dependencies, finalization, cold validation,
  copy-once execution, and atomic partition behavior.
- Preserve the affine dense-result cursor, narrow generator guard, schema 53, stable unchanged
  direct pointwise class identity, schema-52 incompatible misses, exact carriers/forms,
  numerical policy, and every unaffected implementation/test.

### Typed candidate-versus-selection policy

- Existing materialization-policy costs and thresholds may continue producing diagnostic complete
  candidate facts. They no longer authorize ordinary static promotion.
- Add one closed selection reason named `DIRECT_MATERIALIZATION_UNPROVED`, or an equally exact
  task-approved name, meaning complete materialized candidates were retained but no compatible
  end-to-end evidence proves ordinary promotion across the admitted domain. Do not falsely call
  the policy disabled, enumeration incomplete, or the decision measured.
- With materialization enabled and enumeration complete, select the exact direct representation
  of CPU 0008D's already selected topology, return zero materializations, and record that reason.
  Disabled policy retains `DIRECT_POLICY_DISABLED`; existing uncertainty/incomplete paths remain
  fail-closed and direct.
- `Candidate-only` means fully typed, bounded, resource-complete, and executable by its CPU-owned
  realization, but not selected by ordinary preparation. It does not mean incomplete, illegal,
  discarded, or performance-relabelled.
- Retain `CO_CONSUMED_PAIR` before ranking whenever one represented instruction consumes both
  copied sources. Retain both singles and eligible disjoint-consumer pair candidates, but select
  none of them statically.
- Do not require cross-unit reuse for candidate eligibility. The one-copy failure proves the
  current static estimate insufficient, while no evidence proves that reuse alone bounds every
  admitted shape, mapping, type, carrier, consumer, and topology to `<= 1.15x`. Such a filter
  would discard valid future tuning candidates without proving safe static selection.
- A later static promotion requires a separately replanned closed admitted set and fresh no-retry
  five-fork evidence proving the complete selected plan `<= 1.15x` equal optimal direct Java for
  every represented admitted case claimed. Timing-derived constants, blacklists, and favorable
  fixtures are not proof.

### Honest evidence boundary

- Preserve semantic, Class-File, and generated-versus-optimal-clean-Java validation for the
  materialized candidate implementation. Final2 is positive execution-equivalence evidence and
  negative complete-plan promotion evidence.
- Under the identical final2 topology, shape, mapping, heap carrier, policy, and complete facts,
  prove ordinary production analysis selects CPU 0008D direct, returns zero materializations, and
  records `DIRECT_MATERIALIZATION_UNPROVED`; the slow candidate identity remains unchanged.
- Do not make the known-slow candidate masquerade as selected. Do not rerun it. Verify its CSV
  digest, generated class/identity, and retained generated/oracle result.
- Reuse retained `1.000326680` generated/oracle evidence because this correction changes no
  emitter, generator, carrier, finalizer, executable, schema, or class identity. Re-run focused
  semantic/Class-File checks needed to prove those owners remain unchanged.
- Selected/direct validation becomes deterministic direct selection under identical facts, not a
  new timing claim: selected and direct baseline are the same exact plan. End-to-end
  materialization promotion performance is later Tuning 0002 acceptance.
- If another generated/finalizer/executable/carrier/schema/resource/identity owner is needed, or
  any materialized form remains statically selectable, stop and replan.

### Corrective test-owner migration

- Change no additional production or Javadoc owner. The narrow production correction remains
  exclusively `CpuRepresentationDecision` plus `CpuRepresentationPlanner`, and the six-test
  `CpuRepresentationPlannerTest` result remains its focused ordinary-selection evidence.
- Reopen exactly the seven stale existing test owners listed under [Affected files](#affected-files).
  They must not force ordinary promotion, weaken the direct-selection assertions, or infer a copy
  from `plan.materializations()`, `plan.representationUnits()`, selected requirements, or selected
  workspaces after ordinary analysis.
- Obtain the desired complete materialized alternative explicitly from the retained
  `CpuRepresentationDecision.Variant` and its ordered `MaterializationIdentity` values. When a
  test needs the concrete `CpuMaterializationPlan`, represented consumer unit, requirements,
  finalization, or execution, build that exact candidate through the existing CPU-private plan
  records, representation-unit construction rules, shared-assignment test support,
  `CpuPartitionFinalizer`, and prepared executable machinery. The candidate must match the
  retained identity exactly; tests must not create a semantically similar synthetic copy and call
  it the retained candidate.
- `CpuKernelSpecializationTest` replaces selected-plan specialization lookup with the retained
  candidate's copy and represented-consumer specialization identities, preserving graph/run/
  instance exclusion, schema-53 compatibility, and the four-candidate/one-artifact budget oracle.
- `CpuFusedGeneratedKernelTest` executes an explicitly chosen retained single-copy candidate for
  every admitted source regime and orchestration strategy, then compares copy plus represented
  consumer semantics to the existing scalar reference. Ordinary analysis is separately asserted
  direct and zero-copy.
- `CpuShapePolymorphicArtifactTest` generates the explicit retained candidate's represented
  consumer artifacts at two compatible extents and proves their specialization/class bytes are
  shape-polymorphic yet distinct from the unchanged direct class.
- `CpuPreparedExecutableTest` explicitly finalizes and runs retained candidates for copy-once,
  zero-range/copy-failure, and original-source/output-overlap cases. Its oracle remains cold
  validation before writes, exactly-once copy work, consumer ordering, atomic failure behavior,
  and original-boundary alias protection; it must not expect ordinary selected workspaces.
- `CpuMaterializationPlanTest` reads the retained candidate's exact copy geometry, costs,
  workspace identity, and requirement accounting while asserting the ordinary selected plan is
  CPU 0008D direct with zero copies and `DIRECT_MATERIALIZATION_UNPROVED`.
- `CpuPartitionFinalizerTest` finalizes an explicit retained candidate and preserves the exact
  buffer/workspace assignment, worker-before-artifact, artifact-count, and absent-persistence-
  root failure oracles. Ordinary finalization remains direct.
- `CpuPartitionPreparerTest` replaces promotion expectations with two coordinated oracles:
  ordinary enabled preparation selects exact CPU 0008D direct/zero copies/unproved reason, while
  retained variants preserve the same eligibility, cost, source order, workspace identities,
  resource geometry, and rejection facts previously inspected through the selected plan.
- No new production API, test-only production branch, caller detection, blacklist, configuration
  knob, cache/tuning behavior, or ordinary-selection escape hatch is permitted. If the existing
  CPU-private records/finalizer/executable machinery cannot construct and execute the exact
  retained candidate without another production owner, stop and report architectural uncertainty
  instead of inventing a route.

### Status transition

- After implementation, authoritative CPU validation, retained-evidence verification, and clean
  documentation review, 0008E may become Complete. Completion means candidate/execution machinery
  and safe ordinary selection are truthful; it does not claim autotuning exists.
- CPU 0008F becomes Ready only after 0008E completes. Do not create its task specification here.

## Out of scope

- New legality, topology, recognition, pointwise semantics, BFLOAT16 arithmetic, MATMUL, pooling,
  attention, loss, native packing/reorder, route, or generated family.
- Timing-derived constants, type/carrier blacklists, favorable fixtures, retry, evidence
  replacement/relabeling, runtime or prepare-time measurement, tuning-cache access/mutation,
  public/shared API, Config, Prepare 0004, CPU 0016, or Tuning 0001–0002 implementation.
- Static promotion based only on estimated cost, reuse count, cross-unit reuse, type, carrier,
  shape bucket, or previously passing finite rows.
- Runtime selection/copying, hidden coherence, persistence, pooling, cross-partition reuse, new
  resources/schema, architecture, dependency, Gradle, conformance, or integration changes.
- Java/tests, Javadocs, CPU-guide prose, or glossary prose changes in this planning/review
  context. The final clean documentation context is separately authorized below to correct the
  CPU guide and the three named existing glossary entries.
- Any follow-up test change outside the seven named corrective owners, including changes to the
  already passing `CpuRepresentationPlannerTest`.

## Architecture references

- [`ARCHITECTURE.md`](../../../../../ARCHITECTURE.md).
- [`Current architecture documentation`](../../../../architecture/current-architecture-plan.md).
- [`Runtime, Prepare, and Backend boundary`](../../../../architecture/runtime-prepare-backend-boundary.md).
- [`Performance evidence and model autotuning`](../../../../architecture/performance-evidence-and-tuning.md).
- [`Planning guide`](../../../planning-guide.md).
- [`CPU master plan`](../master-plan.md).
- Complete [`CPU 0005F`](0005f-floating-division-and-exact-scalar-power-realization.md),
  [`CPU 0008B`](0008b-general-partition-dag-computation-unit-decomposition-and-bounded-fusion.md),
  [`CPU 0008C`](0008c-typed-specialized-subgraph-and-epilogue-recognition.md), and
  [`CPU 0008D`](0008d-bounded-fusion-profitability-and-typed-decision-facts.md).

## Architecture constraints

- Planning selects CPU ownership only. CPU analysis owns private complete candidates, safe
  ordinary selection, exact declarations, and opaque plan state.
- Ordinary analysis is deterministic from explicit facts/configuration and performs no
  measurement, search, or tuning-cache consumption here.
- Shared Prepare assigns only selected exact resources and does not interpret private candidates.
  CPU finalization cannot rerank or add undeclared needs.
- Runtime executes immutable prepared work and performs no graph inspection, representation
  choice, tuning, cache access, or setting mutation.
- Future model autotuning may measure complete candidates end to end and supply an explicit
  compatible selection before analysis through later-owned contracts. This task preserves the
  candidates but does not implement that handoff.
- Candidate-only generated code still obeys the optimal-clean-Java oracle discipline.
- Any public/shared type, another module, route, generated form, resource model, schema,
  dependency, build, or architecture need is a stop condition.

## Package impact

Existing packages used:

- `io.github.pho001.synaptik.backend.cpu.internal.ir` — closed selection reason and facts.
- `io.github.pho001.synaptik.backend.cpu.internal.lowering` — bounded enumeration and direct
  ordinary-selection policy.

Packages added or changed:

- None.

Type placement:

- `CpuRepresentationDecision` — add only the exact direct-unproved selection reason.
- `CpuRepresentationPlanner` — preserve enumeration; select CPU 0008D direct for ordinary work.
- `CpuRepresentationPlannerTest` — retained-candidate/direct-selection/evidence regression.

## Affected files

The narrow production correction remains limited to:

- `backends/cpu/src/main/java/io/github/pho001/synaptik/backend/cpu/internal/ir/CpuRepresentationDecision.java`;
- `backends/cpu/src/main/java/io/github/pho001/synaptik/backend/cpu/internal/lowering/CpuRepresentationPlanner.java`; and
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/lowering/CpuRepresentationPlannerTest.java`.

The corrective follow-up may additionally change only these seven existing test owners:

- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/cache/CpuKernelSpecializationTest.java`;
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuFusedGeneratedKernelTest.java`;
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuShapePolymorphicArtifactTest.java`;
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/executable/CpuPreparedExecutableTest.java`;
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/lowering/CpuMaterializationPlanTest.java`;
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/prepare/CpuPartitionFinalizerTest.java`; and
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/prepare/CpuPartitionPreparerTest.java`.

Completion planning remains limited to this task, CPU master plan, and roadmap. The later clean
documentation context must finalize affected Javadocs and may edit only
`docs/backend-guide/cpu-backend.md` and `docs/glossary.md` outside the existing implementation and
three planning paths.
The guide must replace its stale one-FLOAT64-input, planned-multi-input, and one-unit-only claims
with the implemented candidate boundary: bounded single-copy and eligible disjoint-pair
candidates across FLOAT64, FLOAT32, INT32, INT64, and canonical BOOL pointwise work; rejection of
a pair co-consumed by one represented instruction; one compatible copy reused across repeated and
cross-unit uses; explicit copy units before represented consumers; and complete candidate
finalization and execution. It must state that ordinary preparation selects direct and keeps
materialized forms candidate-only, future model autotuning may explicitly promote a compatible
candidate only after end-to-end proof, and Runtime never selects. It must preserve the distinction
between these anonymous external-read contiguous-copy workspaces and CPU 0008B's ordinary graph
split `Buffer(ValueId)` resources. The same clean pass must correct exactly the existing
`CPU portable preparation plan`, `CPU contiguous materialization plan`, and `Materialization`
glossary entries. Those entries must describe bounded candidate plural across FLOAT64, FLOAT32,
INT32, INT64, and canonical BOOL; retained singles and eligible disjoint-consumer pairs;
`CO_CONSUMED_PAIR` rejection when one represented instruction consumes both sources; one
compatible copy reused across repeated and cross-unit uses; complete candidate-only ordinary
policy with direct ordinary selection; and no Runtime selection. They must distinguish these
CPU-private external-read workspace candidates from CPU 0008B's ordinary graph split
`Buffer(ValueId)` resources. Do not add a glossary heading unless the existing terms cannot be
corrected accurately. Every production path other than the two narrow correction owners, every
test/evidence owner other than the already preserved set plus the seven explicit corrective
owners, and every public/shared, Gradle, architecture, conformance, integration, or 0008F path is
frozen. In particular, do not change materialization policy/plan, preparer, finalizer, executable,
emitter, generator, carrier, schema/cache, or identity production behavior merely to restore a
test assumption.

## Maximum scope

- Expand the final ceiling from the preserved 34-path accounting to exactly 43 paths:
  14 CPU production/Javadoc, 24 CPU test/evidence, and exactly 5 documentation/planning paths:
  the CPU guide, glossary, this task, CPU master plan, and roadmap. The expected final worktree is
  exactly 36 paths after the guide and glossary are added to the current 34.
- The production correction touches at most the 2 named production paths and its already passing
  focused test owner. The follow-up touches only the 7 named stale test owners, creates no path or
  type, and must not consume the enlarged ceiling for another owner.
- Preserve ceilings of 64 legal topologies, 8 eligible sources/topology, 36 copy choices,
  37 variants/topology, 2,368 variants, 2 copy identities/candidate, 16 consumer-use facts,
  8+2 units/workspaces/artifacts, 74 requirements, and 2,753 decision facts.
- Ordinary selected copies now have exact maximum zero. This does not remove two-copy candidates
  or executable machinery.
- Stop and replan for another file, type, package, owner, form, resource, schema, or ceiling.

## Acceptance criteria

- All admitted direct/single/disjoint-pair identities remain complete, stable, ordered, bounded,
  and inspectable; `CO_CONSUMED_PAIR` remains rejected and its singles retained.
- Costs, identities, resources, copy plans, generated artifacts, finalization, copy-once reuse,
  direct identity, schema 53, and safe fallbacks remain unchanged.
- Disabled policy preserves CPU 0008D-selected direct with `DIRECT_POLICY_DISABLED`.
- Enabled complete policy selects exact CPU 0008D direct with zero materializations and the new
  typed direct-unproved reason. No copied variant becomes selected.
- Identical final2 facts retain the exact slow candidate and select direct without changing
  carrier, policy, topology, mapping, shape, work, or evidence.
- Cross-unit reuse remains candidate identity/executable behavior, never sufficient promotion
  proof.
- All nine stale assertions are replaced by explicit retained-candidate or ordinary-direct
  oracles in exactly the seven authorized owners; no test forces ordinary materialization.
- Explicit candidate construction, finalization, and execution use existing CPU-private records
  and machinery and reproduce the retained `VariantIdentity` exactly. If that cannot be proved
  without a new production owner, implementation stops with architectural uncertainty.
- Candidate semantic execution, copy-once behavior, Class-File shape, absence of hidden helper/
  allocation/boxing/reflection/dispatch, and generated/oracle equivalence remain proved.
- Final2 CSV verifies at
  `c55d07272272fb484b4a6580312aba34aa67483e404f42ecddaf8a717d7f4612`; its five failures
  remain recorded and are not rerun.
- Older co-consumed evidence remains at
  `d581b84022d259fd92805e8ad26aab0d6e6437ace96aaed89eef08d8eb15f26d`.
- Direct pointwise class remains
  `5bb43f82369fcb43a3eb50b9be42d856016ec91bab3aeb0f83b4b0af928a6efd`; affected affine
  class remains `fad075e7abd6586cdd53da6e2787ef77d8a92f972cf6b6fb66a05cf501cb4369`.
- The failed 544-test/9-failure/3-skip full run remains discovery evidence, never a passing gate.
- The completed executable/test edits pass the 115-test focused planner-plus-seven-owner matrix
  and the one final `./gradlew :backends:cpu:test --rerun-tasks` gate with 544 tests across 104
  suites, 3 expected skips, and no failures/errors. No new promotion benchmark is required because
  no materialized form is statically selected.
- Clean documentation review finalizes affected Javadocs and the CPU guide, corrects the three
  named stale glossary entries without adding a heading unless necessary, and passes Javadoc/
  documentation validation without changing executable Java behavior.
- Exact paths, Ready/Draft synchronization, no 0008F spec, empty staging, links, headings, fences,
  final newlines, and whitespace checks pass.

## Tests / validation

The implementation context already ran the authorized focused matrix. Its 115 passing tests prove
candidate identities are unchanged; co-consumed rejection, singles, disjoint pairs, and reuse
facts remain; exact final2 facts select direct/zero copies/new reason; previously favorable
single/pair/reuse fixtures are also candidate-only; disabled/incomplete/overflow/uncertainty paths
stay exact; explicit retained candidates preserve plan, resource, finalization, execution,
semantic, specialization, and Class-File oracles; and no test times a known-unselected plan.

Recorded focused implementation validation:

```bash
./gradlew :backends:cpu:test \
  --tests io.github.pho001.synaptik.backend.cpu.internal.lowering.CpuRepresentationPlannerTest \
  --tests io.github.pho001.synaptik.backend.cpu.internal.cache.CpuKernelSpecializationTest \
  --tests io.github.pho001.synaptik.backend.cpu.internal.codegen.emit.CpuFusedGeneratedKernelTest \
  --tests io.github.pho001.synaptik.backend.cpu.internal.codegen.emit.CpuShapePolymorphicArtifactTest \
  --tests io.github.pho001.synaptik.backend.cpu.internal.executable.CpuPreparedExecutableTest \
  --tests io.github.pho001.synaptik.backend.cpu.internal.lowering.CpuMaterializationPlanTest \
  --tests io.github.pho001.synaptik.backend.cpu.internal.prepare.CpuPartitionFinalizerTest \
  --tests io.github.pho001.synaptik.backend.cpu.internal.prepare.CpuPartitionPreparerTest
```

Record the already executed discovery result exactly:

```text
./gradlew :backends:cpu:test --rerun-tasks
544 tests, 3 skipped, 9 failures in exactly seven existing test owners — discovery failure
```

Recorded final implementation validation after the last executable change:

```bash
./gradlew :backends:cpu:test --rerun-tasks
```

Verify without launching a performance fork:

```bash
shasum -a 256 /private/tmp/synaptik-cpu-0008e-schema53-authoritative-evidence-v3-final2-20260827/forks/fork-0.csv
```

Also verify older co-consumed evidence, control classes/disassembly, schema 53, selected/direct
identity, and no forbidden owner/path leakage.

The later documentation context reuses the 115-test focused and final 544-test/104-suite evidence;
it must not rerun Java tests because it is not authorized to change executable behavior. It runs:

```bash
./gradlew :backends:cpu:javadoc
git diff --check
git diff --cached --check
git status --short -uall
```

It validates canonical 20 headings, fences, final newlines, local links/anchors, the exact
14-production/24-test/5-documentation-and-planning ceiling and implementation allowlist, expected
36-path final worktree, schema/status synchronization, no 0008F spec, and empty staging. Repository-wide,
architecture, conformance, and integration validation remain CPU 0009/CI.

## Dependencies

- Complete CPU 0005D–0005F pointwise/materialization and CPU 0006 affine-copy foundations.
- Complete CPU 0008B decomposition/resources/execution, CPU 0008C recognition, and CPU 0008D
  topology profitability/selection facts.
- Existing Planning, Prepare, Runtime, Config, Trace, artifact-cache, and tuning boundaries.
- Preserved 34-path schema-53 diff and immutable evidence named above.

No architecture or shared-contract blocker is known.

## Follow-up tasks

- CPU 0008F becomes Ready only after 0008E completes; do not create its specification here.
- Config 0006A owns future immutable model-autotuning request inputs.
- Prepare 0004 owns the future narrow opaque complete-candidate handoff.
- CPU 0016 owns compatible CPU workload-cache selection and safe fallback without measurement or
  cache mutation.
- Tuning 0001 owns local workload measurement/cache population. Tuning 0002 owns bounded
  end-to-end complete-plan measurement and explicit compatible materialization promotion. Its
  acceptance includes copy plus every consumer; isolated affine-copy parity is insufficient.
- CPU 0009 owns generated coverage, capability, conformance, and integration closure.

These follow-ups do not hide incomplete 0008E work: 0008E delivers complete candidates and
execution plus truthful ordinary direct selection; later tasks add the already assigned explicit
measurement/handoff/cache lifecycle.

## Architecture impact

Expected impact: None.

The architecture permits private complete candidates and safe ordinary heuristics when tuning is
absent. Evidence shows this heuristic cannot safely promote materialization, so direct is the safe
fallback. Candidate generation, CPU lowering/resources/finalization, Prepare opacity, future
pre-runtime tuning, and Runtime's execute-only boundary remain unchanged. No architecture doc,
ADR, architecture test, shared API, dependency, or build change is needed.

## Implementation prompt

The implementation prompt has been fulfilled. Use this final documentation prompt in a separate
clean documentation-focused task/thread:

```text
You are the mandatory separate clean-context documentation-focused agent for Synaptik CPU task
0008E. Do not commit, stage, or push. Do not change executable Java behavior or tests, and do not
rerun Java tests.

Read AGENTS.md, ARCHITECTURE.md, current architecture plan, runtime/Prepare/backend and
performance/tuning architecture docs, documentation rules and the General/Javadoc/backend-guide/
planning profiles, planning guide, roadmap, CPU master plan, complete CPU 0008B/0008D/0008E tasks,
the complete current diff, affected source/tests/Javadocs, CPU guide, and glossary.

Reuse the implementation evidence: the focused matrix passed 115 tests; the final full CPU run
passed 544 tests across 104 suites with 3 expected skips and no failures/errors. No executable
change occurred afterward. Finalize affected Javadocs and correct only the stale passages in
docs/backend-guide/cpu-backend.md and the existing CPU portable preparation plan, CPU contiguous
materialization plan, and Materialization entries in docs/glossary.md. Explain bounded single and
eligible disjoint-pair candidate
generation/execution across FLOAT64/FLOAT32/INT32/INT64/BOOL, CO_CONSUMED_PAIR rejection,
compatible repeated/cross-unit copy reuse, explicit copy units, candidate finalization/execution,
ordinary direct selection, candidate-only materialized forms, future explicit end-to-end-proved
autotuning promotion, and Runtime's execute-only boundary. Preserve the distinction from CPU
0008B graph split buffers. Correct the three existing glossary terms in place and add no glossary
heading unless those terms cannot accurately express the implemented boundary.

The final ceiling is 43 paths: 14 production/Javadoc, 24 test/evidence, and 5 documentation/
planning paths, with exactly 36 actual paths expected when the CPU guide and glossary are added to
the current 34.
Finalize this task, CPU master plan, and roadmap only after documentation checks pass. Run CPU
Javadoc, headings/fences/newlines/local-link checks, exact-path and empty-staging checks,
git diff --check, and git diff --cached --check. Keep 0008E Ready until this pass completes, keep
0008F Draft with no specification, and do not mark 0008E Complete before every documentation
acceptance criterion passes.
```

## Local decisions

- All materialized forms are candidate-only for ordinary preparation. Failures now cover both a
  co-consumed pair and an honest one-copy plan while generated/oracle parity remains good.
- Cross-unit reuse is not sufficient static eligibility: it may amortize work, but no proof
  covers the admitted domain, and filtering it would discard future tuning candidates.
- Numeric costs remain diagnostic candidate metadata, not promotion proof. No measured constant
  or blacklist is justified.
- CPU 0008D-selected direct topology, not necessarily canonical split, is selected when complete;
  topology profitability remains intact while only representation promotion is removed.
- `CO_CONSUMED_PAIR` remains because the older failure disproved it and the new result does not
  contradict that correction.
- Candidate-only is a completed 0008E end-state because future end-to-end selection already has
  explicit Prepare/tuning/cache owners and Runtime remains selection-free.
- The nine failed full-suite cases are stale test oracles, not permission to restore promotion.
  Retained candidates are inspected or executed explicitly; ordinary analysis remains direct.
- Existing CPU-private plan records, shared-assignment test support, finalizer, and executable
  machinery are the intended candidate-test route. A demonstrated need for another production
  owner is an architectural-uncertainty stop, not an implementation improvisation.
- The CPU guide and three existing glossary entries are demonstrably stale and must be corrected
  in the final clean documentation pass. The glossary correction must remain within those terms
  unless accurate wording requires a new heading.
- Open questions: None.

## Known limitations

- Ordinary preparation does not select external-read materialization until a compatible future
  end-to-end tuning decision is supplied through the owned lifecycle.
- Existing at-most-two-copy and all eligibility/identity/resource ceilings remain.
- Co-consumed pairs remain rejected; BFLOAT16 awaits numerical support.
- Evidence proves implemented forms on retained cases, not universal speedup. 0008E makes no
  materialization speedup claim.

## Validation evidence

- Clean planning/review context: `01a042d6-9dd5-77d1-b729-79cc9a14b8e0`.
- It read required architecture/planning/predecessor/source/test/diff/evidence records and changed
  only this task, CPU master plan, and roadmap. It performed no Java/test/documentation-prose
  implementation, staging, commit, push, evidence replacement, or performance retry.
- Final2 evidence: generated/oracle median `1.000326680`; selected/direct median `2.605250934`;
  samples `2.615038667`, `2.606896076`, `2.498279430`, `2.583623032`, `2.613269873`; SHA-256
  `c55d07272272fb484b4a6580312aba34aa67483e404f42ecddaf8a717d7f4612`.
- Older co-consumed median `1.444574317` and SHA-256
  `d581b84022d259fd92805e8ad26aab0d6e6437ace96aaed89eef08d8eb15f26d` remain retained.
- Generated controls remain direct pointwise
  `5bb43f82369fcb43a3eb50b9be42d856016ec91bab3aeb0f83b4b0af928a6efd` and affine
  `fad075e7abd6586cdd53da6e2787ef77d8a92f972cf6b6fb66a05cf501cb4369`.
- Final planning validation passed: canonical 20-heading order, balanced fences, final newlines,
  all local Markdown link targets, exact Ready 0008E/Draft 0008F synchronization, no 0008F task,
  exact 34-path worktree, exactly the three authorized planning paths relative to the preserved
  implementation diff, empty staging, retained final2 SHA-256, `git diff --check`, and
  `git diff --cached --check`. At that planning checkpoint, implementation validation was pending
  the next isolated context; the later evidence below supersedes that pending state.
- Corrective planning context: `01a042ed-60f8-7961-becd-6c5b09c52730`. It inspected the actual
  34-path worktree and the complete failed XML reports. The discovery run contains 544 tests,
  3 skips, and exactly 9 failures in the seven authorized owners: one specialization-identity,
  one fused-generated, one shape-polymorphic-artifact, three prepared-executable, one
  materialization-plan, one finalizer, and one preparer failure. Each failure traces to an empty
  ordinary selected-copy, representation-unit, or workspace assumption after the final
  precedence correction; no new production failure category was discovered.
- Corrective planning validation passed: the current worktree remains exactly 34 unique paths
  (14 production/Javadoc, 17 test/evidence, 3 planning) inside the newly authorized exact
  41-path ceiling; only the three permitted planning paths changed in this context. The task has
  canonical 20-heading order, 12 balanced fences, final newline, and resolving local link targets.
  Both retained fork hashes reproduce exactly. 0008E remains the sole Ready CPU row, 0008F remains
  Draft with no task specification, the staged index is empty, and `git diff --check` plus
  `git diff --cached --check` pass.
- The first local Ruby link/anchor helper failed before reading any file because `#{1,6}` was
  parsed as string interpolation. The compatible retry used an explicit heading-marker capture
  and passed all local target and same-file anchor checks. This was a validator-command syntax
  error, not a planning-document failure.
- Implementation and final validation are complete. The focused corrective matrix passed 115
  tests. The final `./gradlew :backends:cpu:test --rerun-tasks` passed 544 tests across 104 suites
  with 3 expected skips and no failures or errors.
- Mandatory documentation context `01a04303-9552-72a0-a5dc-c95d20413779` stopped without edits
  because the then-current task did not authorize the demonstrably stale CPU guide. The following
  planning context authorized that one targeted guide path and raised its then-current ceiling to
  42 paths; the glossary-scope correction below supersedes that ceiling.
- Final documentation-scope planning context: `01a04307-34ef-7841-9a0f-e58adcbb0faa`. It changed
  only this task, CPU master plan, and roadmap; ran no Java tests; preserved the exact 34-path
  worktree baseline (14 production/Javadoc, 17 test/evidence, 3 planning); and confirmed empty
  staging, Ready 0008E, Draft 0008F with no task specification, canonical 20-heading order, 12
  balanced fences, final newline, 11 resolving local task links, `git diff --check`, and
  `git diff --cached --check`.
- Replacement documentation context `01a0430b-de37-7de3-bfa1-9b21f2cd21d3` stopped without edits
  because its glossary audit found three stale entries while the then-current task explicitly
  prohibited glossary edits: `CPU portable preparation plan` still limited materialization to an
  optional one-input workspace, `CPU contiguous materialization plan` still allowed at most one
  FLOAT64 copy, and `Materialization` still described one selected CPU workspace copy. This
  planning correction authorizes their targeted final clean-context correction.
- Glossary-scope planning context `01a04311-58d5-7230-9312-b1d590b9d6fe` read the required
  architecture/planning/documentation contracts, the complete current task/master/roadmap, and
  the exact three stale glossary entries. It changed only this task, CPU master plan, and roadmap;
  ran no Java tests, Javadoc, or performance work; and preserved all implementation and evidence
  decisions. Static validation confirmed exactly 34 current paths (14 production/Javadoc, 17
  test/evidence, and 3 planning), empty staging, canonical 20-heading order, balanced fences,
  final newlines, resolving local links/anchors, Ready 0008E, Draft 0008F with no task
  specification, `git diff --check`, and `git diff --cached --check`. The first link-helper attempt
  repeated the historical Ruby interpolation error, and the second encountered the local Ruby's
  lack of `filter_map`; the compatible `map`/`compact` retry passed. Neither failed command read or
  changed repository content.
- Final clean documentation context `01a04317-b784-76e3-a93b-ff35106284b9` independently read the
  governing architecture, planning, documentation profiles, complete diff, affected production
  contracts, and focused tests. It changed Javadocs only inside the already affected CPU
  production allowlist, corrected `docs/backend-guide/cpu-backend.md`, and updated exactly the
  existing `CPU portable preparation plan`, `CPU contiguous materialization plan`, and
  `Materialization` glossary entries without adding a heading. It changed no Java behavior or
  tests and reused the recorded 115-test focused and 544-test/104-suite final CPU evidence.
- The final documentation context ran only `./gradlew :backends:cpu:javadoc` for Java validation.
  Its first successful run exposed 99 documentation warnings in the two new representation types
  and the new representation-aware executable constructor. After completing those missing enum,
  record-component, constructor, and boundary descriptions, the final run passed with only the two
  expected incubating-Vector-module warnings. Relevant rendered pages were inspected for
  representation candidates, typed selection reasons, copy reuse, finalization, and Runtime
  boundaries. Markdown heading/link/anchor/fence/final-newline,
  terminology, schema-53/status, exact-path, no-0008F-spec, unchanged-`CpuCarrierEmitter`, empty-
  staging, and whitespace checks passed, including `git diff --check` and
  `git diff --cached --check`.
- The first final link-helper attempt used unavailable local Ruby `filter_map`; its compatible
  `map`/`compact` retry then exposed that the helper's initial slug algorithm collapsed the two
  spaces around glossary heading slashes and therefore falsely rejected established double-hyphen
  anchors. The GitHub-compatible retry preserved those spaces and passed every local target and
  anchor. These were validator compatibility errors; neither command changed repository content.

## Implementation notes

- The preserved diff already supplies complete bounded facts, explicit copy workspaces/units,
  finalizer/executable chain, reuse, affine cursor, schema-53 invalidation, and stable direct
  identity. All remain unaffected.
- The prior `CO_CONSUMED_PAIR` correction and generated parity remain valid. Final2 independently
  disproves one-copy static promotion, so this replan changes selection authority only.
- The narrow production correction already retains enumeration, returns CPU 0008D-selected direct
  with zero copies, and records why materialization remains unproved.
- The completed corrective follow-up changed only the seven named test owners. Each reads the retained
  candidate identity explicitly; tests needing concrete work construct the exact candidate with
  existing CPU-private plan/finalization/execution machinery and keep ordinary selection direct.

## Completion summary

- Completed changes: complete bounded direct/single/disjoint-pair representation candidates,
  typed `CO_CONSUMED_PAIR` rejection, compatible copy-once reuse, exact resources/generated units,
  finalization/execution, schema 53, and truthful ordinary direct selection with
  `DIRECT_MATERIALIZATION_UNPROVED`.
- Files changed or created: exactly 36 worktree paths—14 CPU production/Javadoc, 17 CPU
  test/evidence, and 5 documentation/planning paths comprising the CPU guide, glossary, this task,
  CPU master plan, and roadmap—within the authorized 43-path ceiling.
- Tests and validation: reused the implementation context's passing 115-test focused matrix and
  final 544-test/104-suite CPU run with 3 expected skips and no failures/errors. The documentation
  context ran only `./gradlew :backends:cpu:javadoc`, which passed, plus the required rendered-
  Javadoc and static documentation/worktree checks.
- Documentation-agent review: context `01a04317-b784-76e3-a93b-ff35106284b9` finalized the CPU
  guide, affected Javadocs, and exactly the three authorized glossary entries.
- Documentation impact: current candidate/ordinary-selection/future-promotion boundaries are
  documented and CPU 0008E external-read workspaces are distinguished from CPU 0008B graph-split
  buffers.
- Javadoc review: finalized within the 14 already affected production paths; generated relevant
  pages were inspected after the passing Javadoc run.
- Glossary impact: corrected the existing `CPU portable preparation plan`, `CPU contiguous
  materialization plan`, and `Materialization` terms in place; no new heading was necessary.
- No-change conclusions: architecture and focused architecture docs, public/shared APIs, other
  modules, tests, build/Gradle, `CpuCarrierEmitter`, backend conformance, integration, and Runtime
  selection behavior required no documentation or implementation change.
- Unresolved issues: None.
- Follow-up required: None. CPU 0008F is the next Ready CPU row and intentionally has no task
  specification yet.

Status: Complete
