# Task 0008Q1A: Vector Scalar-Power Hot-Path Self-Containment

## Status

Complete

## Goal

Make every currently generated FLOAT32 and FLOAT64 vector scalar-power specialization self-contained with respect to Synaptik production helpers. Emit the proved `POSITIVE_ONE`, `IDENTITY`, `SQUARE`, and `RECIPROCAL` realizations directly so each generated Class-File preserves the matching optimal clean Java specialization's semantic algorithm, vector-chunk and scalar-tail dataflow, and avoidable-overhead profile.

This is structural and performance remediation discovered while executing CPU 0008Q1. It does not require literal byte-for-byte Class-File identity or guarantee identical just-in-time compiler assembly.

## Scope

- Replace the generated vector `SCALAR_POW` calls to `CpuVectorMath` for `POSITIVE_ONE` and `RECIPROCAL` with direct typed Java Vector API bytecode. Keep `IDENTITY` as a direct value and `SQUARE` as one direct typed vector multiply, and test all four realizations under one fail-closed contract.
- Cover every currently generated vector scalar-power form for FLOAT32 and FLOAT64, including `VECTOR` and caller-parallel `PARALLEL_VECTOR` orchestration; primitive-array input/output, native-order `MemorySegment` input/output, and every currently admitted ordered mixed array/segment input/output carrier pair; arbitrary legal ranges; complete vector chunks; and scalar tails.
- Cover standalone scalar-power occurrences and representative legal fused pointwise occurrences whenever surrounding instructions change local/dataflow shape. Derive the finite form inventory from current lowering, preparation, specialization, and generation rather than only the first failing CPU 0008Q1 fixture.
- Preserve exact represented exponent bits and `PowerRealization` identity. `DIRECT` remains vector-ineligible and uses the safe scalar or caller-parallel scalar fallback; no scalarization inside a vector body, lane-wise `Math.pow`/`StrictMath.pow`, approximation, or new realization is allowed.
- Advance the current-only generator compatibility schema exactly once after generated bytes change. Old artifacts are safe misses; do not add a migration reader or weaken specialization identity.
- Add semantic tests against independent typed optimal clean Java specializations for all affected realizations, types, carriers, ranges, vector chunks, and tails, including NaN classification, infinities, signed zero, subnormals, overflow, and underflow where applicable.
- Add automated Class-File/decompilation tests for direct descriptors, carrier access, vector loop and scalar-tail dataflow, exact allowed JDK calls, and absence of Synaptik-owned hot-path calls, allocation, boxing, reflection, method handles, dynamic calls, maps, string dispatch, generic carrier branches, and avoidable virtual/interface dispatch.
- Produce proportionate generated-versus-direct performance evidence from fixed equivalent Java 26 specializations. Use a source-derived finite matrix covering both lane types, all four realizations, each distinct carrier-access code shape, vector chunks and tails, and both orchestration modes; project a timed row only when Class-File inspection proves the omitted dimension does not change the relevant hot body.
- Review and finalize affected Javadocs, package summaries, the CPU backend guide, glossary impact, and planning evidence in a separate clean documentation-focused context after implementation.

## Out of scope

- Scalar-only or BFLOAT16 scalar-power behavior, scalar DIV, ordinary vector `RECIPROCAL`, unrelated `CpuVectorMath` formulas, scalar-immediate arithmetic, clamp, Tensor/Tensor power, or other pointwise operations.
- New semantics, capability, lowering, access regimes, carriers, route/strategy selection, materialization policy, fusion policy, public API, configuration, tuning, native routes, or architecture changes.
- Removing `CpuVectorMath` where it remains the established vector boundary for unrelated transcendental/activation operations.
- Treating an optimal clean Java oracle as a production fallback, mutating production settings from benchmark results, or committing generated classes, decompilation output, or timing artifacts.
- Claiming literal generated/direct Class-File identity, byte-for-byte equality, or guaranteed identical JIT assembly.

## Architecture references

- [`ARCHITECTURE.md`](../../../../../ARCHITECTURE.md): CPU ownership and generated-code semantic, hot-loop/dataflow, and avoidable-overhead parity with optimal clean Java.
- [Current architecture plan](../../../../architecture/current-architecture-plan.md).
- [Performance evidence and tuning](../../../../architecture/performance-evidence-and-tuning.md).
- [Planning Guide](../../../planning-guide.md): task ordering, validation, evidence, and clean-context handoff.
- [Documentation rules](../../../../developer-guide/documentation-rules.md), [General profile](../../../../developer-guide/documentation/general-style.md), and [Planning profile](../../../../developer-guide/documentation/planning-style.md).
- [CPU master plan](../master-plan.md), [CPU 0005F](0005f-floating-division-and-exact-scalar-power-realization.md), [CPU 0008Q](0008q-scalar-immediate-clamp-generated-equivalence.md), and blocked [CPU 0008Q1](0008q1-finite-scalar-immediate-clamp-matrix.md).

## Architecture constraints

- Model semantics and exact represented exponent bits remain unchanged. CPU preparation owns realization and route selection; finalization owns generation; Runtime invokes the prepared direct artifact without graph, operation, helper, or route dispatch.
- Generated hot execution remains typed and direct for its exact ordered carrier pattern. It performs no Synaptik helper invocation for any proved vector scalar-power realization and introduces no allocation, boxing, reflection, lookup, or semantic dispatch.
- The generated algorithm and hot-loop/dataflow shape must match the corresponding well-written clean Java specialization. A safe scalar fallback remains only where vector specialization cannot be proved, including `DIRECT`.
- Benchmarking is observational and fail-closed. A failing or incomplete row cannot justify the changed vector form or mutate production selection.

## Package impact

Existing packages used:

- `io.github.pho001.synaptik.backend.cpu.internal.codegen.emit` — direct vector scalar-power emission, Class-File structural tests, semantic oracles, and performance harness.
- `io.github.pho001.synaptik.backend.cpu.internal.cache` — current-only generator schema and compatibility tests.
- `io.github.pho001.synaptik.backend.cpu.internal.prepare` — existing strategy/carrier admission and selection tests only.

Packages added or changed:

- None.

Type placement:

- `CpuVectorInstructionEmitter` remains the generation-time owner of direct typed vector scalar-power emission.
- `CpuVectorMath` remains the package-private owner of unrelated vector formulas; this task narrows only scalar-power generated references to it.
- A focused `CpuVectorScalarPowerSelfContainmentTest` and optional package-private test oracle/harness belong in `.internal.codegen.emit` because they inspect emitted Class-Files and execute direct generated entries.

## Affected files

Changed production and contract paths:

- `backends/cpu/src/main/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuVectorInstructionEmitter.java`
- `backends/cpu/src/main/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuClassFileKernelGenerator.java`
- `backends/cpu/src/main/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuLoopEmitter.java`
- `backends/cpu/src/main/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/package-info.java`
- `backends/cpu/src/main/java/io/github/pho001/synaptik/backend/cpu/internal/cache/CpuGeneratorSchema.java`

Changed build and test paths:

- `backends/cpu/build.gradle.kts`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuVectorScalarPowerSelfContainmentTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuVectorScalarPowerPerformanceTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/cache/CpuGeneratedKernelArtifactStoreTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/cache/CpuKernelSpecializationTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuBatchNormTrainingEvidenceTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuConv2dEvidenceTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuConv3dEvidenceTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuConvSimdEvidenceTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuPartitionDagGeneratedEvidenceTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuPointwiseLedgerEvidenceTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/codegen/emit/CpuPointwiseMaskEvidenceTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/prepare/CpuPartitionFinalizerTest.java`
- `backends/cpu/src/test/java/io/github/pho001/synaptik/backend/cpu/internal/prepare/CpuPartitionPreparerTest.java`

Changed documentation and planning paths:

- `docs/backend-guide/cpu-backend.md`
- this task
- [CPU 0008Q1](0008q1-finite-scalar-immediate-clamp-matrix.md)
- [CPU 0009](0009-portable-generated-coverage-closure-checkpoint.md)
- [CPU master plan](../master-plan.md)
- [roadmap](../../../roadmap.md)

The six existing untracked CPU 0008Q1 test/resource paths are evidence inputs owned by 0008Q1 and must not be edited by this remediation task.

## Maximum scope

The planned limit was five production/Javadoc paths, eight test paths, two explanatory-documentation paths, five planning paths, and 20 repository paths total. The completed change has five production/Javadoc paths, thirteen test paths, one build path, one explanatory guide, and five planning paths: 25 repository paths. This source-derived exception was necessary because the schema-63-to-64 migration required all eleven repository assertions of the global `CURRENT_VERSION` to move atomically, in addition to the two new task tests. It did not broaden production behavior, public API, dependencies, or module scope. Performance output remains only beneath the fresh caller-supplied `/private/tmp/synaptik-cpu-0008q1a-*` root, and none of the six protected CPU 0008Q1 paths changed.

## Acceptance criteria

- Every source-reachable FLOAT32/FLOAT64 `POSITIVE_ONE`, `IDENTITY`, `SQUARE`, and `RECIPROCAL` vector scalar-power artifact across array, segment, and ordered mixed carriers has a direct typed entry and no member reference whose owner begins `io.github.pho001.synaptik`.
- Structural dossiers show the same semantic algorithm, carrier loads/stores, vector chunk loop, scalar tail, and avoidable-overhead profile as the matching clean Java specialization. Differences in constant-pool layout, labels, stack-map encoding, class naming, or other non-semantic Class-File details are allowed and recorded.
- Raw semantic results match the independent oracle for all affected forms and edge cases. `DIRECT` remains scalar/parallel-scalar and no vector body silently scalarizes.
- Generated artifacts contain no forbidden allocation, boxing, reflection, method-handle/dynamic invocation, map/string dispatch, generic carrier branch, hidden helper, or avoidable dispatch. Automated tests inspect the complete generated entry, not only the first fixture.
- The generator schema advances exactly once after bytes change; current-only cache/persistence tests prove old artifacts are safe misses and no migration path appears.
- One sealed external evidence root contains source and class hashes, environment/protocol metadata, generated and direct classes, complete `javap -c -p`/`javap -v -p` output, member-reference reports, raw results, summaries, and a SHA-256 manifest.
- Every timed case verifies semantics before timing, runs in five isolated Java 26 forks with fixed heap, deterministic inputs, warmup, randomized measured order, adaptive batches, and per-fork medians. Every fork and median-of-fork-medians generated/direct ratio is `<= 1.15x`; failed, interrupted, unsealed, or structurally unproved projections fail the task.
- The implementation and clean documentation-focused contexts are distinct. The documentation pass finalizes Javadoc/package/guide/glossary/planning impact, reuses stable executable evidence unless it changes behavior, and records reasoned no-change conclusions.

## Tests / validation

The clean implementation context runs focused tests during development and exactly one final CPU module suite after executable code stabilizes:

```bash
./gradlew :backends:cpu:test --tests '*VectorScalarPower*' --tests '*PointwiseGeneratedKernelTest' --tests '*FusedGeneratedKernelTest' --tests '*VectorMathTest' --tests '*GeneratedKernelArtifactStoreTest' --tests '*KernelSpecializationTest' --tests '*PartitionPreparerTest' --rerun-tasks
./gradlew :backends:cpu:test
```

For every structurally distinct retained class, record complete inspection rather than excerpts:

```bash
javap -c -p <generated-class-file>
javap -v -p <generated-class-file>
sha256sum <generated-class-file>
```

Run the fixed paired generated/direct harness in five fresh Java 26 processes under the protocol above, then verify its seal and manifest. The clean documentation-focused context runs:

```bash
./gradlew :backends:cpu:javadoc
git diff --check
git status --short
```

Also validate exact changed paths; preservation of the six untracked 0008Q1 paths; local Markdown links, heading anchors, and fences; Javadoc; schema references; task/master-plan/roadmap status and dependency synchronization; Class-File descriptors/member references; external evidence hashes; and performance projection provenance. Repository-wide, architecture, conformance, and integration validation remains CPU 0009/CI scope because this task changes one CPU-private realization and no shared boundary.

## Dependencies

- Complete CPU 0005F, 0005I, 0007A1A, 0008L, and 0008Q; current Java 26 Class-File and Vector API toolchain.
- Independent CPU 0008Q1 review evidence: the focused structural command failed at `CpuScalarImmediateClampMatrixStructuralTest.java:32` because `FIXTURE-POW-FLOAT32-POSITIVE_ONE` contains a Synaptik-owned invocation. Source inspection also found vector scalar-power `RECIPROCAL` routed through `CpuVectorMath`; `IDENTITY` and `SQUARE` are direct but share the same required closure boundary.

## Follow-up tasks

- Resume CPU 0008Q1 only after this task and its separate documentation pass are `Complete`; regenerate its affected hashes/resources in that task rather than here.
- CPU 0009 remains `Blocked` until CPU 0008Q1 and its documentation pass complete.

## Architecture impact

Expected impact: None. This task changes a CPU-private generated realization within existing ownership and lifecycle boundaries. Stop for any architecture, dependency, public API, capability, or route-selection change.

## Implementation prompt

Use this prompt in a separate agentic task/thread with a clean context:

```text
You are the clean implementation agent for Synaptik CPU task 0008Q1A. Work in the existing dirty worktree without committing or pushing. Preserve all unrelated work and do not edit the six untracked CPU 0008Q1 test/resource paths.

Read AGENTS.md, ARCHITECTURE.md, docs/architecture/current-architecture-plan.md, docs/architecture/performance-evidence-and-tuning.md, docs/planning/planning-guide.md, docs/planning/backends/cpu/master-plan.md, docs/planning/backends/cpu/tasks/0008q1a-vector-scalar-power-hot-path-self-containment.md, and its directly referenced predecessor/blocker tasks. Implement exactly that task and stop for architecture or scope conflict.

After executable and performance validation, hand the stable diff, task, affected contracts, exact external evidence root, commands, and results to a distinct clean documentation-focused agent. That pass must follow docs/developer-guide/documentation-rules.md, finalize Javadoc/package/guide/glossary/planning review in the same overall change, and not repeat stable Java or timing runs unless it changes executable behavior or records a concrete risk.

Update this task with decisions, limitations, exact evidence, implementation notes, completion summary, and final status. Do not mark Complete before every gate and the documentation pass succeed.
```

## Local decisions

- Identifier `0008Q1A` is a corrective descendant of the stopped CPU 0008Q1 attempt. It is intentionally inserted immediately before its blocked parent in table order; the master-plan queue remains authoritative when lexical ID order differs.
- The remediation closes all currently generated vector special realizations rather than only the first positive-one fixture. A direct realization that currently happens not to call a helper is still part of semantic, structural, carrier, and performance regression coverage.
- `CpuVectorMath` remains valid for unrelated vector operations. This task removes scalar-power generated dependencies on it without making a broader vector-body self-containment claim.

## Known limitations

`DIRECT` scalar power remains a safe scalar or parallel-scalar fallback. This task proves current FLOAT32/FLOAT64 vector specializations and does not claim arbitrary exponent, carrier, access-regime, byte-for-byte Class-File, or JIT assembly equivalence.

## Validation evidence

The final focused implementation command passed 90 tests with zero failures and two opt-in skips. The final CPU suite ran 812 tests with 26 skips and two failures; both are the protected CPU 0008Q1 fixture/schema expectations that intentionally remain stale until that task regenerates its six owned files, not regressions in this task. The CPU Javadoc command passed with 94 unrelated pre-existing warnings. The implementation review was clean after fixing unsafe fused `LAST_AXIS_BIAS` dense-loop selection, full-output comparison before benchmark timing, and the segment byte-order contract.

Fresh five-fork evidence is sealed at `/private/tmp/synaptik-cpu-0008q1a-review.YQyVfS`. Its 342-entry manifest verifies and its seal is `39dc67f426bb2040dda848effe6527c5d926d8201cab25485a61d715bf6a9cb8`. All 160 fork rows and all 32 aggregate rows are at most `1.15x`; the maximum individual ratio is `1.140150714x` and the maximum aggregate ratio is `1.078991870x`. The structural evidence covers all four realizations, both floating types, four ordered carrier pairs, arbitrary ranges and tails, representative fusion including `LAST_AXIS_BIAS`, and both vector orchestration modes. It proves the generated entries contain no Synaptik member reference and retain the required direct Vector API shape.

Clean documentation context `01a07963-3511-73e1-aa79-9beb7500d1d8` reviewed the affected Javadocs, package summary, CPU guide, glossary, schema wording, tasks, master plan, roadmap, implementation diff, tests, and sealed evidence without rerunning successful Java, CPU, or performance commands. `./gradlew :backends:cpu:javadoc` passed again with the same 94 unrelated pre-existing warnings. Local Markdown links, heading anchors, and fences passed; the final task path inventory contains exactly 25 paths; all six protected CPU 0008Q1 paths remain separate untracked files; status/schema searches are consistent; the 342-entry evidence manifest and seal verify; and `git diff --check` passes.

## Implementation notes

- FLOAT32/FLOAT64 `POSITIVE_ONE`, `IDENTITY`, `SQUARE`, and `RECIPROCAL` now emit direct typed Vector API operations. `DIRECT`, BFLOAT16, ordinary vector reciprocal, and non-vector behavior are unchanged.
- Dense vector bounds are computed once. Loop selection considers only boundaries the body accesses; accessed non-dense fused boundaries retain the safe boundary-aware loop. Segment byte order is prepared only for actual non-scalar vector segment access, and positive-one omits unused base-address work.
- Schema 64 records the generated-byte change while preserving semantic realization and specialization identity fields. Earlier envelopes remain incompatible safe misses.
- No glossary term or identity meaning changed. `CpuVectorMath` remains the owner of unrelated pure vector formulas, so its implementation and Javadoc remain accurate.
- No architecture/ADR/architecture-test, public API, conformance/integration, other-module, dependency, or build-structure update is required. The only build change forwards the two opt-in performance properties.

## Completion summary

Completed the self-contained scalar-power vector bodies, safe accessed-boundary loop selection, exact structural/semantic tests, and sealed five-fork performance proof. Finalized the affected Javadocs, code-generation package summary, CPU guide, and synchronized planning records. The eleven global schema assertion migrations are an explicit necessary scope exception. CPU 0008Q1 is now `Ready` and owns regeneration of its six protected files; CPU 0009 remains `Blocked` on completed CPU 0008Q1 and its documentation pass.

Status: Complete
