/**
 * Supplies the explicitly constructed, fail-closed Metal capability provider.
 *
 * <p>The package currently promises no executable operation, device discovery, Engine
 * composition, or fallback. Native device and storage mechanics remain package-private in the
 * {@code internal} package until a later task delivers a complete prepared execution path.</p>
 */
package io.github.pho001.synaptik.backend.metal;
