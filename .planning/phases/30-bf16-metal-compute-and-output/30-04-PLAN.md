# Plan 30-04: Docs And Regression Closure

**Phase:** 30 BF16 Metal Compute And Output
**Requirements:** METALBF16-01, METALBF16-02, METALBF16-03
**Status:** Planned
**Wave:** 4

## Objective

Close Phase 30 with clear docs, summaries, and verification evidence that BF16 support is real, scoped, and capability-gated.

## Tasks

1. Update `docs/metal-backend.md` with BF16 support scope, capability requirements, native dtype path, and unsupported families.
2. Update `docs/gpu-lowering-coverage.md` with BF16 supported/fallback rows and evidence rules.
3. Update troubleshooting docs for missing BF16 native/runtime capability if needed.
4. Write wave summaries and `30-VERIFICATION.md`.
5. Run source hygiene and profile artifact checks.
6. Keep local calibration/profile artifacts unstaged unless intentionally promoted as canonical fixtures.

## Acceptance Criteria

- Docs do not overclaim universal BF16 Metal support.
- Phase 30 verification maps directly to `METALBF16-01/02/03`.
- Unsupported BF16 paths remain visible and documented.
- Working tree hygiene excludes local benchmark/profile output.

## Verification

```bash
./gradlew test --tests SourceTreeHygieneTest
./gradlew test --tests backend.metal.MetalMpsCapabilitiesTest --tests backend.metal.lowering.MetalRegionLowererTest --tests GpuCoverageSummaryTest
./gradlew metalTest
git diff --check
```

## Must Haves

- METALBF16-01: BF16 support scope is documented and tested.
- METALBF16-02: BF16 parity/materialization evidence is documented.
- METALBF16-03: BF16 coverage and regression evidence is documented.

## Threat Model

- **Threat:** Docs imply all BF16 operations are Metal-supported.
  **Mitigation:** Publish a scoped support table and explicit fallback reasons.
- **Threat:** Local benchmark artifacts are committed accidentally.
  **Mitigation:** Verify `git status --short profiles/platform` before commit.
