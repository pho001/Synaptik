---
phase: 45-metal-output-buffer-write-and-copy-closure
plan: 45-03-copy-strategy-reporting-and-gates
status: planned
requirements:
  - METALCOPY-03
---

# 45-03 Copy Strategy Reporting And Gates

## Objective

Harden trace, benchmark, and coverage reporting so copy strategy, copy timing, output-write proof status, and route context are reviewable.

## Tasks

1. Extend tests around `metalNativeCopyStrategy`, `metalOutputBufferWriteProven`, `nativeDeviceCopyNs`, and route fields.
2. Add or update coverage gates that fail false `TRUE_OUTPUT_BUFFER_WRITE` claims for MPSGraph.
3. Keep custom-kernel direct-write evidence separate from MPSGraph copy evidence.
4. Ensure text and JSON benchmark reports preserve copy strategy summaries.

## Verification

- `./gradlew test --tests BenchmarkSessionTest --tests GpuCoverageSummaryTest --tests GpuCoverageRegressionGateTest`
- `./gradlew classes`

## Completion Criteria

- Reports make MPSGraph copy vs custom direct-write behavior obvious.
- Regression gates fail if route/copy classifications drift silently.
