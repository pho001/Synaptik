---
phase: 14-coverage-gap-triage-and-hot-path-targets
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/main/java/tuning/benchmark/report/GpuHotPathCoverageTarget.java
  - src/main/java/tuning/benchmark/report/GpuHotPathCoverageTargets.java
  - src/test/java/GpuHotPathCoverageTargetsTest.java
  - src/test/java/BenchmarkSuiteSessionTest.java
  - .planning/phases/14-coverage-gap-triage-and-hot-path-targets/14-02-SUMMARY.md
autonomous: true
requirements: [GPUTRIAGE-02, GPUTRIAGE-03]
must_haves:
  truths:
    - "GPUTRIAGE-02: Engineers can run deterministic transformer block, MLP, and conv/normalization-style workloads that expose GPU region length, CPU exits, and unsupported reason codes."
    - "GPUTRIAGE-03: Hot-path targets must map to downstream requirement families before later phases add support."
    - "Phase 14 target workload names must be checked against StandardWorkloads.defaultCatalog()."
---

<objective>
Define the v1.3 hot-path GPU coverage target registry and prove the selected transformer, MLP, conv, and normalization workloads exist in the standard workload catalog.
</objective>

<source_audit>
GOAL: Converts the milestone's workload examples into an executable target contract for later phases.
REQ GPUTRIAGE-02: Covered by deterministic target workload registry and suite request tests.
REQ GPUTRIAGE-03: Covered by mapping each target to downstream requirement families and owner phases.
</source_audit>

<threat_model>
T-14-04: A target list could drift from available workload names and silently skip the desired hot path. Mitigation: tests must require every default target name in `StandardWorkloads.defaultCatalog()`.
T-14-05: Later phases could claim coverage work without knowing which family owns the gap. Mitigation: every target records requirement families and owner phase numbers.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing-first hot-path target tests</name>
  <files>src/test/java/GpuHotPathCoverageTargetsTest.java, src/test/java/BenchmarkSuiteSessionTest.java</files>
  <read_first>
    src/main/java/tuning/workload/StandardWorkloads.java
    src/main/java/tuning/workload/WorkloadCatalog.java
    src/test/java/BenchmarkSuiteSessionTest.java
    .planning/phases/14-coverage-gap-triage-and-hot-path-targets/14-RESEARCH.md
  </read_first>
  <action>
    Create `src/test/java/GpuHotPathCoverageTargetsTest.java` with tests named:
    - `defaultTargetsNameTransformerMlpConvAndNormalizationHotPaths`
    - `defaultTargetsExistInStandardWorkloadCatalog`
    - `benchmarkSuiteCanBeBuiltForDefaultTargets`
    - `targetsMapToDownstreamRequirementFamilies`

    Extend `BenchmarkSuiteSessionTest` with a test named:
    - `phase14CoverageTargetsBuildDeterministicBenchmarkSuite`

    Tests must assert exact target workload names:
    - `transformer_block_hot_path`
    - `mlp_classifier_small`
    - `conv2d_resnet_3x3`
    - `layer_norm_small`

    Tests must assert exact downstream families:
    - `GPUDAG`
    - `GPUSTORAGE`
    - `GPUNORM`
    - `GPUFUSEX`
    - `GPUMULTI`
    - `GPUHARDEN`
  </action>
  <verify>
    <automated>./gradlew test --tests GpuHotPathCoverageTargetsTest --tests BenchmarkSuiteSessionTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "defaultTargetsNameTransformerMlpConvAndNormalizationHotPaths|defaultTargetsExistInStandardWorkloadCatalog|benchmarkSuiteCanBeBuiltForDefaultTargets|targetsMapToDownstreamRequirementFamilies|phase14CoverageTargetsBuildDeterministicBenchmarkSuite|transformer_block_hot_path|mlp_classifier_small|conv2d_resnet_3x3|layer_norm_small|GPUMULTI" src/test/java/GpuHotPathCoverageTargetsTest.java src/test/java/BenchmarkSuiteSessionTest.java` exits 0.
  </acceptance_criteria>
  <done>Tests define the hot-path target contract before implementation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement hot-path target records and registry</name>
  <files>src/main/java/tuning/benchmark/report/GpuHotPathCoverageTarget.java, src/main/java/tuning/benchmark/report/GpuHotPathCoverageTargets.java</files>
  <read_first>
    src/main/java/tuning/workload/StandardWorkloads.java
    src/main/java/tuning/benchmark/report/GpuCoverageGap.java
    src/test/java/GpuHotPathCoverageTargetsTest.java
  </read_first>
  <action>
    Add record `GpuHotPathCoverageTarget` with exact fields:
    - `String workloadName`
    - `String targetKind`
    - `List<String> requirementFamilies`
    - `int ownerPhase`
    - `String rationale`

    Normalize nulls:
    - blank `workloadName` to `"unknown"`
    - blank `targetKind` to `"generic"`
    - null `requirementFamilies` to `List.of()`
    - negative `ownerPhase` to `0`
    - blank `rationale` to `"unspecified"`

    Add final utility class `GpuHotPathCoverageTargets` with exact public static methods:
    - `List<GpuHotPathCoverageTarget> defaults()`
    - `List<String> defaultWorkloadNames()`
    - `BenchmarkSuiteRequest benchmarkSuite(List<BenchmarkEntry> entries)`

    `defaults()` must return exactly four targets in this order:
    1. `transformer_block_hot_path`, targetKind `transformer`, ownerPhase `19`, families `[GPUDAG, GPUNORM, GPUFUSEX, GPUMULTI, GPUHARDEN]`
    2. `mlp_classifier_small`, targetKind `mlp`, ownerPhase `18`, families `[GPUSTORAGE, GPUFUSEX, GPUMULTI, GPUHARDEN]`
    3. `conv2d_resnet_3x3`, targetKind `conv`, ownerPhase `17`, families `[GPUNORM, GPUMULTI, GPUHARDEN]`
    4. `layer_norm_small`, targetKind `normalization`, ownerPhase `17`, families `[GPUSTORAGE, GPUNORM, GPUMULTI, GPUHARDEN]`

    `benchmarkSuite(...)` must call `StandardWorkloads.benchmarkSuite(defaultWorkloadNames(), entries)`.
  </action>
  <verify>
    <automated>./gradlew test --tests GpuHotPathCoverageTargetsTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "record GpuHotPathCoverageTarget|class GpuHotPathCoverageTargets|defaults\\(|defaultWorkloadNames|benchmarkSuite|transformer_block_hot_path|mlp_classifier_small|conv2d_resnet_3x3|layer_norm_small|StandardWorkloads\\.benchmarkSuite" src/main/java/tuning/benchmark/report/GpuHotPathCoverageTarget.java src/main/java/tuning/benchmark/report/GpuHotPathCoverageTargets.java` exits 0.
  </acceptance_criteria>
  <done>Hot-path target registry exists and uses standard workloads.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire benchmark suite target coverage assertions</name>
  <files>src/test/java/BenchmarkSuiteSessionTest.java, src/test/java/GpuHotPathCoverageTargetsTest.java</files>
  <read_first>
    src/test/java/BenchmarkSuiteSessionTest.java
    src/main/java/tuning/benchmark/report/GpuHotPathCoverageTargets.java
    src/main/java/tuning/workload/StandardWorkloads.java
    src/main/java/config/profile/ExecutionProfile.java
  </read_first>
  <action>
    Add or update tests so `BenchmarkSuiteSessionTest` builds a `BenchmarkSuiteRequest` using:
    - `GpuHotPathCoverageTargets.benchmarkSuite(List.of(BenchmarkEntry.candidate("phase14-target-coverage", profile)))`

    The test must assert:
    - `request.workloads().size()` equals `4`
    - workload names contain `transformer_block_hot_path`
    - workload names contain `mlp_classifier_small`
    - workload names contain `conv2d_resnet_3x3`
    - workload names contain `layer_norm_small`
    - the candidate name `phase14-target-coverage` is present

    Do not execute a slow native benchmark in this test; building the deterministic request is sufficient for Phase 14 planning.
  </action>
  <verify>
    <automated>./gradlew test --tests BenchmarkSuiteSessionTest --tests GpuHotPathCoverageTargetsTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "phase14CoverageTargetsBuildDeterministicBenchmarkSuite|GpuHotPathCoverageTargets\\.benchmarkSuite|phase14-target-coverage|request\\.workloads\\(\\)\\.size\\(\\)|layer_norm_small" src/test/java/BenchmarkSuiteSessionTest.java src/test/java/GpuHotPathCoverageTargetsTest.java` exits 0.
  </acceptance_criteria>
  <done>Phase 14 target workloads are executable as a deterministic benchmark suite request.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Record hot-path target summary and hygiene evidence</name>
  <files>.planning/phases/14-coverage-gap-triage-and-hot-path-targets/14-02-SUMMARY.md</files>
  <read_first>
    .planning/phases/14-coverage-gap-triage-and-hot-path-targets/14-02-PLAN.md
    src/test/java/GpuHotPathCoverageTargetsTest.java
    src/test/java/BenchmarkSuiteSessionTest.java
  </read_first>
  <action>
    Create `14-02-SUMMARY.md` with:
    - frontmatter containing `phase: 14-coverage-gap-triage-and-hot-path-targets`, `plan: "02"`, and `status: complete`
    - a section `Hot path target registry`
    - the four workload names `transformer_block_hot_path`, `mlp_classifier_small`, `conv2d_resnet_3x3`, and `layer_norm_small`
    - a verification table listing `./gradlew test --tests GpuHotPathCoverageTargetsTest --tests BenchmarkSuiteSessionTest`
    - a hygiene note containing exact text `profiles/platform/.../tuning/abc/* remained unstaged`
  </action>
  <verify>
    <automated>test -f .planning/phases/14-coverage-gap-triage-and-hot-path-targets/14-02-SUMMARY.md</automated>
    <automated>rg -n "Hot path target registry|transformer_block_hot_path|mlp_classifier_small|conv2d_resnet_3x3|layer_norm_small|profiles/platform/.../tuning/abc/\\* remained unstaged" .planning/phases/14-coverage-gap-triage-and-hot-path-targets/14-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/14-coverage-gap-triage-and-hot-path-targets/14-02-SUMMARY.md` contains all four target workload names and the unstaged profile hygiene note.
  </acceptance_criteria>
  <done>Wave 2 target evidence is recorded for downstream plans.</done>
</task>

</tasks>

<verification>
Run:
- `./gradlew test --tests GpuHotPathCoverageTargetsTest --tests BenchmarkSuiteSessionTest`
- `rg -n "GpuHotPathCoverageTarget|GpuHotPathCoverageTargets|transformer_block_hot_path|mlp_classifier_small|conv2d_resnet_3x3|layer_norm_small" src/main/java/tuning/benchmark/report src/test/java`
</verification>

<success_criteria>
- v1.3 hot-path targets are encoded in a checked Java registry.
- Target workload names are validated against `StandardWorkloads.defaultCatalog()`.
- Transformer, MLP, conv, and normalization flows are represented.
- Every target maps to downstream requirement families and an owner phase.
</success_criteria>
