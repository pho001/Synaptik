---
phase: 18-fused-elementwise-and-epilogue-subregions
plan: "03"
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - src/main/java/graph/optimizer/region/GenericGpuRegionOptimizationPolicy.java
  - src/main/java/graph/optimizer/region/RegionOptimizationUnitSupport.java
  - src/main/java/backend/accelerator/lowering/GpuCompoundPatternDetector.java
  - src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java
  - src/main/java/backend/metal/lowering/MetalPartitionSupport.java
  - src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java
  - src/main/java/backend/metal/lowering/MetalRegionLowerer.java
  - src/main/java/backend/cuda/lowering/CudaRegionLowerer.java
  - src/test/java/graph/optimizer/region/DefaultRegionOptimizerTest.java
  - src/test/java/backend/accelerator/lowering/AcceleratorSubgraphLowererTest.java
  - src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
  - src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
  - src/test/java/PreparedExecutionBuildTest.java
  - .planning/phases/18-fused-elementwise-and-epilogue-subregions/18-03-SUMMARY.md
autonomous: true
requirements: [GPUFUSEX-02, GPUFUSEX-03]
must_haves:
  truths:
    - "GPUFUSEX-02: GPU regions can lower supported linear or matmul epilogues such as bias and activation as region-internal fused subpatterns when backend legality and dtype/layout gates allow it."
    - "GPUFUSEX-03: Epilogue fusion must not reuse CPU fused ASM, CPU vector dispatch, or Operation.OpType.FUSED."
    - "Backend legality, dtype, layout, and capability gates must remain explicit for epilogue fusion."
---

<objective>
Add region-internal matmul/linear epilogue fusion metadata and execution gates for bias plus activation patterns.
</objective>

<source_audit>
REQ GPUFUSEX-02: Covered by explicit matmul/linear + bias + activation epilogue detection, lowering, prepared execution, and CPU parity tests.
REQ GPUFUSEX-03: Covered by CPU fused isolation and backend-neutral accelerator lowering.
RESEARCH: Existing `AcceleratorSubgraphLowerer.tryBuildLegacyMatMulSpec(...)` already recognizes matmul/linear bias and post ops; Phase 18 must expose this as a subpattern with stricter legality evidence.
</source_audit>

<threat_model>
T-18-05: Epilogue fusion could bypass dtype/layout/backend legality and produce incorrect native execution. Mitigation: run backend legality adapters before marking support and assert rejection detail for illegal dtype/layout/capability cases.
T-18-06: Epilogue fusion could duplicate CPU fused semantics or invoke CPU fused internals. Mitigation: use accelerator DAG/post-op metadata only and source hygiene checks in Wave 4.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add epilogue fusion optimizer and lowerer tests</name>
  <files>src/test/java/graph/optimizer/region/DefaultRegionOptimizerTest.java, src/test/java/backend/accelerator/lowering/AcceleratorSubgraphLowererTest.java</files>
  <read_first>
    src/test/java/graph/optimizer/region/DefaultRegionOptimizerTest.java
    src/test/java/backend/accelerator/lowering/AcceleratorSubgraphLowererTest.java
    src/main/java/graph/optimizer/region/GenericGpuRegionOptimizationPolicy.java
    src/main/java/graph/optimizer/region/RegionOptimizationUnitSupport.java
    src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java
    src/main/java/backend/accelerator/lowering/AcceleratorMatMulSpec.java
  </read_first>
  <action>
    Add tests named:
    - `gpuRegionBuildsMatmulBiasActivationEpilogueUnit`
    - `lowererRecordsMatmulBiasActivationEpilogueSubpattern`
    - `epilogueFusionRejectsUnsupportedActivationWithStableReason`

    Build a graph shape compatible with:
    - `MATMUL` or `LINEAR`
    - bias via `ADD` or the existing `LINEAR` bias input
    - activation `RELU`, `SIGMOID`, or `TANH`

    Assert:
    - the selected region ordered node ids include the matmul/linear node and epilogue nodes
    - an execution unit covers the epilogue span without forcing materialization between bias and activation
    - `AcceleratorSubgraphLoweringResult.matMulSpec().biasInputNodeId()` is non-negative
    - `AcceleratorSubgraphLoweringResult.matMulSpec().postOps()` contains activation type `RELU`, `SIGMOID`, or `TANH`
    - the manifest includes a supported `GpuFusionSubpatternSummary` with `GpuCompoundPatternType.LINEAR_BIAS_ACTIVATION`, `loweredPrimitiveCount() >= 2`, and `detail()` containing `epilogue`
    - unsupported activations or illegal shapes produce a rejection reason instead of a supported subpattern
  </action>
  <verify>
    <automated>./gradlew test --tests graph.optimizer.region.DefaultRegionOptimizerTest --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "gpuRegionBuildsMatmulBiasActivationEpilogueUnit|lowererRecordsMatmulBiasActivationEpilogueSubpattern|epilogueFusionRejectsUnsupportedActivationWithStableReason|LINEAR_BIAS_ACTIVATION|epilogue|postOps" src/test/java/graph/optimizer/region/DefaultRegionOptimizerTest.java src/test/java/backend/accelerator/lowering/AcceleratorSubgraphLowererTest.java` exits 0.
  </acceptance_criteria>
  <done>Tests specify epilogue fusion behavior before implementation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add epilogue unit selection and legality-preserving detection</name>
  <files>src/main/java/graph/optimizer/region/GenericGpuRegionOptimizationPolicy.java, src/main/java/graph/optimizer/region/RegionOptimizationUnitSupport.java, src/main/java/backend/accelerator/lowering/GpuCompoundPatternDetector.java, src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java</files>
  <read_first>
    src/main/java/graph/optimizer/region/GenericGpuRegionOptimizationPolicy.java
    src/main/java/graph/optimizer/region/RegionOptimizationUnitSupport.java
    src/main/java/backend/accelerator/lowering/GpuCompoundPatternDetector.java
    src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java
    src/main/java/backend/accelerator/lowering/AcceleratorMatMulSpec.java
    src/main/java/backend/accelerator/dag/AcceleratorPostOpType.java
  </read_first>
  <action>
    Extend GPU region unit selection to recognize a contiguous epilogue span:
    - first node: `Operation.OpType.MATMUL` or `Operation.OpType.LINEAR`
    - optional bias node: `Operation.OpType.ADD` consuming the matmul output, or `LINEAR` with third bias input
    - activation node: `Operation.OpType.RELU`, `Operation.OpType.SIGMOID`, or `Operation.OpType.TANH`

    Build the span as one `ExecutionUnit` using an existing stable kind where possible:
    - prefer `ExecutionUnitKind.SPECIALIZED_PRIMITIVE` for epilogue spans
    - trace must contain exact string `gpu-epilogue-subregion:`
    - `requiredPreparedInputNodeIds` must include only external inputs, not internal epilogue values

    Update `GpuCompoundPatternDetector` and `AcceleratorSubgraphLowerer` so the epilogue span creates a supported subpattern:
    - `GpuCompoundPatternType.LINEAR_BIAS_ACTIVATION`
    - original operation node ids equal to the span
    - lowered primitive ids matching the underlying DAG nodes
    - detail containing `epilogue`

    Preserve existing dtype/layout/backend capability checks by leaving Metal/CUDA legality adapters in control of candidate acceptance before execution.
  </action>
  <verify>
    <automated>./gradlew test --tests graph.optimizer.region.DefaultRegionOptimizerTest --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "gpu-epilogue-subregion|SPECIALIZED_PRIMITIVE|LINEAR_BIAS_ACTIVATION|epilogue" src/main/java/graph/optimizer/region src/main/java/backend/accelerator/lowering` exits 0.
  </acceptance_criteria>
  <done>Epilogue spans can be represented inside selected GPU regions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Metal and CUDA epilogue lowering and rejection tests</name>
  <files>src/test/java/backend/metal/lowering/MetalRegionLowererTest.java, src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java, src/main/java/backend/metal/lowering/MetalPartitionSupport.java, src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java</files>
  <read_first>
    src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
    src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
    src/main/java/backend/metal/lowering/MetalRegionLowerer.java
    src/main/java/backend/cuda/lowering/CudaRegionLowerer.java
    src/main/java/backend/metal/lowering/MetalPartitionSupport.java
    src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java
    src/main/java/backend/accelerator/lowering/GpuFusionSubpatternSummary.java
  </read_first>
  <action>
    Add tests named:
    - `metalLowersMatmulBiasActivationAsEpilogueSubpattern`
    - `cudaLowersMatmulBiasActivationAsEpilogueSubpattern`
    - `metalRejectsIllegalEpilogueLayoutWithStableReason`
    - `cudaRejectsIllegalEpilogueLayoutWithStableReason`

    Supported tests must assert:
    - selected plan or lowered artifact exposes `GpuCompoundPatternType.LINEAR_BIAS_ACTIVATION`
    - `fusedSubpatterns()` has an entry whose `detail()` contains `epilogue`
    - `loweredPrimitiveCount()` is at least `2`
    - original operation span includes matmul/linear, bias/add, and activation nodes

    Rejection tests must assert reason strings contain:
    - `UNSUPPORTED_LAYOUT` for illegal non-dense or storage-offset epilogue values
    - `family=MATMUL_LINEAR` or `LINEAR_BIAS_ACTIVATION`
    - backend name `GPU_METAL` or `GPU_CUDA`
  </action>
  <verify>
    <automated>./gradlew test --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "metalLowersMatmulBiasActivationAsEpilogueSubpattern|cudaLowersMatmulBiasActivationAsEpilogueSubpattern|metalRejectsIllegalEpilogueLayoutWithStableReason|cudaRejectsIllegalEpilogueLayoutWithStableReason|LINEAR_BIAS_ACTIVATION|epilogue" src/test/java/backend/metal/lowering/MetalRegionLowererTest.java src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java` exits 0.
  </acceptance_criteria>
  <done>Metal and CUDA expose supported and rejected epilogue fusion consistently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Prove prepared epilogue parity and visible fallback</name>
  <files>src/test/java/PreparedExecutionBuildTest.java, .planning/phases/18-fused-elementwise-and-epilogue-subregions/18-03-SUMMARY.md</files>
  <read_first>
    src/test/java/PreparedExecutionBuildTest.java
    src/main/java/backend/metal/exec/PreparedMetalExecutable.java
    src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
    src/main/java/graph/execution/PreparedExecution.java
    .planning/phases/18-fused-elementwise-and-epilogue-subregions/18-03-PLAN.md
  </read_first>
  <action>
    Add prepared execution tests named:
    - `metalMatmulBiasActivationEpilogueMatchesCpuAndStaysDeviceOwned`
    - `cudaMatmulBiasActivationEpilogueMatchesCpuAndStaysDeviceOwned`
    - `requiredModeReportsEpilogueFusionFallbackInsteadOfSilentCpuReplay`

    Each parity test must:
    - build a small `FLOAT32` matmul/linear plus bias plus activation graph
    - execute CPU-only reference
    - execute or prepare accelerator-configured graph
    - assert final tensor values match CPU within existing FLOAT32 tolerance
    - assert accelerator metadata includes `LINEAR_BIAS_ACTIVATION`
    - assert internal bias/activation values do not require `CPU_CONSUMER` materialization

    Create `18-03-SUMMARY.md` with section `Matmul and linear epilogue subregions`, requirement notes for `GPUFUSEX-02` and `GPUFUSEX-03`, command evidence, and hygiene note `profiles/platform/.../tuning/abc/* remained unstaged`.
  </action>
  <verify>
    <automated>./gradlew test --tests PreparedExecutionBuildTest</automated>
    <automated>test -f .planning/phases/18-fused-elementwise-and-epilogue-subregions/18-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "metalMatmulBiasActivationEpilogueMatchesCpuAndStaysDeviceOwned|cudaMatmulBiasActivationEpilogueMatchesCpuAndStaysDeviceOwned|requiredModeReportsEpilogueFusionFallbackInsteadOfSilentCpuReplay|LINEAR_BIAS_ACTIVATION|CPU_CONSUMER" src/test/java/PreparedExecutionBuildTest.java` exits 0.
    `.planning/phases/18-fused-elementwise-and-epilogue-subregions/18-03-SUMMARY.md` contains `Matmul and linear epilogue subregions`.
  </acceptance_criteria>
  <done>Epilogue fusion has parity and visible fallback evidence.</done>
</task>

</tasks>

<verification>
Run:

```bash
./gradlew test --tests graph.optimizer.region.DefaultRegionOptimizerTest --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest
git status --short
```
</verification>

<success_criteria>
- Supported matmul/linear epilogues are recorded as `LINEAR_BIAS_ACTIVATION` region-internal subpatterns.
- Dtype/layout/backend legality remains explicit before epilogue support is claimed.
- CPU parity and visible fallback are covered by portable tests.
</success_criteria>

