---
phase: 19-multi-op-gpu-region-execution
plan: "05"
type: execute
wave: 5
depends_on: ["19-01", "19-02", "19-03", "19-04"]
files_modified:
  - docs/gpu-lowering-coverage.md
  - docs/compute-flow.md
  - docs/development.md
  - src/test/java/SourceTreeHygieneTest.java
  - .planning/phases/19-multi-op-gpu-region-execution/19-05-SUMMARY.md
autonomous: true
requirements: [GPUMULTI-01, GPUMULTI-02, GPUMULTI-03]
must_haves:
  truths:
    - "GPUMULTI-01/02/03: Phase closure documents multi-op GPU region execution, no hidden internal materialization, and backend-neutral planning with backend-specific Metal/CUDA execution."
    - "D-09/D-11/D-12: Vendor library routing and default native ABI widening are deferred; GPU fusion remains region-internal and does not reuse CPU fused ASM."
    - "D-14/D-15: Transformer/MLP are prioritized and evidence includes fallback/materialization/runtime path metrics; conv/norm blockers remain visible if unsupported."
---

<objective>
Close Phase 19 with documentation, source hygiene, final validation evidence, and a summary proving the multi-op GPU region contract is reviewable.
</objective>

<source_audit>
REQ GPUMULTI-01: Covered by docs and summary describing multi-op lowered region execution.
REQ GPUMULTI-02: Covered by docs and validation describing no hidden CPU materialization before true boundaries.
REQ GPUMULTI-03: Covered by docs and hygiene tests preserving backend-neutral planning and backend-specific execution.
CONTEXT D-09, D-11, D-12, D-14, and D-15 are translated into closure documentation and source hygiene.
</source_audit>

<threat_model>
T-19-08: Docs could imply universal GPU op support or vendor-library routing. Mitigation: explicitly document stable rejection/fallback and `GPULIB-*` deferral.
T-19-09: Future edits could import CPU fused internals into accelerator packages. Mitigation: add source hygiene checks that reject CPU fused imports in accelerator, Metal, and CUDA production packages.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source hygiene checks for Phase 19 boundaries</name>
  <files>src/test/java/SourceTreeHygieneTest.java</files>
  <read_first>
    src/test/java/SourceTreeHygieneTest.java
    src/main/java/backend/accelerator/lowering/GpuFusionSubpatternSummary.java
    src/main/java/backend/metal/lowering/MetalRegionLowerer.java
    src/main/java/backend/cuda/lowering/CudaRegionLowerer.java
  </read_first>
  <action>
    Add or extend tests named:
    - `phaseNineteenAcceleratorPackagesDoNotImportCpuFusedInternals`
    - `phaseNineteenPublicTensorApiDoesNotExposeDeviceResidency`
    - `phaseNineteenTensorArrayBridgeIsNotMarkedNativeBufferCoverage`

    The CPU fused import check must scan production files under:
    - `src/main/java/backend/accelerator`
    - `src/main/java/backend/metal`
    - `src/main/java/backend/cuda`

    and fail if a file contains exact string:
    - `import backend.cpu.fused`

    The public Tensor check must scan `src/main/java/tensor/Tensor.java` and fail if Phase 19 added public method names containing `DeviceHandle`, `GpuHandle`, or `residentOnDevice`.
  </action>
  <verify>
    <automated>./gradlew test --tests SourceTreeHygieneTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "phaseNineteenAcceleratorPackagesDoNotImportCpuFusedInternals|phaseNineteenPublicTensorApiDoesNotExposeDeviceResidency|phaseNineteenTensorArrayBridgeIsNotMarkedNativeBufferCoverage|import backend.cpu.fused|DeviceHandle|GpuHandle|residentOnDevice" src/test/java/SourceTreeHygieneTest.java` exits 0.
  </acceptance_criteria>
  <done>Source hygiene protects Phase 19 architectural boundaries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document multi-op GPU region execution</name>
  <files>docs/gpu-lowering-coverage.md, docs/compute-flow.md, docs/development.md</files>
  <read_first>
    docs/gpu-lowering-coverage.md
    docs/compute-flow.md
    docs/development.md
    .planning/phases/19-multi-op-gpu-region-execution/19-CONTEXT.md
    .planning/phases/19-multi-op-gpu-region-execution/19-RESEARCH.md
  </read_first>
  <action>
    Add or update documentation with exact strings:
    - `Phase 19 multi-op GPU region execution`
    - `selected GPU partition can execute as one backend-owned lowered region`
    - `ExecutionState and device buffer bindings carry supported internal values`
    - `tensor-array bridge execution is not native buffer GPU coverage`
    - `GPU fusion remains region-internal lowering/fusion, not CPU fused ASM reuse`
    - `vendor library routing is deferred to GPULIB-*`
    - `profiles/platform/.../tuning/abc/* remained unstaged`

    Do not document universal Metal/CUDA operation support. Unsupported normalization, reduction, conv, and loss-adjacent blockers must remain described as visible support/rejection outcomes.
  </action>
  <verify>
    <automated>rg -n "Phase 19 multi-op GPU region execution|selected GPU partition can execute as one backend-owned lowered region|ExecutionState and device buffer bindings carry supported internal values|tensor-array bridge execution is not native buffer GPU coverage|GPU fusion remains region-internal lowering/fusion, not CPU fused ASM reuse|vendor library routing is deferred to GPULIB-\\*|profiles/platform/.../tuning/abc/\\* remained unstaged" docs/gpu-lowering-coverage.md docs/compute-flow.md docs/development.md</automated>
  </verify>
  <acceptance_criteria>
    `docs/gpu-lowering-coverage.md`, `docs/compute-flow.md`, and `docs/development.md` contain the Phase 19 boundary and evidence strings above.
  </acceptance_criteria>
  <done>Docs explain Phase 19 behavior and boundaries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run final Phase 19 focused verification</name>
  <files>.planning/phases/19-multi-op-gpu-region-execution/19-05-SUMMARY.md</files>
  <read_first>
    .planning/phases/19-multi-op-gpu-region-execution/19-01-SUMMARY.md
    .planning/phases/19-multi-op-gpu-region-execution/19-02-SUMMARY.md
    .planning/phases/19-multi-op-gpu-region-execution/19-03-SUMMARY.md
    .planning/phases/19-multi-op-gpu-region-execution/19-04-SUMMARY.md
    .planning/phases/19-multi-op-gpu-region-execution/19-VALIDATION.md
  </read_first>
  <action>
    Run final verification:
    - `./gradlew classes`
    - `./gradlew test --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests BenchmarkSuiteSessionTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests graph.execution.DeviceLayoutViewPropagationTest --tests SourceTreeHygieneTest`
    - `git status --short`

    Create `19-05-SUMMARY.md` with:
    - frontmatter containing `phase: 19-multi-op-gpu-region-execution`, `plan: "05"`, and `status: complete`
    - section `Phase 19 closure`
    - requirement coverage notes for `GPUMULTI-01`, `GPUMULTI-02`, and `GPUMULTI-03`
    - exact text `hot path stayed on GPU evidence is trace/report based, not timing-only`
    - exact text `tensor-array bridge execution is not native buffer GPU coverage`
    - final verification command table
    - hygiene note `profiles/platform/.../tuning/abc/* remained unstaged`
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests BenchmarkSuiteSessionTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests graph.execution.DeviceLayoutViewPropagationTest --tests SourceTreeHygieneTest</automated>
    <automated>git status --short</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/19-multi-op-gpu-region-execution/19-05-SUMMARY.md` contains `Phase 19 closure`, all three `GPUMULTI` ids, `hot path stayed on GPU evidence is trace/report based, not timing-only`, and `tensor-array bridge execution is not native buffer GPU coverage`.
  </acceptance_criteria>
  <done>Phase 19 closure evidence is recorded.</done>
</task>

</tasks>

<verification>
Run:

```bash
./gradlew classes
./gradlew test --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests BenchmarkSuiteSessionTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests graph.execution.DeviceLayoutViewPropagationTest --tests SourceTreeHygieneTest
git status --short
```
</verification>

<success_criteria>
- Docs state the Phase 19 multi-op GPU region contract and explicit exclusions.
- Source hygiene prevents CPU fused internals and public GPU tensor API drift.
- Final verification proves planning, lowering, execution, trace, report, and docs closure.
</success_criteria>
