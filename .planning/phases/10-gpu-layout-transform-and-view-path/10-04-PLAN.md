---
phase: 10-gpu-layout-transform-and-view-path
plan: "04"
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java
  - src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java
  - src/test/java/CompiledGraphTraceTest.java
  - docs/metal-backend.md
  - docs/native-bridges-and-blas.md
  - docs/development.md
  - .planning/phases/10-gpu-layout-transform-and-view-path/10-04-SUMMARY.md
autonomous: true
requirements: [GPUVIEW-01, GPUVIEW-02, GPUVIEW-03]
---

<objective>
Close Phase 10 with layout-heavy forward and forward/backward coverage, trace/documentation evidence, and regression gates proving supported view/layout flows stay device-owned until true CPU boundaries.
</objective>

<source_audit>
GOAL: Covers Phase 10 end-to-end evidence and documentation closure.
REQ GPUVIEW-01: Tests cover reshape, permute, expand, contiguous, and alias/view-like values.
REQ GPUVIEW-02: Tests prove CPU parity and no intermediate CPU materialization before graph output or gradient publication boundaries.
REQ GPUVIEW-03: Docs explain shared request/decision records and backend-owned implementation details.
</source_audit>

<threat_model>
T-10-07: Trace evidence could claim GPU residency while CPU materialization already occurred. Mitigation: E2E tests assert `cpuMaterializations()` contains no intermediate CPU consumer materialization for supported flows.
T-10-08: Documentation could overclaim universal non-contiguous CUDA support. Mitigation: docs must explicitly state direct non-dense CUDA compute remains conservative unless the path is metadata-only view propagation or dense GPU materialization.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add layout-heavy Metal and CUDA forward-flow tests</name>
  <files>src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java, src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java</files>
  <read_first>
    src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java
    src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
    src/main/java/graph/execution/PreparedExecution.java
    src/main/java/graph/execution/DeviceLayoutViewPropagator.java
    src/main/java/backend/metal/exec/PreparedMetalExecutable.java
    src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
  </read_first>
  <action>
    Extend `MetalLayoutAwareDeviceFlowTest` with:
    - `reshapePermuteContiguousFlowAvoidsIntermediateCpuMaterialization`
    - `expandContiguousFlowAvoidsIntermediateCpuMaterialization`

    Create `CudaLayoutTransformDeviceFlowTest` with:
    - `reshapePermuteContiguousFlowAvoidsIntermediateCpuMaterialization`
    - `unsupportedDirectNonDenseCudaConsumerFallsBackVisibly`

    Each supported-flow test must:
    - build a `FLOAT32` graph with `linear` or `relu` followed by `reshape`, `permute` or `expand`, and `contiguous`
    - mark accelerator backend on the compute/layout nodes using `TensorInternalAccess.setBackend(...)`
    - execute through `CompiledGraph.compile(...).prepare(...).executeTraced(ExecutionMode.FORWARD)`
    - compare graph output with a CPU execution result using `assertArrayEquals`
    - assert no `CpuMaterializationTrace` has reason `CPU_CONSUMER` before graph output
    - assert at least one trace attribute contains `GPU_LAYOUT_VIEW_BINDING_AVAILABLE` or `GPU_LAYOUT_DENSE_MATERIALIZATION_AVAILABLE`

    The unsupported CUDA test must assert a trace reason code `NATIVE_LAYOUT_ABI_UNAVAILABLE`, `NATIVE_LAYOUT_METADATA_UNSUPPORTED`, or `GPU_LAYOUT_TRANSFORM_UNSUPPORTED` and must not leave stale residency state.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.metal.MetalLayoutAwareDeviceFlowTest --tests backend.cuda.exec.CudaLayoutTransformDeviceFlowTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "reshapePermuteContiguousFlowAvoidsIntermediateCpuMaterialization|expandContiguousFlowAvoidsIntermediateCpuMaterialization|unsupportedDirectNonDenseCudaConsumerFallsBackVisibly|GPU_LAYOUT_DENSE_MATERIALIZATION_AVAILABLE" src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java` exits 0.
  </acceptance_criteria>
  <done>Layout-heavy forward flows have portable or capability-gated E2E evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add forward/backward or gradient-publication boundary coverage</name>
  <files>src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java, src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java</files>
  <read_first>
    src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java
    src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java
    src/main/java/graph/execution/PreparedExecution.java
    src/main/java/backend/memory/CpuMaterializationReason.java
  </read_first>
  <action>
    Add or extend tests to cover gradient publication:
    - Metal method: `layoutTransformForwardBackwardPublishesGradientsWithCpuParity`
    - CUDA method: `layoutTransformForwardBackwardPublishesGradientsWithCpuParity`

    Test graph:
    - `input.linear(weight).reshape(...).permute(...).contiguous().sum()`
    - `input` and `weight` require gradients
    - accelerator backend assigned to supported forward compute/layout nodes

    Assertions:
    - CPU and accelerator runs produce matching `input.getGradient().toDoubleArrayCopy()`
    - CPU and accelerator runs produce matching `weight.getGradient().toDoubleArrayCopy()`
    - trace contains `CpuMaterializationReason.GRADIENT_PUBLICATION` or a visible accelerator fallback reason
    - no hidden stale CPU publication occurs
  </action>
  <verify>
    <automated>./gradlew test --tests backend.metal.MetalLayoutAwareDeviceFlowTest --tests backend.cuda.exec.CudaLayoutTransformDeviceFlowTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "layoutTransformForwardBackwardPublishesGradientsWithCpuParity|GRADIENT_PUBLICATION|assertArrayEquals\\(expected.*Gradient" src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java` exits 0.
  </acceptance_criteria>
  <done>Gradient-publication boundary has CPU parity and visible materialization evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document trace fields, supported scope, and verification commands</name>
  <files>docs/metal-backend.md, docs/native-bridges-and-blas.md, docs/development.md, src/test/java/CompiledGraphTraceTest.java</files>
  <read_first>
    docs/metal-backend.md
    docs/native-bridges-and-blas.md
    docs/development.md
    src/test/java/CompiledGraphTraceTest.java
    src/main/java/graph/execution/PreparedExecution.java
  </read_first>
  <action>
    Update docs with a section or subsection containing the exact phrase `GPU layout transform and view path`.

    Documentation must state:
    - metadata-only views can preserve device bindings without Java array materialization
    - dense GPU materialization covers `contiguous()` and non-contiguous-source `reshape` when backend capability exists
    - direct non-dense CUDA compute remains conservative until Phase 11 lowering coverage
    - graph output, CPU consumer, and gradient publication are the valid CPU materialization boundaries
    - recommended verification commands:
      - `./gradlew classes`
      - `./gradlew test --tests backend.accelerator.buffer.AcceleratorLayoutTransformPlannerTest --tests graph.execution.DeviceLayoutViewPropagationTest`
      - `./gradlew test --tests backend.metal.MetalLayoutAwareDeviceFlowTest --tests backend.cuda.exec.CudaLayoutTransformDeviceFlowTest`
      - `./gradlew metalTest`
      - `./gradlew buildCudaGraphShim cudaTest`

    Extend `CompiledGraphTraceTest` or the closest trace test to assert trace attributes include:
    - `acceleratorBufferReasonCode`
    - `storageResidency`
    - one of `GPU_LAYOUT_VIEW_BINDING_AVAILABLE`, `GPU_LAYOUT_DENSE_MATERIALIZATION_AVAILABLE`, or a documented fallback reason for unsupported local capability
  </action>
  <verify>
    <automated>./gradlew test --tests CompiledGraphTraceTest</automated>
    <automated>rg -n "GPU layout transform and view path|metadata-only views|dense GPU materialization|direct non-dense CUDA compute remains conservative|buildCudaGraphShim cudaTest" docs src/test/java/CompiledGraphTraceTest.java</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "GPU layout transform and view path|metadata-only views|dense GPU materialization|direct non-dense CUDA compute remains conservative|GPU_LAYOUT_VIEW_BINDING_AVAILABLE|GPU_LAYOUT_DENSE_MATERIALIZATION_AVAILABLE" docs src/test/java/CompiledGraphTraceTest.java` exits 0.
  </acceptance_criteria>
  <done>Phase 10 behavior is documented and trace-visible.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run final focused Phase 10 verification gate</name>
  <files>.planning/phases/10-gpu-layout-transform-and-view-path/10-04-SUMMARY.md</files>
  <read_first>
    .planning/phases/10-gpu-layout-transform-and-view-path/10-VALIDATION.md
    .planning/phases/10-gpu-layout-transform-and-view-path/10-01-PLAN.md
    .planning/phases/10-gpu-layout-transform-and-view-path/10-02-PLAN.md
    .planning/phases/10-gpu-layout-transform-and-view-path/10-03-PLAN.md
    docs/development.md
  </read_first>
  <action>
    Run and record these commands in the summary:
    - `./gradlew classes`
    - `./gradlew test --tests backend.accelerator.buffer.AcceleratorLayoutTransformPlannerTest --tests graph.execution.DeviceLayoutViewPropagationTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests backend.metal.MetalLayoutAwareDeviceFlowTest --tests backend.cuda.exec.CudaLayoutTransformDeviceFlowTest --tests CompiledGraphTraceTest`
    - `./gradlew metalTest`
    - `./gradlew buildCudaGraphShim cudaTest`

    The summary must include:
    - whether native Metal/CUDA checks passed or were capability-skipped
    - whether local profile tuning files under `profiles/platform/.../tuning/abc/*` remained unstaged
    - the accepted unsupported CUDA scope, if any
  </action>
  <verify>
    <automated>test -f .planning/phases/10-gpu-layout-transform-and-view-path/10-04-SUMMARY.md</automated>
    <automated>rg -n "classes|AcceleratorLayoutTransformPlannerTest|DeviceLayoutViewPropagationTest|metalTest|buildCudaGraphShim cudaTest|profiles/platform" .planning/phases/10-gpu-layout-transform-and-view-path/10-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/10-gpu-layout-transform-and-view-path/10-04-SUMMARY.md` contains `Phase 10 final verification`, `metalTest`, `buildCudaGraphShim cudaTest`, and `profiles/platform`.
  </acceptance_criteria>
  <done>Phase 10 has final verification evidence ready for `$gsd-validate-phase 10` and milestone audit.</done>
</task>

</tasks>

<verification>
Run:
- `./gradlew classes`
- `./gradlew test --tests backend.accelerator.buffer.AcceleratorLayoutTransformPlannerTest --tests graph.execution.DeviceLayoutViewPropagationTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests backend.metal.MetalLayoutAwareDeviceFlowTest --tests backend.cuda.exec.CudaLayoutTransformDeviceFlowTest --tests CompiledGraphTraceTest`
- Optional native: `./gradlew metalTest`
- Optional native: `./gradlew buildCudaGraphShim cudaTest`
</verification>

<success_criteria>
- At least one layout-heavy forward flow avoids intermediate CPU materialization.
- A forward/backward or gradient-publication boundary has CPU parity.
- Unsupported layout paths remain visibly rejected.
- Docs describe exact supported scope and verification commands.
- Local benchmark/profile artifacts are not committed.
</success_criteria>
