---
phase: 003-materialization-aware-region-planning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModel.java
  - src/main/java/graph/optimizer/partition/ScoredCandidatePartitionPlanner.java
  - src/main/java/graph/execution/trace/PartitionDecisionTrace.java
  - src/test/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModelTest.java
  - src/test/java/PreparedExecutionBuildTest.java
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-03: scored accelerator planning prefers longer legal device-owned regions on equal or ambiguous scores when they reduce CPU materialization boundaries."
    - "D-04/D-05/D-06/D-13: Phase 3 cost summaries use static byte/work/layout/boundary/fallback/dispatch fields and named presets only, with no profile-derived speed evidence."
    - "D-08/D-09: compile partition traces include selected candidate score summaries and a bounded list of top rejected finalists."
  artifacts:
    - path: "src/main/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModel.java"
      provides: "static materialization-aware score summaries and reason-coded score decisions"
      contains: "MaterializationCostSummary"
    - path: "src/main/java/graph/optimizer/partition/ScoredCandidatePartitionPlanner.java"
      provides: "candidate finalist tracking and materialization-aware scoring"
      contains: "topRejectedFinalists"
    - path: "src/main/java/graph/execution/trace/PartitionDecisionTrace.java"
      provides: "compile-time cost summary and finalist trace fields"
      contains: "finalists"
    - path: "src/test/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModelTest.java"
      provides: "unit coverage for static cost scoring"
      contains: "dispatchOverheadPenalizesTinyAcceleratorIslands"
  key_links:
    - from: "src/main/java/graph/optimizer/partition/ScoredCandidatePartitionPlanner.java"
      to: "src/main/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModel.java"
      via: "score summary construction"
      pattern: "MaterializationCostSummary"
    - from: "src/main/java/graph/execution/trace/PartitionDecisionTrace.java"
      to: "src/test/java/PreparedExecutionBuildTest.java"
      via: "compile trace assertions"
      pattern: "topRejectedFinalists"
---

<objective>
Add the static materialization-aware score vocabulary and compile-time planner finalist tracing.

Output: `AcceleratorPartitionScoreModel` returns explainable score summaries; `ScoredCandidatePartitionPlanner` records selected and top rejected finalists; compile traces expose enough fields to explain why longer accelerator regions won or costly regions were rejected.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/003-materialization-aware-region-planning/003-CONTEXT.md
@.planning/phases/003-materialization-aware-region-planning/003-RESEARCH.md
@.planning/phases/003-materialization-aware-region-planning/003-VALIDATION.md
@.planning/phases/003-materialization-aware-region-planning/003-PATTERNS.md
@.planning/phases/001-accelerator-buffer-layout-abi/001-VERIFICATION.md
@.planning/phases/002-metal-layout-aware-device-flow/002-VERIFICATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@AGENTS.md
</context>

<source_audit>
GOAL: Covered by adding score summaries that reward long profitable device-owned regions and explain costly splits.
REQ PLAN-01: Covered by static cost fields for materialization, upload/download, tensor-array fallback, layout fallback, dispatch overhead, and estimated compute work.
REQ PLAN-02: Covered by planner tie-break and avoided-intermediate/boundary scoring.
REQ PLAN-03: Covered by rejected finalist reason summaries and negative/low scores for costly candidates.
CONTEXT D-01 through D-10 and D-13: Covered in `must_haves.truths` and tasks.
AGENTS: Keeps public `Tensor` API logical and stores residency/cost decisions in compile-time runtime state.
</source_audit>

<threat_model>
T-3-01: Invalid cost inputs could produce NaN/negative overflow and select unsafe accelerator paths. Mitigation: clamp byte/work/count inputs in score records and reject non-finite scores.
T-3-02: Trace finalist lists could grow without bound and make compile/benchmark output unusable. Mitigation: cap top rejected finalists to exactly 3 by default.
T-3-03: Hidden profile-derived constants could make planning non-reproducible. Mitigation: Phase 3 score construction must not read profile/calibration files and must name the static preset in traces.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add static materialization cost summary records and scoring tests</name>
  <files>src/main/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModel.java, src/test/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModelTest.java</files>
  <read_first>
    .planning/phases/003-materialization-aware-region-planning/003-CONTEXT.md
    .planning/phases/003-materialization-aware-region-planning/003-RESEARCH.md
    src/main/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModel.java
    src/main/java/config/optimizer/MetalTransferModel.java
    src/main/java/config/optimizer/PartitionConfig.java
    src/test/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModelTest.java
  </read_first>
  <action>
    Extend `AcceleratorPartitionScoreModel` with backend-neutral static cost records:
    - `public record MaterializationSignals(int boundaryCount, long uploadBytes, long downloadBytes, long tensorArrayFallbackBytes, long layoutFallbackBytes, long avoidedIntermediateBytes, String fallbackMode, String layoutClass)`
    - `public record StaticCostPreset(String name, double boundaryPenalty, double uploadBytePenalty, double downloadBytePenalty, double tensorArrayFallbackBytePenalty, double layoutFallbackBytePenalty, double avoidedIntermediateByteCredit, double dispatchOverhead, double computeWorkCredit)`
    - `public record MaterializationCostSummary(String preset, int boundaryCount, long estimatedTransferBytes, long estimatedComputeWork, long avoidedIntermediateBytes, double dispatchCost, double finalScore, String reasonCode, String fallbackMode, String layoutClass)`

    Add `StaticCostPreset.conservative()`, `StaticCostPreset.measured()`, and `StaticCostPreset.aggressive()` factory methods. These names must map to internal constants only. Do not read execution profiles, calibration artifacts, or benchmark output.

    Add `scoreMaterializationAware(CandidateMetrics metrics, long estimatedWork, MaterializationSignals signals, PlannerPolicy plannerPolicy, StaticCostPreset preset)` that returns `MaterializationCostSummary`.

    Required reason codes:
    - `accepted-static-profitable`
    - `rejected-non-positive-work`
    - `rejected-non-finite-score`
    - `rejected-materialization-cost`

    Update existing `acceptedScore(...)` transfer-aware methods to delegate to the new summary where possible while keeping current public methods available for existing callers.

    Add tests with exact method names:
    - `dispatchOverheadPenalizesTinyAcceleratorIslands`
    - `boundaryAndTransferBytesReduceStaticScore`
    - `avoidedIntermediateBytesCanMakeLongerRegionWin`
    - `layoutAndTensorArrayFallbackBytesCanRejectCandidate`
    - `staticPresetsDoNotRequireProfileDerivedCosts`
  </action>
  <verify>
    <automated>./gradlew test --tests graph.optimizer.partition.cost.AcceleratorPartitionScoreModelTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "record MaterializationSignals|record StaticCostPreset|record MaterializationCostSummary|scoreMaterializationAware" src/main/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModel.java` finds all new APIs. `rg -n "accepted-static-profitable|rejected-non-positive-work|rejected-non-finite-score|rejected-materialization-cost" src/main/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModel.java` finds stable reason codes. `rg -n "dispatchOverheadPenalizesTinyAcceleratorIslands|boundaryAndTransferBytesReduceStaticScore|avoidedIntermediateBytesCanMakeLongerRegionWin|layoutAndTensorArrayFallbackBytesCanRejectCandidate|staticPresetsDoNotRequireProfileDerivedCosts" src/test/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModelTest.java` finds the required tests. Targeted Gradle test passes.
  </acceptance_criteria>
  <done>Static materialization-aware cost summaries exist, are named-preset based, and have unit coverage for PLAN-01 cost dimensions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry cost summaries and top rejected finalists in partition traces</name>
  <files>src/main/java/graph/execution/trace/PartitionDecisionTrace.java, src/main/java/graph/optimizer/partition/ScoredCandidatePartitionPlanner.java, src/test/java/PreparedExecutionBuildTest.java</files>
  <read_first>
    src/main/java/graph/execution/trace/PartitionDecisionTrace.java
    src/main/java/graph/execution/trace/PartitionCompileTrace.java
    src/main/java/graph/optimizer/partition/ScoredCandidatePartitionPlanner.java
    src/main/java/graph/optimizer/partition/PartitionPlanningRequest.java
    src/main/java/graph/optimizer/partition/PartitionCandidate.java
    src/main/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModel.java
    src/test/java/PreparedExecutionBuildTest.java
  </read_first>
  <action>
    Add a compact finalist record to `PartitionDecisionTrace`:
    - `public record CandidateCostTrace(List<Integer> nodeIds, String reason, double finalScore, int boundaryCount, long estimatedTransferBytes, long estimatedComputeWork, String preset)`

    Extend `PartitionDecisionTrace` with:
    - `AcceleratorPartitionScoreModel.MaterializationCostSummary costSummary`
    - `List<CandidateCostTrace> finalists`

    Constructor normalization:
    - `costSummary` may be `null` for non-scored/legacy planner paths.
    - `finalists` must become `List.of()` when null.
    - keep at most 3 finalist entries.

    Update `ScoredCandidatePartitionPlanner`:
    - compute `MaterializationSignals` for each lowered candidate using boundary count, input bytes, output bytes, avoided intermediate bytes, and fallback/layout fields available from the candidate/plan.
    - use `StaticCostPreset` derived from `request.metalTransferModel()` for Metal; use `StaticCostPreset.conservative()` for CUDA/non-Metal accelerator planning.
    - keep accepted tie-break behavior deterministic: higher final score, then higher work, then larger node count, then lower anchor id.
    - track exactly the top 3 rejected or non-winning lowered finalists by final score with reason `not-selected-lower-score`, `rejected-materialization-cost`, or `lowerer-rejected`.
    - include selected `costSummary` and `finalists` in accepted and rejected `PartitionDecisionTrace` objects.

    Add a `PreparedExecutionBuildTest` assertion that compiling an offloadable CPU `matmul -> relu` graph with `OffloadConfig.acceleratorScored()` or equivalent scored policy yields at least one partition decision where `costSummary()` is non-null, `costSummary().preset()` is non-blank, and `finalists().size() <= 3`.
  </action>
  <verify>
    <automated>./gradlew test --tests PreparedExecutionBuildTest --tests graph.optimizer.partition.cost.AcceleratorPartitionScoreModelTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "record CandidateCostTrace|MaterializationCostSummary|finalists\\(\\)" src/main/java/graph/execution/trace/PartitionDecisionTrace.java` finds trace fields. `rg -n "topRejectedFinalists|not-selected-lower-score|rejected-materialization-cost" src/main/java/graph/optimizer/partition/ScoredCandidatePartitionPlanner.java` finds finalist tracking. `rg -n "costSummary\\(\\).*preset|finalists\\(\\)\\.size\\(\\) <= 3|acceleratorScored" src/test/java/PreparedExecutionBuildTest.java` finds compile trace assertions. Targeted Gradle command passes.
  </acceptance_criteria>
  <done>Compile-time scored planning emits bounded, explainable cost/finalist traces while preserving deterministic accelerator tie-breaks.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew test --tests graph.optimizer.partition.cost.AcceleratorPartitionScoreModelTest --tests PreparedExecutionBuildTest`
</verification>
