---
phase: 003-materialization-aware-region-planning
plan: "02"
type: execute
wave: 2
depends_on:
  - "003-01"
files_modified:
  - src/main/java/backend/accelerator/select/AcceleratorPlanCostModel.java
  - src/main/java/backend/select/DefaultBackendSelectionPolicy.java
  - src/main/java/graph/execution/trace/BackendSelectionDecisionTrace.java
  - src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java
  - src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java
  - src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java
  - src/test/java/PreparedExecutionBuildTest.java
  - src/test/java/BenchmarkSessionTest.java
autonomous: true
requirements:
  - PLAN-01
  - PLAN-03
user_setup: []
must_haves:
  truths:
    - "D-08/D-10: backend selection and benchmark reports surface summary static cost fields without expanding runtime step traces."
    - "D-09: selected backend candidate and top rejected finalists are visible with score summaries and rejection reasons."
    - "D-13: prepare-time selection ignores profile-derived speed evidence in Phase 3."
  artifacts:
    - path: "src/main/java/backend/accelerator/select/AcceleratorPlanCostModel.java"
      provides: "prepare-time static accelerator cost gate"
      contains: "MaterializationCostSummary"
    - path: "src/main/java/graph/execution/trace/BackendSelectionDecisionTrace.java"
      provides: "backend selection score and finalist diagnostics"
      contains: "finalists"
    - path: "src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java"
      provides: "human-readable planner/backend selection cost report"
      contains: "backendSelectionCost"
    - path: "src/test/java/BenchmarkSessionTest.java"
      provides: "text/json report assertions for cost summaries"
      contains: "renderersExposeBackendSelectionCostDiagnostics"
  key_links:
    - from: "src/main/java/backend/select/DefaultBackendSelectionPolicy.java"
      to: "src/main/java/backend/accelerator/select/AcceleratorPlanCostModel.java"
      via: "cost decision and reason code"
      pattern: "AcceleratorPlanCostModel.decide"
    - from: "src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java"
      to: "src/test/java/BenchmarkSessionTest.java"
      via: "structured cost summary assertions"
      pattern: "\"backendSelectionCost\""
---

<objective>
Apply Phase 3 static cost decisions at prepare time and make the selected/rejected decision visible in benchmark reports.

Output: backend selection traces explain final score, boundary count, estimated transfers, estimated compute, selected preset, selected candidate, and top rejected finalists; benchmark text/json reports summarize these fields compactly.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/003-materialization-aware-region-planning/003-CONTEXT.md
@.planning/phases/003-materialization-aware-region-planning/003-RESEARCH.md
@.planning/phases/003-materialization-aware-region-planning/003-VALIDATION.md
@.planning/phases/003-materialization-aware-region-planning/003-PATTERNS.md
@.planning/phases/003-materialization-aware-region-planning/003-01-PLAN.md
@AGENTS.md
</context>

<source_audit>
GOAL: Covered by preparing backend decisions with materialization-aware cost traces.
REQ PLAN-01: Covered by prepare-time static cost summary fields.
REQ PLAN-03: Covered by cost rejection before execution and stable trace/report reasons.
CONTEXT D-08, D-09, D-10, D-13: Covered in `must_haves.truths` and renderer tests.
AGENTS: Fallback stays visible in traces and benchmark reports; public `Tensor` API remains logical.
</source_audit>

<threat_model>
T-3-03: Prepare-time selection could silently select costly accelerator plans when compile-time scoring is missing. Mitigation: `AcceleratorPlanCostModel.Decision` includes a summary and rejects non-profitable static scores.
T-3-04: Report output could leak too much candidate detail or become unstable. Mitigation: render selected candidate plus top 3 rejected finalists only, sorted deterministically.
T-3-07: Runtime trace bloat could slow hot execution paths. Mitigation: add fields to compile/prepare/report surfaces only; do not add per-step planner cost metadata.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend backend selection cost decisions and traces</name>
  <files>src/main/java/backend/accelerator/select/AcceleratorPlanCostModel.java, src/main/java/backend/select/DefaultBackendSelectionPolicy.java, src/main/java/graph/execution/trace/BackendSelectionDecisionTrace.java, src/test/java/PreparedExecutionBuildTest.java</files>
  <read_first>
    src/main/java/backend/accelerator/select/AcceleratorPlanCostModel.java
    src/main/java/backend/select/DefaultBackendSelectionPolicy.java
    src/main/java/graph/execution/trace/BackendSelectionDecisionTrace.java
    src/main/java/graph/execution/trace/BackendSelectionTrace.java
    src/main/java/graph/optimizer/partition/PartitionPlan.java
    src/main/java/graph/optimizer/partition/cost/AcceleratorPartitionScoreModel.java
    src/test/java/PreparedExecutionBuildTest.java
  </read_first>
  <action>
    Extend `AcceleratorPlanCostModel.Decision` with `AcceleratorPartitionScoreModel.MaterializationCostSummary costSummary`.

    Update `AcceleratorPlanCostModel.decide(PartitionPlan plan, RuntimeConfig runtimeConfig)`:
    - keep existing `missing-plan`, `non-positive-estimated-work`, and `estimated-work-below-minimum` reasons.
    - for accepted plans, attach a static cost summary with preset `CONSERVATIVE` unless compile-time metadata provides a more specific preset.
    - reject plans when the summary reason is `rejected-materialization-cost`.
    - do not read any profile, calibration, history, benchmark, or tuning files.

    Extend `BackendSelectionDecisionTrace` with:
    - `AcceleratorPartitionScoreModel.MaterializationCostSummary costSummary`
    - `List<PartitionDecisionTrace.CandidateCostTrace> finalists`

    Update `DefaultBackendSelectionPolicy` so every decision record includes the cost summary when a plan is present. Selected decision reason remains `selected`; rejected cost decision reason uses `decision.reason()`.

    Add `PreparedExecutionBuildTest` coverage:
    - a selected accelerator decision has non-null `costSummary()`, non-blank `preset()`, and non-negative `estimatedTransferBytes()`.
    - a runtime minimum-work rejection still records reason `estimated-work-below-minimum` and carries `estimatedWork()`.
  </action>
  <verify>
    <automated>./gradlew test --tests PreparedExecutionBuildTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "MaterializationCostSummary costSummary|costSummary\\(\\)" src/main/java/backend/accelerator/select/AcceleratorPlanCostModel.java src/main/java/graph/execution/trace/BackendSelectionDecisionTrace.java src/main/java/backend/select/DefaultBackendSelectionPolicy.java` finds cost fields. `rg -n "estimated-work-below-minimum|rejected-materialization-cost|selected" src/test/java/PreparedExecutionBuildTest.java` finds assertions for selected and rejected decisions. Targeted Gradle test passes.
  </acceptance_criteria>
  <done>Prepare-time backend selection emits materialization-aware cost summaries and stable rejection reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render compile/prepare cost summaries in benchmark reports</name>
  <files>src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java, src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java, src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java, src/test/java/BenchmarkSessionTest.java</files>
  <read_first>
    src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java
    src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java
    src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java
    src/main/java/graph/execution/trace/ExecutionTrace.java
    src/main/java/graph/execution/trace/CompileTrace.java
    src/main/java/graph/execution/trace/PrepareTrace.java
    src/main/java/graph/execution/trace/BackendSelectionTrace.java
    src/test/java/BenchmarkSessionTest.java
  </read_first>
  <action>
    Add compact report output for backend selection cost summaries.

    Text renderer target strings:
    - `backendSelectionCost:`
    - `selectedBackend=`
    - `preset=`
    - `finalScore=`
    - `boundaryCount=`
    - `estimatedTransferBytes=`
    - `estimatedComputeWork=`
    - `reason=`
    - `rejectedFinalists:`

    JSON renderer target shape under each measured candidate:
    - `trace.backendSelectionCost.selected`
    - `trace.backendSelectionCost.rejectedFinalists`
    - each selected/finalist object has `nodeIds`, `reason`, `finalScore`, `boundaryCount`, `estimatedTransferBytes`, `estimatedComputeWork`, and `preset`.

    Keep runtime step output unchanged except for existing execution metadata. Do not add planner cost fields to each `ExecutionStepTrace`.

    Add `BenchmarkSessionTest.renderersExposeBackendSelectionCostDiagnostics` using a hand-built `ExecutionTrace` with a `BackendSelectionTrace` containing one selected decision and two rejected finalists. Assert both text and JSON contain the exact target strings above.
  </action>
  <verify>
    <automated>./gradlew test --tests BenchmarkSessionTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "backendSelectionCost|rejectedFinalists|selectedBackend=|estimatedTransferBytes" src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java` finds renderer output. `rg -n "renderersExposeBackendSelectionCostDiagnostics|backendSelectionCost|rejectedFinalists" src/test/java/BenchmarkSessionTest.java` finds the renderer test. `rg -n "backendSelectionCost" src/main/java/graph/execution/trace/ExecutionStepTrace.java` returns no matches. Targeted Gradle test passes.
  </acceptance_criteria>
  <done>Benchmark text/json output exposes compact planner/backend cost diagnostics without bloating runtime step traces.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew test --tests PreparedExecutionBuildTest --tests BenchmarkSessionTest`
</verification>
