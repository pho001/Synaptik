---
phase: 003-materialization-aware-region-planning
plan: "03"
type: execute
wave: 3
depends_on:
  - "003-01"
  - "003-02"
files_modified:
  - src/test/java/graph/optimizer/partition/CpuNaturalExecutionRegionPlannerTest.java
  - src/test/java/OptimizerFuseTest.java
  - src/test/java/BFloat16BlasDispatchTest.java
  - src/test/java/PreparedExecutionBuildTest.java
  - docs/architecture.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PLAN-02
  - PLAN-03
  - PLAN-04
user_setup: []
must_haves:
  truths:
    - "D-11/D-12: CPU natural regions, CPU fusion, and BLAS paths are protected by focused regression tests."
    - "D-01/D-02/D-03: accelerator preference remains conditional on legality and static cost, not blanket GPU offload."
    - "D-07/D-13: profile/calibration-derived cost model wiring is explicitly deferred to Phase 4 and not implemented in Phase 3."
    - "D-08/D-10: final docs describe where planner/backend cost decisions appear in compile traces, prepare traces, and benchmark reports."
  artifacts:
    - path: "src/test/java/graph/optimizer/partition/CpuNaturalExecutionRegionPlannerTest.java"
      provides: "CPU natural region guard coverage"
      contains: "cpuNaturalRegionsRemainAvailableWhenAcceleratorBenefitIsAmbiguous"
    - path: "src/test/java/OptimizerFuseTest.java"
      provides: "CPU fused execution guard coverage"
      contains: "cpuFusionRemainsAvailableWithAcceleratorOffloadPolicy"
    - path: "src/test/java/BFloat16BlasDispatchTest.java"
      provides: "CPU BLAS guard coverage"
      contains: "blasDispatchRemainsAvailableWithAcceleratorOffloadPolicy"
    - path: "docs/architecture.md"
      provides: "developer documentation for static cost planning and Phase 4 tuning deferral"
      contains: "Materialization-aware region planning"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 plan list and Phase 4 cost-model ownership note"
      contains: "profile/calibration-derived cost model update"
  key_links:
    - from: "src/test/java/PreparedExecutionBuildTest.java"
      to: "src/main/java/backend/select/DefaultBackendSelectionPolicy.java"
      via: "selection trace assertions"
      pattern: "backendSelection"
    - from: "docs/architecture.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 4 deferred profile/calibration cost ownership"
      pattern: "profile/calibration-derived cost model update"
---

<objective>
Lock CPU safeguards, complete regression/documentation coverage, and update planning state for Phase 3.

Output: CPU natural/fusion/BLAS tests prove CPU hot paths remain competitive, docs explain static cost trace surfaces, and Phase 4 explicitly owns profile/calibration-derived cost model updates.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/003-materialization-aware-region-planning/003-CONTEXT.md
@.planning/phases/003-materialization-aware-region-planning/003-RESEARCH.md
@.planning/phases/003-materialization-aware-region-planning/003-VALIDATION.md
@.planning/phases/003-materialization-aware-region-planning/003-PATTERNS.md
@.planning/phases/003-materialization-aware-region-planning/003-01-PLAN.md
@.planning/phases/003-materialization-aware-region-planning/003-02-PLAN.md
@.planning/phases/002-metal-layout-aware-device-flow/002-03-SUMMARY.md
@AGENTS.md
</context>

<source_audit>
GOAL: Covered by CPU safeguards and final trace/report documentation.
REQ PLAN-02: Covered by end-to-end tests confirming profitable longer regions still win only under static cost gates.
REQ PLAN-03: Covered by backend selection and compile trace rejection assertions from earlier waves plus final targeted suite.
REQ PLAN-04: Covered by CPU natural region, CPU fusion, and BLAS regression tests.
CONTEXT D-01 through D-13: Covered across this plan's must-haves and prior plans.
USER NOTE: Phase 4 must plan profile/calibration-derived cost model update; this plan updates ROADMAP notes so it is not dropped.
</source_audit>

<threat_model>
T-3-05: Accelerator offload could capture CPU-hot graphs and regress CPU-only workloads. Mitigation: add tests for CPU natural regions, CPU fused execution, and BLAS dispatch under accelerator offload policy.
T-3-06: Phase 4 cost model ownership could be forgotten. Mitigation: update ROADMAP Phase 4 notes with an explicit profile/calibration-derived cost model update item.
T-3-08: Documentation could imply profile-derived selection is active in Phase 3. Mitigation: docs must state Phase 3 uses static named presets only and Phase 4 owns profile-derived cost wiring.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CPU natural, fusion, and BLAS safeguard regressions</name>
  <files>src/test/java/graph/optimizer/partition/CpuNaturalExecutionRegionPlannerTest.java, src/test/java/OptimizerFuseTest.java, src/test/java/BFloat16BlasDispatchTest.java, src/test/java/PreparedExecutionBuildTest.java</files>
  <read_first>
    src/test/java/graph/optimizer/partition/CpuNaturalExecutionRegionPlannerTest.java
    src/test/java/OptimizerFuseTest.java
    src/test/java/BFloat16BlasDispatchTest.java
    src/test/java/PreparedExecutionBuildTest.java
    src/main/java/graph/optimizer/partition/PartitionIntentRule.java
    src/main/java/graph/optimizer/partition/CpuNaturalExecutionRegionPlanner.java
    src/main/java/graph/optimizer/region/DefaultRegionOptimizer.java
    src/main/java/backend/cpu/kernels/linalg/matmul/plan/MatMulPlanner.java
  </read_first>
  <action>
    Add focused regression tests:
    - `CpuNaturalExecutionRegionPlannerTest.cpuNaturalRegionsRemainAvailableWhenAcceleratorBenefitIsAmbiguous`
    - `OptimizerFuseTest.cpuFusionRemainsAvailableWithAcceleratorOffloadPolicy`
    - `BFloat16BlasDispatchTest.blasDispatchRemainsAvailableWithAcceleratorOffloadPolicy`
    - `PreparedExecutionBuildTest.staticCostDoesNotSelectAcceleratorWhenCpuPathIsClearlyCompetitive`

    Required assertions:
    - CPU graph compilation with default CPU region policy still yields a `PartitionPlannerStrategy.CPU_NATURAL_EXECUTION_REGION` partition.
    - Fused CPU execution still produces a prepared step where `step.metadata().fusedExecutable() != null`.
    - BLAS-capable BF16 matmul/linear still prepares a CPU plan with compute contract backend `CPU_MATMUL_BLAS` when runtime BLAS config is enabled.
    - Accelerator offload candidate rejection reason is visible when static cost does not clearly beat CPU path; accepted CPU execution remains available.
  </action>
  <verify>
    <automated>./gradlew test --tests graph.optimizer.partition.CpuNaturalExecutionRegionPlannerTest --tests OptimizerFuseTest --tests BFloat16BlasDispatchTest --tests PreparedExecutionBuildTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "cpuNaturalRegionsRemainAvailableWhenAcceleratorBenefitIsAmbiguous|cpuFusionRemainsAvailableWithAcceleratorOffloadPolicy|blasDispatchRemainsAvailableWithAcceleratorOffloadPolicy|staticCostDoesNotSelectAcceleratorWhenCpuPathIsClearlyCompetitive" src/test/java` finds the required tests. `rg -n "CPU_NATURAL_EXECUTION_REGION|CPU_MATMUL_BLAS|fusedExecutable\\(\\) != null|rejected-materialization-cost" src/test/java/graph/optimizer/partition/CpuNaturalExecutionRegionPlannerTest.java src/test/java/OptimizerFuseTest.java src/test/java/BFloat16BlasDispatchTest.java src/test/java/PreparedExecutionBuildTest.java` finds the required assertions. Targeted Gradle command passes.
  </acceptance_criteria>
  <done>CPU natural regions, CPU fusion, and BLAS behavior are guarded against Phase 3 accelerator over-selection.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document static planning and Phase 4 cost-model ownership</name>
  <files>docs/architecture.md, .planning/ROADMAP.md</files>
  <read_first>
    docs/architecture.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/003-materialization-aware-region-planning/003-CONTEXT.md
    .planning/phases/003-materialization-aware-region-planning/003-RESEARCH.md
  </read_first>
  <action>
    Update `docs/architecture.md` with a section titled `Materialization-aware region planning` containing:
    - `Phase 3 uses static named presets only.`
    - `Planner summaries live in compile partition traces.`
    - `Backend selection summaries live in prepare traces.`
    - `Benchmark reports summarize selected candidates and top rejected finalists.`
    - `Runtime step traces remain execution-focused.`
    - `Profile/calibration-derived costs are deferred to Phase 4.`

    Update `.planning/ROADMAP.md`:
    - Add Phase 3 `Status: Planned` and `Plans: 3 plans`.
    - Add Phase 3 plan list for `003-01-PLAN.md`, `003-02-PLAN.md`, and `003-03-PLAN.md`.
    - In Phase 4 Notes, add exact sentence: `Phase 4 must include the profile/calibration-derived cost model update deferred from Phase 3 after graph autotune versus platform calibration ownership is audited.`
  </action>
  <verify>
    <automated>rg -n "Materialization-aware region planning|Phase 3 uses static named presets only|Profile/calibration-derived costs are deferred to Phase 4" docs/architecture.md && rg -n "003-01-PLAN.md|003-02-PLAN.md|003-03-PLAN.md|profile/calibration-derived cost model update deferred from Phase 3" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    `docs/architecture.md` contains `## Materialization-aware region planning` or `### Materialization-aware region planning`. `.planning/ROADMAP.md` contains `Plans: 3 plans`, all three Phase 3 plan filenames, and the exact Phase 4 deferral sentence. No files under `profiles/platform/` or `.planning/tmp/` are modified by this task.
  </acceptance_criteria>
  <done>Architecture docs and roadmap explicitly describe Phase 3 static cost behavior and Phase 4 profile/calibration cost-model ownership.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run final targeted verification and summarize Phase 3 execution readiness</name>
  <files>.planning/phases/003-materialization-aware-region-planning/003-03-SUMMARY.md, .planning/STATE.md</files>
  <read_first>
    .planning/phases/003-materialization-aware-region-planning/003-VALIDATION.md
    .planning/phases/003-materialization-aware-region-planning/003-01-PLAN.md
    .planning/phases/003-materialization-aware-region-planning/003-02-PLAN.md
    .planning/phases/003-materialization-aware-region-planning/003-03-PLAN.md
    .planning/STATE.md
  </read_first>
  <action>
    Run final targeted verification:
    - `./gradlew classes`
    - `./gradlew test --tests graph.optimizer.partition.cost.AcceleratorPartitionScoreModelTest --tests graph.optimizer.partition.CpuNaturalExecutionRegionPlannerTest --tests PreparedExecutionBuildTest --tests BenchmarkSessionTest --tests OptimizerFuseTest --tests BFloat16BlasDispatchTest`
    - `./gradlew metalTest` only if native Metal execution code changed in this phase.

    Create `003-03-SUMMARY.md` with:
    - changed source/test/docs files,
    - exact verification commands and results,
    - note that `.planning/tmp/` and `profiles/platform/.../tuning/abc/*` were not staged,
    - residual risk if `metalTest` is skipped due no native path changes.

    Update `.planning/STATE.md` Phase 3 row to `Planned` with `3 plans`, and set current focus to Phase 3 execution.
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests graph.optimizer.partition.cost.AcceleratorPartitionScoreModelTest --tests graph.optimizer.partition.CpuNaturalExecutionRegionPlannerTest --tests PreparedExecutionBuildTest --tests BenchmarkSessionTest --tests OptimizerFuseTest --tests BFloat16BlasDispatchTest</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/003-materialization-aware-region-planning/003-03-SUMMARY.md` exists and contains `./gradlew classes`, `BenchmarkSessionTest`, and `profiles/platform`. `.planning/STATE.md` contains `Materialization-Aware Region Planning | Planned | 3 plans`. `git status --short` shows no staged or newly modified `profiles/platform/.../tuning/abc/*` files caused by Phase 3 execution.
  </acceptance_criteria>
  <done>Phase 3 has final targeted verification evidence and planning state is ready for execute-phase.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew classes`
`./gradlew test --tests graph.optimizer.partition.cost.AcceleratorPartitionScoreModelTest --tests graph.optimizer.partition.CpuNaturalExecutionRegionPlannerTest --tests PreparedExecutionBuildTest --tests BenchmarkSessionTest --tests OptimizerFuseTest --tests BFloat16BlasDispatchTest`
</verification>
