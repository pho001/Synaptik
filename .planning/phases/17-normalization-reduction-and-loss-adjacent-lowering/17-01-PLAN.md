---
phase: 17-normalization-reduction-and-loss-adjacent-lowering
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
  - src/test/java/backend/accelerator/lowering/GpuLoweringCoverageMatrixTest.java
  - docs/gpu-lowering-coverage.md
  - .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-01-SUMMARY.md
autonomous: true
requirements: [GPUNORM-01, GPUNORM-02]
must_haves:
  truths:
    - "GPUNORM-01: The shared Metal/CUDA lowering matrix must explicitly cover normalization, reduction, softmax-ish, and loss-adjacent operation families."
    - "GPUNORM-02: The highest-impact Phase 14 hot-path blockers must be tied to either supported lowering or stable rejection evidence."
    - "D-01/D-02/D-03/D-04: Phase 17 coverage lives in GpuLoweringCoverageMatrix and references layer_norm_small, conv2d_resnet_3x3, and transformer_block_hot_path."
---

<objective>
Harden the shared Phase 17 lowering coverage contract so Metal and CUDA have explicit rows, stable statuses, and hot-path target evidence before backend legality changes.
</objective>

<source_audit>
GOAL: Expands or explicitly rejects high-impact normalization, reduction, softmax-ish, and loss-adjacent GPU lowering gaps under a shared Metal/CUDA contract.
REQ GPUNORM-01: Covered by exact matrix rows and tests for normalization, reduction, softmax-ish, and loss-adjacent families.
REQ GPUNORM-02: Covered by matrix notes tying blockers to Phase 14 target workloads.
CONTEXT D-01, D-02, D-03, and D-04 are implemented in this wave.
</source_audit>

<threat_model>
T-17-01: Matrix updates could claim GPU support without execution. Mitigation: mark rows `SUPPORTED` only when lowering and tests already prove it; otherwise use `FALLBACK` or `UNSUPPORTED`.
T-17-02: Target evidence could drift into backend-specific forks. Mitigation: keep target evidence in shared matrix notes/docs and keep backend adapters consuming the same rows.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing-first Phase 17 matrix tests</name>
  <files>src/test/java/backend/accelerator/lowering/GpuLoweringCoverageMatrixTest.java</files>
  <read_first>
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-CONTEXT.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-RESEARCH.md
    .planning/phases/14-coverage-gap-triage-and-hot-path-targets/14-HOT-PATH-TARGETS.md
    src/test/java/backend/accelerator/lowering/GpuLoweringCoverageMatrixTest.java
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageEntry.java
  </read_first>
  <action>
    Extend `GpuLoweringCoverageMatrixTest` with tests named:
    - `phaseSeventeenMatrixCoversNormalizationReductionSoftmaxAndLossRows`
    - `phaseSeventeenCoverageRowsTieToHotPathTargets`
    - `phaseSeventeenNonSupportedRowsUseStableReasonCodes`

    For both `ComputeBackend.GPU_METAL` and `ComputeBackend.GPU_CUDA`, assert matrix rows exist for exactly these Phase 17 op types:
    - `Operation.OpType.LAYER_NORM`
    - `Operation.OpType.RMS_NORM`
    - `Operation.OpType.SUM`
    - `Operation.OpType.MEAN`
    - `Operation.OpType.REDUCE_MIN`
    - `Operation.OpType.REDUCE_MAX`
    - `Operation.OpType.SOFTMAX`
    - `Operation.OpType.LOG_SOFTMAX`
    - `Operation.OpType.NLL_LOSS`
    - `Operation.OpType.CROSS_ENTROPY_LOSS`
    - `Operation.OpType.CROSS_ENTROPY_LOSS_INDICES`
    - `Operation.OpType.CROSS_ENTROPY_LOSS_INDICES_GRAD`
    - `Operation.OpType.CONV2D`

    Assert `SOFTMAX` and `LOG_SOFTMAX` are `SUPPORTED`, reductions/norms are not `SUPPORTED` unless implementation in this phase proves otherwise, and loss-adjacent INT32-target rows use `GpuLoweringUnsupportedReason.UNSUPPORTED_DTYPE`.

    Assert at least one Phase 17 row note contains each exact hot-path target string:
    - `target=layer_norm_small`
    - `target=conv2d_resnet_3x3`
    - `target=transformer_block_hot_path`
  </action>
  <verify>
    <automated>./gradlew test --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "phaseSeventeenMatrixCoversNormalizationReductionSoftmaxAndLossRows|phaseSeventeenCoverageRowsTieToHotPathTargets|phaseSeventeenNonSupportedRowsUseStableReasonCodes|target=layer_norm_small|target=conv2d_resnet_3x3|target=transformer_block_hot_path" src/test/java/backend/accelerator/lowering/GpuLoweringCoverageMatrixTest.java` exits 0.
  </acceptance_criteria>
  <done>Tests specify the Phase 17 coverage contract before implementation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add shared matrix helpers and Phase 17 target notes</name>
  <files>src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java</files>
  <read_first>
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageEntry.java
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageStatus.java
    src/main/java/backend/accelerator/lowering/GpuLoweringOperationFamily.java
    src/main/java/backend/accelerator/lowering/GpuLoweringUnsupportedReason.java
    src/test/java/backend/accelerator/lowering/GpuLoweringCoverageMatrixTest.java
  </read_first>
  <action>
    Add a public helper:
    `public static List<GpuLoweringCoverageEntry> entriesForFamily(ComputeBackend backend, GpuLoweringOperationFamily family)`

    Add a public helper:
    `public static String plannerUnsupportedDetail(ComputeBackend backend, Operation.OpType opType)`

    `plannerUnsupportedDetail(...)` must return a stable string containing:
    - `entry.reason().name()`
    - `operation <OP> is not supported by <BACKEND> lowering`
    - `family=<FAMILY>`
    - `status=<STATUS>`
    - `note=<entry.note()>`

    Update Phase 17 row notes in `addBackend(...)` so the relevant rows contain:
    - `target=layer_norm_small` for `LAYER_NORM`, `RMS_NORM`, `SUM`, `MEAN`, `REDUCE_MIN`, and `REDUCE_MAX`
    - `target=conv2d_resnet_3x3` for `CONV2D`
    - `target=transformer_block_hot_path` for `SOFTMAX`, `LOG_SOFTMAX`, and loss-adjacent rows

    Preserve existing statuses unless the task also adds lowering and parity tests proving support.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "entriesForFamily|plannerUnsupportedDetail|family=|status=|target=layer_norm_small|target=conv2d_resnet_3x3|target=transformer_block_hot_path" src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java` exits 0.
  </acceptance_criteria>
  <done>Shared coverage matrix exposes Phase 17 target evidence and reusable detail strings.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document the Phase 17 coverage contract</name>
  <files>docs/gpu-lowering-coverage.md</files>
  <read_first>
    docs/gpu-lowering-coverage.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-CONTEXT.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-RESEARCH.md
    .planning/phases/14-coverage-gap-triage-and-hot-path-targets/14-HOT-PATH-TARGETS.md
  </read_first>
  <action>
    Add or update a section named `Phase 17 normalization, reduction, and loss-adjacent contract` in `docs/gpu-lowering-coverage.md`.

    The section must include exact strings:
    - `GPUNORM-01`
    - `GPUNORM-02`
    - `target=layer_norm_small`
    - `target=conv2d_resnet_3x3`
    - `target=transformer_block_hot_path`
    - `LOG_SOFTMAX remains lowered as SOFTMAX followed by LOG`
    - `unsupported loss-adjacent rows must remain visible fallback, not silent CPU replay`
  </action>
  <verify>
    <automated>rg -n "Phase 17 normalization, reduction, and loss-adjacent contract|GPUNORM-01|GPUNORM-02|target=layer_norm_small|target=conv2d_resnet_3x3|target=transformer_block_hot_path|LOG_SOFTMAX remains lowered as SOFTMAX followed by LOG|unsupported loss-adjacent rows must remain visible fallback" docs/gpu-lowering-coverage.md</automated>
  </verify>
  <acceptance_criteria>
    `docs/gpu-lowering-coverage.md` contains the Phase 17 section and all three target strings.
  </acceptance_criteria>
  <done>Developer docs describe the shared Phase 17 coverage contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Record coverage contract summary</name>
  <files>.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-01-PLAN.md
    src/test/java/backend/accelerator/lowering/GpuLoweringCoverageMatrixTest.java
    docs/gpu-lowering-coverage.md
  </read_first>
  <action>
    Create `17-01-SUMMARY.md` with:
    - frontmatter containing `phase: 17-normalization-reduction-and-loss-adjacent-lowering`, `plan: "01"`, and `status: complete`
    - a section `Phase 17 coverage matrix contract`
    - a verification table listing `./gradlew test --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest`
    - a requirement coverage note for `GPUNORM-01` and `GPUNORM-02`
    - a target evidence note containing `target=layer_norm_small`, `target=conv2d_resnet_3x3`, and `target=transformer_block_hot_path`
    - a hygiene note containing exact text `profiles/platform/.../tuning/abc/* remained unstaged`
  </action>
  <verify>
    <automated>test -f .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-01-SUMMARY.md</automated>
    <automated>rg -n "Phase 17 coverage matrix contract|GPUNORM-01|GPUNORM-02|target=layer_norm_small|target=conv2d_resnet_3x3|target=transformer_block_hot_path|profiles/platform/.../tuning/abc/\\* remained unstaged" .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-01-SUMMARY.md` contains `Phase 17 coverage matrix contract`, all three target strings, and both requirement IDs.
  </acceptance_criteria>
  <done>Wave 1 evidence is ready for backend legality work.</done>
</task>

</tasks>

<verification>
Run:

```bash
./gradlew test --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest
git status --short
```

Only intentional source/docs/planning files may be staged. Local `profiles/platform/.../tuning/abc/*` files must remain unstaged.
</verification>

<success_criteria>
- Phase 17 matrix rows are testable for both Metal and CUDA.
- Matrix notes tie top Phase 17 blockers to Phase 14 target workloads.
- Docs distinguish supported softmax-ish lowering from visible rejection/fallback rows.
</success_criteria>
