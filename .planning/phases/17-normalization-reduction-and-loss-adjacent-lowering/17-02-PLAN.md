---
phase: 17-normalization-reduction-and-loss-adjacent-lowering
plan: "02"
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/main/java/backend/metal/lowering/MetalPartitionSupport.java
  - src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java
  - src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
  - src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
  - src/test/java/PreparedExecutionBuildTest.java
  - .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-02-SUMMARY.md
autonomous: true
requirements: [GPUNORM-01, GPUNORM-02, GPUNORM-03]
must_haves:
  truths:
    - "GPUNORM-01/GPUNORM-02: Metal and CUDA legality must consume the shared Phase 17 matrix and expose precise rejections for reduction, normalization, conv, and loss blockers."
    - "GPUNORM-03: Layout and dtype legality from earlier phases must remain enforced before unsupported reduction or loss cases reach execution."
    - "D-05/D-06/D-07/D-08: Backend-specific adapters own capability/layout gates but must not fork the shared semantic lowering contract."
---

<objective>
Make Metal and CUDA planner legality render precise Phase 17 rejection details for reductions, normalization, conv, and loss-adjacent blockers while preserving dtype/layout gates.
</objective>

<source_audit>
GOAL: Expands or explicitly rejects high-impact Phase 17 GPU lowering gaps under a shared Metal/CUDA contract.
REQ GPUNORM-01: Covered by backend adapters consuming shared matrix rows.
REQ GPUNORM-02: Covered by stable rejection detail tied to hot-path blockers.
REQ GPUNORM-03: Covered by required-mode and layout/dtype gate tests.
CONTEXT D-05, D-06, D-07, and D-08 are implemented in this wave.
</source_audit>

<threat_model>
T-17-03: Backend adapters could fork semantic legality and diverge from the shared matrix. Mitigation: use `GpuLoweringCoverageMatrix.plannerUnsupportedDetail(...)` for shared rejection text.
T-17-04: New rejection detail could accidentally bypass existing Metal dtype gates or CUDA non-dense layout gates. Mitigation: keep those guards before/after matrix checks as currently ordered and add regression tests.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend rejection detail tests</name>
  <files>src/test/java/backend/metal/lowering/MetalRegionLowererTest.java, src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java</files>
  <read_first>
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-CONTEXT.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-RESEARCH.md
    src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
    src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
    src/main/java/backend/metal/lowering/MetalPartitionSupport.java
    src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
  </read_first>
  <action>
    Extend Metal and CUDA lowerer tests with methods named:
    - `metalPhaseSeventeenReductionAndNormReasonsIncludeMatrixDetail`
    - `metalPhaseSeventeenConvAndLossReasonsIncludeMatrixDetail`
    - `cudaPhaseSeventeenReductionAndNormReasonsIncludeMatrixDetail`
    - `cudaPhaseSeventeenConvAndLossReasonsIncludeMatrixDetail`

    For each backend, build graph snippets and assert rejection strings contain:
    - `REDUCTION_ADJACENT` for `SUM`, `MEAN`, `LAYER_NORM`, and `RMS_NORM`
    - `family=REDUCTION` for reduction rows
    - `family=NORMALIZATION` for normalization rows
    - `family=CONV_POOL` for `CONV2D`
    - `family=LOSS_ADJACENT` for `CROSS_ENTROPY_LOSS_INDICES`
    - `target=layer_norm_small`, `target=conv2d_resnet_3x3`, or `target=transformer_block_hot_path` as appropriate

    Preserve existing assertions that strings still contain `operation <OP> is not supported by GPU_METAL lowering` and `operation <OP> is not supported by GPU_CUDA lowering`.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "metalPhaseSeventeenReductionAndNormReasonsIncludeMatrixDetail|metalPhaseSeventeenConvAndLossReasonsIncludeMatrixDetail|cudaPhaseSeventeenReductionAndNormReasonsIncludeMatrixDetail|cudaPhaseSeventeenConvAndLossReasonsIncludeMatrixDetail|family=REDUCTION|family=NORMALIZATION|family=CONV_POOL|family=LOSS_ADJACENT" src/test/java/backend/metal/lowering/MetalRegionLowererTest.java src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java` exits 0.
  </acceptance_criteria>
  <done>Tests specify precise backend rejection evidence before adapter changes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire backend adapters to shared matrix detail</name>
  <files>src/main/java/backend/metal/lowering/MetalPartitionSupport.java, src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java</files>
  <read_first>
    src/main/java/backend/metal/lowering/MetalPartitionSupport.java
    src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
    src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
    src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
  </read_first>
  <action>
    In `MetalPartitionSupport.plannerUnsupportedReason(...)`, replace the non-supported matrix-row string assembly with:
    `return compoundPatternPrefix(opType) + GpuLoweringCoverageMatrix.plannerUnsupportedDetail(ComputeBackend.GPU_METAL, opType);`

    In `CudaGpuRegionLegalityAdapter.plannerUnsupportedReason(...)`, replace the non-supported matrix-row string assembly with:
    `return compoundPatternPrefix(opType) + GpuLoweringCoverageMatrix.plannerUnsupportedDetail(ComputeBackend.GPU_CUDA, opType);`

    Do not change:
    - Metal null/leaf/dtype/SDPA checks
    - CUDA backend assignment, leaf checks, or `hasDirectNonDenseInput(...)` rejection
    - `compoundPatternPrefix(...)` behavior
  </action>
  <verify>
    <automated>./gradlew test --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "plannerUnsupportedDetail\\(ComputeBackend.GPU_METAL|plannerUnsupportedDetail\\(ComputeBackend.GPU_CUDA|compoundPatternPrefix" src/main/java/backend/metal/lowering/MetalPartitionSupport.java src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java` exits 0.
  </acceptance_criteria>
  <done>Backend legality diagnostics share the Phase 17 matrix detail path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add required-mode and gate preservation tests</name>
  <files>src/test/java/PreparedExecutionBuildTest.java, src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java</files>
  <read_first>
    src/test/java/PreparedExecutionBuildTest.java
    src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
    src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java
    src/main/java/backend/metal/lowering/MetalPartitionSupport.java
  </read_first>
  <action>
    Add or extend tests named:
    - `metalRequiredModeExposesPhaseSeventeenReductionRejection`
    - `cudaRequiredModeExposesPhaseSeventeenNormalizationRejection`
    - `cudaPhaseSeventeenKeepsDirectNonDenseLayoutRejectionBeforeExecution`

    Required-mode tests must build small `FLOAT32` graphs using `sum(1)` and `layerNorm(...)`, assign `GPU_METAL` or `GPU_CUDA`, prepare with accelerator-required behavior already used in `PreparedExecutionBuildTest`, and assert the prepare failure or trace detail contains:
    - `family=REDUCTION` or `family=NORMALIZATION`
    - `target=layer_norm_small`
    - `operation SUM is not supported` or `operation LAYER_NORM is not supported`

    The CUDA non-dense test must preserve the exact existing layout rejection string:
    `UNSUPPORTED_LAYOUT: direct non-dense CUDA compute remains conservative until metadata-only view propagation or dense materialization makes the consumer layout legal`
  </action>
  <verify>
    <automated>./gradlew test --tests PreparedExecutionBuildTest --tests backend.cuda.lowering.CudaRegionLowererTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "metalRequiredModeExposesPhaseSeventeenReductionRejection|cudaRequiredModeExposesPhaseSeventeenNormalizationRejection|cudaPhaseSeventeenKeepsDirectNonDenseLayoutRejectionBeforeExecution|UNSUPPORTED_LAYOUT: direct non-dense CUDA compute remains conservative" src/test/java/PreparedExecutionBuildTest.java src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java` exits 0.
  </acceptance_criteria>
  <done>Required-mode behavior and prior layout gates remain visible.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Record backend legality summary</name>
  <files>.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-02-SUMMARY.md</files>
  <read_first>
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-02-PLAN.md
    src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
    src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
    src/test/java/PreparedExecutionBuildTest.java
  </read_first>
  <action>
    Create `17-02-SUMMARY.md` with:
    - frontmatter containing `phase: 17-normalization-reduction-and-loss-adjacent-lowering`, `plan: "02"`, and `status: complete`
    - a section `Backend legality and stable rejection detail`
    - a verification table listing `./gradlew test --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest` and `./gradlew test --tests PreparedExecutionBuildTest --tests backend.cuda.lowering.CudaRegionLowererTest`
    - requirement coverage notes for `GPUNORM-01`, `GPUNORM-02`, and `GPUNORM-03`
    - a guardrail note containing exact text `layout and dtype legality gates remained explicit`
    - a hygiene note containing exact text `profiles/platform/.../tuning/abc/* remained unstaged`
  </action>
  <verify>
    <automated>test -f .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-02-SUMMARY.md</automated>
    <automated>rg -n "Backend legality and stable rejection detail|GPUNORM-01|GPUNORM-02|GPUNORM-03|layout and dtype legality gates remained explicit|profiles/platform/.../tuning/abc/\\* remained unstaged" .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-02-SUMMARY.md` contains all three `GPUNORM` IDs and the explicit gate phrase.
  </acceptance_criteria>
  <done>Wave 2 evidence is ready for trace/report parity work.</done>
</task>

</tasks>

<verification>
Run:

```bash
./gradlew test --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest
./gradlew test --tests PreparedExecutionBuildTest --tests backend.cuda.lowering.CudaRegionLowererTest
git status --short
```
</verification>

<success_criteria>
- Metal and CUDA rejection detail comes from the shared matrix.
- Reduction, normalization, conv, and loss blockers include family/status/target evidence.
- Existing dtype and layout gates remain explicit and tested.
</success_criteria>
