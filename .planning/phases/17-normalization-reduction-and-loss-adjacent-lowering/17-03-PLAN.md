---
phase: 17-normalization-reduction-and-loss-adjacent-lowering
plan: "03"
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - src/test/java/PreparedExecutionBuildTest.java
  - src/test/java/CompiledGraphTraceTest.java
  - src/test/java/GpuCoverageSummaryTest.java
  - src/test/java/BenchmarkSessionTest.java
  - .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-03-SUMMARY.md
autonomous: true
requirements: [GPUNORM-02, GPUNORM-03]
must_haves:
  truths:
    - "GPUNORM-02: Softmax-ish support and loss-adjacent rejection must be tied to measured hot-path blockers instead of generic CPU replay."
    - "GPUNORM-03: Numerically sensitive softmax/loss flows must preserve CPU parity and visible fallback when backend primitive coverage is incomplete."
    - "D-09/D-10/D-11/D-12/D-13/D-14: LOG_SOFTMAX support, INT32 loss rejection, CPU parity, materialization boundaries, and trace/report evidence must remain visible."
---

<objective>
Add trace, coverage, benchmark, and CPU parity evidence proving `LOG_SOFTMAX` remains supported while loss-adjacent and sensitive fallback paths are explicit.
</objective>

<source_audit>
GOAL: Makes high-impact softmax-ish and loss-adjacent region blockers auditable in prepare traces, coverage summaries, benchmark reports, and CPU parity tests.
REQ GPUNORM-02: Covered by trace/report evidence for selected and rejected candidates.
REQ GPUNORM-03: Covered by CPU parity tests and visible fallback evidence for numerically sensitive flows.
CONTEXT D-09, D-10, D-11, D-12, D-13, and D-14 are implemented in this wave.
</source_audit>

<threat_model>
T-17-05: Loss-adjacent fallback could be numerically correct but invisible in traces. Mitigation: assert trace/report strings for `UNSUPPORTED_DTYPE`, `family=LOSS_ADJACENT`, and target evidence.
T-17-06: Softmax-ish support could regress while rejection tests still pass. Mitigation: assert `LOG_SOFTMAX` manifests still contain `SOFTMAX` and `LOG` lowered primitives.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add softmax/loss CPU parity and prepare evidence tests</name>
  <files>src/test/java/PreparedExecutionBuildTest.java</files>
  <read_first>
    src/test/java/PreparedExecutionBuildTest.java
    src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
    src/main/java/backend/accelerator/residency/AcceleratorDTypeResidencyPolicy.java
    src/main/java/tensor/TensorOps.java
  </read_first>
  <action>
    Add tests named:
    - `phaseSeventeenLogSoftmaxResidualFlowMatchesCpuAndStaysSupported`
    - `phaseSeventeenCrossEntropyIndexFallbackMatchesCpuAndReportsUnsupportedDType`
    - `phaseSeventeenLayerNormFallbackMatchesCpuAndReportsReductionAdjacent`

    The `LOG_SOFTMAX` test must build a small `linear(...).logSoftmax(1)` or `matmul(...).logSoftmax(1)` graph, assign accelerator backend to lowerable nodes, compare the final output to a CPU-only graph with tolerance `1.0e-5`, and assert selected manifest/lowering evidence contains both `LOG_SOFTMAX` and lowered primitive strings `SOFTMAX` and `LOG`.

    The cross-entropy index test must build `logits.crossEntropyLossFromIndices(targetIndices, 1)` with `targetIndices` as `DataType.INT32`, compare output to CPU baseline with tolerance `1.0e-5`, and assert prepare trace or failure detail contains `UNSUPPORTED_DTYPE`, `family=LOSS_ADJACENT`, and `target=transformer_block_hot_path`.

    The layer norm test must build `input.layerNorm(gamma, beta, 1.0e-5)`, compare CPU baseline to accelerator-configured output with tolerance `1.0e-5`, and assert visible rejection contains `REDUCTION_ADJACENT`, `family=NORMALIZATION`, and `target=layer_norm_small`.
  </action>
  <verify>
    <automated>./gradlew test --tests PreparedExecutionBuildTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "phaseSeventeenLogSoftmaxResidualFlowMatchesCpuAndStaysSupported|phaseSeventeenCrossEntropyIndexFallbackMatchesCpuAndReportsUnsupportedDType|phaseSeventeenLayerNormFallbackMatchesCpuAndReportsReductionAdjacent|target=transformer_block_hot_path|target=layer_norm_small" src/test/java/PreparedExecutionBuildTest.java` exits 0.
  </acceptance_criteria>
  <done>Numerically sensitive flows have CPU parity and visible support/rejection evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add prepare trace evidence tests</name>
  <files>src/test/java/CompiledGraphTraceTest.java</files>
  <read_first>
    src/test/java/CompiledGraphTraceTest.java
    src/main/java/graph/execution/trace/PreparedExecutionTrace.java
    src/main/java/backend/select/BackendSelectionDecisionTrace.java
    src/main/java/backend/accelerator/lowering/GpuLoweredRegionManifestRenderer.java
  </read_first>
  <action>
    Add or extend a test named `prepareTraceRendersPhaseSeventeenNormAndLossEvidence`.

    The rendered trace must contain exact strings:
    - `LOG_SOFTMAX`
    - `SOFTMAX`
    - `UNSUPPORTED_DTYPE`
    - `family=LOSS_ADJACENT`
    - `REDUCTION_ADJACENT`
    - `family=NORMALIZATION`
    - `target=layer_norm_small`
    - `target=transformer_block_hot_path`

    Use synthetic `BackendSelectionDecisionTrace` fixtures if constructing a full graph would make the test brittle.
  </action>
  <verify>
    <automated>./gradlew test --tests CompiledGraphTraceTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "prepareTraceRendersPhaseSeventeenNormAndLossEvidence|family=LOSS_ADJACENT|family=NORMALIZATION|target=layer_norm_small|target=transformer_block_hot_path" src/test/java/CompiledGraphTraceTest.java` exits 0.
  </acceptance_criteria>
  <done>Prepare trace renders Phase 17 support and rejection evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add coverage and benchmark report evidence tests</name>
  <files>src/test/java/GpuCoverageSummaryTest.java, src/test/java/BenchmarkSessionTest.java</files>
  <read_first>
    src/test/java/GpuCoverageSummaryTest.java
    src/test/java/BenchmarkSessionTest.java
    src/main/java/tuning/benchmark/report/GpuCoverageSummary.java
    src/main/java/tuning/benchmark/report/BenchmarkReportRenderer.java
    src/main/java/tuning/benchmark/report/BenchmarkJsonReportRenderer.java
  </read_first>
  <action>
    Add tests named:
    - `coverageSummaryCountsPhaseSeventeenNormAndLossReasons`
    - `benchmarkReportsRenderPhaseSeventeenNormAndLossEvidence`

    The coverage summary fixture must include at least one selected `LOG_SOFTMAX` manifest and at least two rejected candidates:
    - one normalization rejection with `family=NORMALIZATION`, `REDUCTION_ADJACENT`, and `target=layer_norm_small`
    - one loss-adjacent rejection with `family=LOSS_ADJACENT`, `UNSUPPORTED_DTYPE`, and `target=transformer_block_hot_path`

    Text and JSON benchmark assertions must contain:
    - `LOG_SOFTMAX`
    - `SOFTMAX`
    - `family=NORMALIZATION`
    - `family=LOSS_ADJACENT`
    - `UNSUPPORTED_DTYPE`
    - `target=layer_norm_small`
    - `target=transformer_block_hot_path`
  </action>
  <verify>
    <automated>./gradlew test --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "coverageSummaryCountsPhaseSeventeenNormAndLossReasons|benchmarkReportsRenderPhaseSeventeenNormAndLossEvidence|family=NORMALIZATION|family=LOSS_ADJACENT|target=layer_norm_small|target=transformer_block_hot_path" src/test/java/GpuCoverageSummaryTest.java src/test/java/BenchmarkSessionTest.java` exits 0.
  </acceptance_criteria>
  <done>Coverage and benchmark reports expose Phase 17 evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Record trace and parity summary</name>
  <files>.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-03-SUMMARY.md</files>
  <read_first>
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-03-PLAN.md
    src/test/java/PreparedExecutionBuildTest.java
    src/test/java/CompiledGraphTraceTest.java
    src/test/java/GpuCoverageSummaryTest.java
    src/test/java/BenchmarkSessionTest.java
  </read_first>
  <action>
    Create `17-03-SUMMARY.md` with:
    - frontmatter containing `phase: 17-normalization-reduction-and-loss-adjacent-lowering`, `plan: "03"`, and `status: complete`
    - a section `Softmax and loss-adjacent evidence`
    - a verification table listing `./gradlew test --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest`
    - requirement coverage notes for `GPUNORM-02` and `GPUNORM-03`
    - a parity note containing exact text `CPU parity remained the correctness oracle`
    - a visibility note containing exact text `loss-adjacent fallback remained visible`
    - a hygiene note containing exact text `profiles/platform/.../tuning/abc/* remained unstaged`
  </action>
  <verify>
    <automated>test -f .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-03-SUMMARY.md</automated>
    <automated>rg -n "Softmax and loss-adjacent evidence|GPUNORM-02|GPUNORM-03|CPU parity remained the correctness oracle|loss-adjacent fallback remained visible|profiles/platform/.../tuning/abc/\\* remained unstaged" .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-03-SUMMARY.md` contains `Softmax and loss-adjacent evidence`, both requirement IDs, and the fallback visibility phrase.
  </acceptance_criteria>
  <done>Wave 3 evidence is ready for closure.</done>
</task>

</tasks>

<verification>
Run:

```bash
./gradlew test --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest
git status --short
```
</verification>

<success_criteria>
- `LOG_SOFTMAX` support still renders as `SOFTMAX` plus `LOG`.
- Loss-adjacent INT32-target fallback is visible with `UNSUPPORTED_DTYPE`.
- Numerically sensitive flows compare against CPU output with explicit tolerances.
</success_criteria>
