---
phase: 08-cuda-observability-and-documentation-closure
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java
  - src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
  - src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java
  - src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
  - .planning/phases/08-cuda-observability-and-documentation-closure/08-02-SUMMARY.md
autonomous: true
requirements:
  - CUDA-06
user_setup:
  - "Do not broaden CUDA operation coverage beyond the narrow dense FLOAT32 native-buffer path."
must_haves:
  truths:
    - "D-21: CUDA fallback and required-mode failures expose stable `AcceleratorBufferReasonCode` values."
    - "D-22: REQUIRED mode throws before tensor-array bridge execution or CPU fallback can hide buffer unavailability."
    - "D-23: Unsupported dtype/layout and incompatible device binding reasons are tested without CUDA hardware."
  artifacts:
    - path: "src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java"
      provides: "CUDA reason-code matrix for policy decisions"
      contains: "INPUT_DTYPE_UNSUPPORTED"
    - path: "src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java"
      provides: "prepared execution required-mode and native-failure coverage"
      contains: "REQUIRED_BUFFER_EXECUTION_UNAVAILABLE"
---

<objective>
Stabilize CUDA fallback and required-mode reason-code behavior so unavailable native runtime, unsupported dtype/layout, incompatible bindings, native failures, and required-but-unavailable execution are explicit in tests and traces.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cuda-observability-and-documentation-closure/08-CONTEXT.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-RESEARCH.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-VALIDATION.md
@src/main/java/backend/accelerator/buffer/AcceleratorBufferReasonCode.java
@src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java
@src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
@src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java
@src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
@AGENTS.md
</context>

<source_audit>
GOAL: Covers stable CUDA fallback and required-mode failure observability.
REQ CUDA-06: Covered by exact reason-code tests and required-mode non-fallback assertions.
CONTEXT: Covers stable reason codes and visible fallback.
</source_audit>

<threat_model>
T-8-03: CUDA fallback could collapse distinct failures into generic CPU fallback. Mitigation: test exact reason codes for unavailable ABI, unsupported dtype/layout, incompatible binding, native failure, and required-unavailable paths.
T-8-04: REQUIRED mode could silently execute tensor-array or CPU fallback after buffer unavailability. Mitigation: fake bridge tests assert tensor-array execute is not called and CPU fallback does not satisfy required buffer mode.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete CUDA buffer policy reason-code matrix</name>
  <files>src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java, src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java</files>
  <read_first>
    src/main/java/backend/accelerator/buffer/AcceleratorBufferReasonCode.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferExecutionPath.java
    src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java
    src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java
  </read_first>
  <action>
    Ensure `CudaAcceleratorBufferBinderTest` asserts exact `AcceleratorBufferReasonCode` and path values for:
    - bridge lacking native buffer ABI -> `NATIVE_BUFFER_ABI_UNAVAILABLE`, path `TENSOR_ARRAY` in AUTO and `UNAVAILABLE` in REQUIRE
    - disabled buffer binding -> `BUFFER_BINDINGS_DISABLED`, path `TENSOR_ARRAY`
    - input dtype mismatch -> `INPUT_DTYPE_UNSUPPORTED`
    - output dtype mismatch -> `OUTPUT_DTYPE_UNSUPPORTED`
    - input layout mismatch -> `INPUT_LAYOUT_UNSUPPORTED`
    - output layout mismatch -> `OUTPUT_LAYOUT_UNSUPPORTED`
    - missing compatible CUDA binding with stale CPU storage -> `INPUT_NOT_CPU_CURRENT`
    - incompatible existing non-CUDA or mismatched CUDA binding -> `INPUT_BINDING_UNAVAILABLE`

    If production code does not currently produce one of these exact reason codes, update `CudaAcceleratorBufferBinder` so it does.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "NATIVE_BUFFER_ABI_UNAVAILABLE|BUFFER_BINDINGS_DISABLED|INPUT_DTYPE_UNSUPPORTED|OUTPUT_DTYPE_UNSUPPORTED|INPUT_LAYOUT_UNSUPPORTED|OUTPUT_LAYOUT_UNSUPPORTED|INPUT_NOT_CPU_CURRENT|INPUT_BINDING_UNAVAILABLE" src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java` exits 0. Focused CUDA binder tests pass.
  </acceptance_criteria>
  <done>CUDA buffer policy has portable exact reason-code coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove REQUIRED mode blocks tensor-array and CPU fallback</name>
  <files>src/main/java/backend/cuda/exec/PreparedCudaExecutable.java, src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java</files>
  <read_first>
    src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
    src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
    src/main/java/backend/accelerator/exec/PreparedAcceleratorExecutionSupport.java
    src/main/java/backend/cuda/bridge/CudaGraphBridge.java
  </read_first>
  <action>
    Extend `PreparedCudaExecutableBufferPolicyTest` with exact tests:
    - `requiredModeBridgeUnavailableThrowsBridgeUnavailableBeforeTensorArray`
    - `requiredModeNativeBufferAbiUnavailableThrowsBeforeTensorArray`
    - `requiredModeNativeBufferFailureThrowsNativeBufferExecutionFailed`

    Each test must:
    - configure `AcceleratorBufferBindingMode.REQUIRE`
    - execute a fake CUDA executable
    - assert the thrown message contains `Accelerator buffer path is required for GPU_CUDA but unavailable:`
    - assert the message contains the exact reason code under test
    - assert fake tensor-array `execute(...)` was not invoked
    - assert CPU fallback did not publish a successful required-mode result

    If current production behavior falls through to CPU fallback, update `PreparedCudaExecutable.requireBufferOrThrow(...)` call sites so required failure is thrown before fallback execution.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "requiredModeBridgeUnavailableThrowsBridgeUnavailableBeforeTensorArray|requiredModeNativeBufferAbiUnavailableThrowsBeforeTensorArray|requiredModeNativeBufferFailureThrowsNativeBufferExecutionFailed|Accelerator buffer path is required for GPU_CUDA" src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java src/main/java/backend/cuda/exec/PreparedCudaExecutable.java` exits 0. Focused prepared CUDA tests pass.
  </acceptance_criteria>
  <done>REQUIRED CUDA buffer mode cannot be hidden by tensor-array or CPU fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove reason codes appear in run trace attributes</name>
  <files>src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java</files>
  <read_first>
    src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
    src/main/java/graph/execution/PreparedExecution.java
    src/main/java/graph/execution/trace/StepExecutionMetadata.java
  </read_first>
  <action>
    Add or extend a trace-oriented prepared CUDA test that executes through a prepared/traced path and asserts step metadata contains:
    - `acceleratorBufferBackend = GPU_CUDA`
    - `acceleratorBufferExecutionPath`
    - `acceleratorBufferReasonCode`
    - `acceleratorBufferReason`
    - `cudaExecutionPath`
    - `cudaFallbackReason`

    Use fake CUDA fixtures; do not require CUDA hardware.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "acceleratorBufferReasonCode|cudaExecutionPath|cudaFallbackReason|GPU_CUDA" src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java` exits 0. Focused prepared CUDA tests pass.
  </acceptance_criteria>
  <done>Stable CUDA reason codes are visible in run trace metadata.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew test --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
</verification>

<success_criteria>
- CUDA fallback paths expose stable reason codes.
- REQUIRED mode throws before tensor-array or CPU fallback.
- Run trace attributes include the selected reason code and fallback reason.
</success_criteria>
