---
phase: 08-cuda-observability-and-documentation-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/backend/cuda/bridge/CudaBridgeExecutionStats.java
  - src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
  - src/main/java/graph/execution/PreparedExecution.java
  - src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java
  - src/test/java/BenchmarkSessionTest.java
  - src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
  - .planning/phases/08-cuda-observability-and-documentation-closure/08-01-SUMMARY.md
autonomous: true
requirements:
  - CUDADOC-01
user_setup:
  - "Do not stage local profile tuning files under profiles/platform/.../tuning/abc/*."
must_haves:
  truths:
    - "D-18: CUDA trace/report parity uses backend-neutral accelerator attributes first and keeps Metal output stable."
    - "D-19: CUDA execution stats expose path, fallback reason, input/output bytes, Java-observed native timing, and copy timing fields."
    - "D-20: Public Tensor remains logical; CUDA observability is runtime trace/report metadata only."
  artifacts:
    - path: "src/main/java/backend/cuda/bridge/CudaBridgeExecutionStats.java"
      provides: "CUDA execution diagnostics record"
      contains: "CudaBridgeExecutionStats"
    - path: "src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java"
      provides: "backend-neutral accelerator report aggregation"
      contains: "acceleratorInputBytes"
    - path: "src/test/java/BenchmarkSessionTest.java"
      provides: "portable CUDA report contract test"
      contains: "benchmarkSessionReportsCudaAcceleratorEvidenceContract"
---

<objective>
Make CUDA native-buffer execution visible in run traces and benchmark reports with the same accelerator evidence contract Metal already exposes: backend, buffer path, reason code, fallback reason, prepared-input usage, materialization count, copy timing, bytes, and storage residency.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cuda-observability-and-documentation-closure/08-CONTEXT.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-RESEARCH.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-VALIDATION.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-PATTERNS.md
@.planning/phases/07-cuda-buffer-execution-and-materialization/07-VERIFICATION.md
@src/main/java/backend/metal/bridge/MetalMpsBridgeExecutionStats.java
@src/main/java/backend/metal/exec/PreparedMetalExecutable.java
@src/main/java/graph/execution/PreparedExecution.java
@src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java
@src/test/java/BenchmarkSessionTest.java
@AGENTS.md
</context>

<source_audit>
GOAL: Covers CUDA trace/report evidence parity.
REQ CUDADOC-01: Covered by CUDA execution stats, trace attributes, and benchmark report assertions.
CONTEXT: Covers Phase 8 reportability gap and keeps CUDA support narrow.
</source_audit>

<threat_model>
T-8-01: CUDA report output could claim accelerator execution without stable backend/path/reason evidence. Mitigation: benchmark tests assert `GPU_CUDA`, `BUFFER_BINDING`, `BUFFER_BINDING_AVAILABLE`, prepared input usage, bytes, copy timing, and residency in text and JSON.
T-8-02: CUDA timing fields could be invented as native GPU timings. Mitigation: docs/comments label them Java-observed boundary timings and permit `0L` when the current shim has no sub-timer.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add portable CUDA benchmark report contract test</name>
  <files>src/test/java/BenchmarkSessionTest.java</files>
  <read_first>
    src/test/java/BenchmarkSessionTest.java
    src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java
    src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java
    src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java
  </read_first>
  <action>
    Add `benchmarkSessionReportsCudaAcceleratorEvidenceContract` to `BenchmarkSessionTest`.

    Construct a synthetic `ExecutionStepTrace` with:
    - backend string `GPU_CUDA`
    - kernel string `PreparedCudaExecutable`
    - `acceleratorBufferBackend = "GPU_CUDA"`
    - `acceleratorBufferExecutionPath = "BUFFER_BINDING"`
    - `acceleratorBufferReasonCode = "BUFFER_BINDING_AVAILABLE"`
    - `acceleratorBufferReason = "using native CUDA buffer bindings"`
    - `acceleratorBufferPreparedInputUsed = true`
    - `acceleratorInputBytes = 2048L`
    - `acceleratorOutputBytes = 1024L`
    - `acceleratorJavaToNativeCopyNs = 100000L`
    - `acceleratorNativeToJavaCopyNs = 0L`
    - `acceleratorNativeDeviceCopyNs = 25000L`
    - `storageResidency = "DEVICE_OWNED"`
    - `storageCpuCurrent = false`
    - `storageDeviceCurrent = true`

    Assert text report contains:
    - `backend=GPU_CUDA`
    - `bufferBindingSteps=1`
    - `preparedInputSteps=1`
    - `reasonCodes=[BUFFER_BINDING_AVAILABLE]`
    - `fallbackReasons=[using native CUDA buffer bindings]`
    - `bytes=2048->1024`
    - `javaToNativeMs=0.100000`
    - `nativeDeviceCopyMs=0.025000`
    - `storageResidency=DEVICE_OWNED`

    Assert JSON report contains:
    - `"GPU_CUDA"`
    - `"bufferBindingSteps": 1`
    - `"preparedInputSteps": 1`
    - `"reasonCodes": ["BUFFER_BINDING_AVAILABLE"]`
    - `"fallbackReasons": ["using native CUDA buffer bindings"]`
    - `"inputBytes": 2048`
    - `"outputBytes": 1024`
    - `"javaToNativeCopyNs": 100000`
    - `"nativeDeviceCopyNs": 25000`
    - `"storageResidency": "DEVICE_OWNED"`
  </action>
  <verify>
    <automated>./gradlew test --tests BenchmarkSessionTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "benchmarkSessionReportsCudaAcceleratorEvidenceContract|GPU_CUDA|acceleratorInputBytes|using native CUDA buffer bindings|nativeDeviceCopyMs=0\\.025000" src/test/java/BenchmarkSessionTest.java` exits 0. `./gradlew test --tests BenchmarkSessionTest` exits 0.
  </acceptance_criteria>
  <done>CUDA report parity has a portable failing-first contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add CUDA execution stats and trace attributes</name>
  <files>src/main/java/backend/cuda/bridge/CudaBridgeExecutionStats.java, src/main/java/backend/cuda/exec/PreparedCudaExecutable.java, src/main/java/graph/execution/PreparedExecution.java, src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java</files>
  <read_first>
    src/main/java/backend/metal/bridge/MetalMpsBridgeExecutionStats.java
    src/main/java/backend/metal/exec/PreparedMetalExecutable.java
    src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
    src/main/java/graph/execution/PreparedExecution.java
    src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
  </read_first>
  <action>
    Create `src/main/java/backend/cuda/bridge/CudaBridgeExecutionStats.java` as a record with fields:
    `boolean usedCpuFallback`, `String fallbackReason`, `AcceleratorBufferExecutionPath executionPath`, `int externalInputCount`, `int outputCount`, `long inputBytes`, `long outputBytes`, `long javaToNativeCopyNs`, `long nativeExecuteNs`, `long nativeDeviceCopyNs`, `long nativeToJavaCopyNs`, `long totalNs`.

    Add static factory:
    `public static CudaBridgeExecutionStats fallback(String reason, int externalInputCount, int outputCount, long inputBytes, long outputBytes)`.

    Update `PreparedCudaExecutable`:
    - add `private volatile CudaBridgeExecutionStats lastExecutionStats = CudaBridgeExecutionStats.fallback("not executed yet", 0, 0, 0L, 0L);`
    - add public accessor `public CudaBridgeExecutionStats lastExecutionStats()`
    - when bridge/context/executable is unavailable, set fallback stats before CPU fallback
    - when buffer binding executes, measure `long startNs = System.nanoTime()` before `bridge.executeBuffers(...)`, set stats with path `BUFFER_BINDING`, counts from bindings, bytes from binding logical byte lengths, nativeExecuteNs and totalNs as elapsed time, Java/native copy counters as `0L` unless measured
    - when buffer binding fails, set fallback stats with path `CPU_FALLBACK` and fallback reason `CUDA buffer binding execution failed: ...`
    - when CPU fallback is selected by decision, set fallback stats with the decision reason
    - when tensor-array bridge executes, measure elapsed time with path `TENSOR_ARRAY`, input/output counts and logical bytes from resolved tensors

    Update `PreparedExecution.buildStepMetadata(...)`:
    - if accelerator executable is `PreparedCudaExecutable`, publish `cudaBridgeAvailable`, `cudaBridgeContextAvailable`, `cudaBridgeExecutableAvailable`, `cudaSupportsBufferBindings`, `cudaExecutionPath`, `cudaFallbackReason`, `cudaExternalInputCount`, `cudaOutputCount`, `cudaInputBytes`, `cudaOutputBytes`, `cudaJavaToNativeCopyNs`, `cudaNativeExecuteNs`, `cudaNativeDeviceCopyNs`, `cudaNativeToJavaCopyNs`, `cudaBridgeTotalNs`
    - publish backend-neutral aggregate attrs `acceleratorInputBytes`, `acceleratorOutputBytes`, `acceleratorJavaToNativeCopyNs`, `acceleratorNativeToJavaCopyNs`, `acceleratorNativeDeviceCopyNs` from CUDA stats

    Extend `PreparedCudaExecutableBufferPolicyTest` with assertions that a traced CUDA buffer execution has `cudaExecutionPath = BUFFER_BINDING`, `cudaInputBytes > 0`, `cudaOutputBytes > 0`, and `acceleratorInputBytes > 0`.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "record CudaBridgeExecutionStats|lastExecutionStats\\(\\)|cudaExecutionPath|cudaFallbackReason|acceleratorInputBytes|cudaNativeExecuteNs" src/main/java/backend/cuda src/main/java/graph/execution/PreparedExecution.java src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java` exits 0. Focused prepared CUDA tests pass without CUDA hardware.
  </acceptance_criteria>
  <done>CUDA execution publishes trace metadata needed by reports and diagnostics.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Aggregate backend-neutral CUDA bytes and copy timing in reports</name>
  <files>src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java, src/test/java/BenchmarkSessionTest.java</files>
  <read_first>
    src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java
    src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java
    src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java
    src/test/java/BenchmarkSessionTest.java
  </read_first>
  <action>
    Update `AcceleratorTraceSummary.fromSteps(...)`:
    - add bytes from `acceleratorInputBytes` and `acceleratorOutputBytes`
    - add copy timing from `acceleratorJavaToNativeCopyNs`, `acceleratorNativeToJavaCopyNs`, and `acceleratorNativeDeviceCopyNs`
    - keep fallback to `metalInputBytes`, `metalOutputBytes`, `metalJavaToNativeCopyNs`, `metalNativeToJavaCopyNs`, and `metalNativeDeviceCopyNs` only when the corresponding backend-neutral attr is absent
    - keep existing reason-code and fallback-reason behavior unchanged

    Make `benchmarkSessionReportsCudaAcceleratorEvidenceContract` pass without changing existing Metal report tests.
  </action>
  <verify>
    <automated>./gradlew test --tests BenchmarkSessionTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "acceleratorInputBytes|acceleratorOutputBytes|acceleratorJavaToNativeCopyNs|acceleratorNativeDeviceCopyNs|metalInputBytes" src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java` exits 0. `BenchmarkSessionTest` passes and still contains `benchmarkSessionReportsAcceleratorEvidenceContract` and `benchmarkSessionReportsMetalMaterializationDetails`.
  </acceptance_criteria>
  <done>CUDA and Metal both flow through the accelerator report summary contract.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew test --tests BenchmarkSessionTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
</verification>

<success_criteria>
- CUDA trace attrs include path, fallback reason, bytes, copy timing, and storage residency.
- Text and JSON benchmark reports summarize `GPU_CUDA` accelerator evidence.
- Existing Metal report tests keep passing.
</success_criteria>
