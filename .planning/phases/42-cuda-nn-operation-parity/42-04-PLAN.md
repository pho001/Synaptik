---
phase: 42
plan: 42-04
type: execute
wave: 4
depends_on:
  - 42-01
  - 42-02
  - 42-03
files_modified:
  - src/main/java/tuning/benchmark/report/GpuHotPathCoverageTargets.java
  - src/test/java/GpuCoverageTriageReportTest.java
  - src/test/java/GpuHotPathCoverageTargetsTest.java
  - docs/cuda-backend.md
  - docs/gpu-lowering-coverage.md
autonomous: true
requirements:
  - CUDANN-01
  - CUDANN-02
  - CUDANN-03
requirements_addressed:
  - CUDANN-01
  - CUDANN-02
  - CUDANN-03
---

# Plan 42-04: Coverage, Docs, And Regression Closure

**Phase:** 42 CUDA NN Operation Parity
**Requirements:** CUDANN-01, CUDANN-02, CUDANN-03
**Status:** Planned
**Wave:** 4

## Objective

Close Phase 42 with report-visible CUDA NN support/blocker evidence and documentation that prevents CUDA capability gaps from being counted as support.

## Tasks

1. Update coverage target metadata for CUDA NN families:
   - `masked_sdpa_small` -> CUDANN-01;
   - `conv2d_resnet_3x3`, `max_pool2d_small`, `avg_pool2d_small` -> CUDANN-02;
   - `dense_loss_small` -> CUDANN-03.
2. Update CUDA and lowering coverage docs with final Phase 42 status for each family.
3. Add or update report tests proving visible CUDA blockers for unsupported rows and native evidence for any supported rows.
4. Write `42-01-SUMMARY.md` through `42-04-SUMMARY.md` and `42-VERIFICATION.md`.
5. Update `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` after verification passes.

## Acceptance Criteria

- Coverage reports distinguish CUDA SDPA, conv/pool, and dense loss blockers.
- Docs explicitly state whether Phase 42 implemented native CUDA execution or stable rejection for each family.
- Phase verification maps every CUDANN requirement to source/test/docs evidence.

## Verification

```bash
./gradlew test --tests backend.cuda.lowering.CudaRegionLowererTest
./gradlew test --tests backend.accelerator.lowering.GpuBackendParityReportTest --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest
./gradlew test --tests GpuCoverageTriageReportTest --tests GpuHotPathCoverageTargetsTest --tests SourceTreeHygieneTest
./gradlew classes
git diff --check
```

## Threat Model

- **T42-04:** Report gates could pass because an unsupported CUDA NN row silently fell back to CPU.
  **Mitigation:** report tests must require visible blocker text or native buffer evidence, never silent success.
