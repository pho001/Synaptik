---
phase: 001-accelerator-buffer-layout-abi
plan: "03"
type: execute
wave: 3
depends_on:
  - "001-02"
files_modified:
  - src/test/java/backend/accelerator/buffer/AcceleratorBufferLayoutClassifierTest.java
  - src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java
  - src/test/java/backend/metal/buffer/MetalBufferAllocatorTest.java
  - src/test/java/backend/metal/buffer/MetalBufferBindingTest.java
  - src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
  - docs/compute-flow.md
autonomous: true
requirements:
  - ABI-01
  - ABI-02
  - ABI-03
  - ABI-04
user_setup: []
must_haves:
  truths:
    - "Focused tests prove the shared classifier and binding metadata cover all ABI-required layout classes."
    - "Metal tests prove unsupported view/layout classes fall back conservatively with exact layout-class diagnostics."
    - "CUDA tests prove required native-buffer execution remains an explicit unavailable failure."
    - "Trace/report documentation names the new stable layout and required-unavailable reason codes."
  artifacts:
    - path: "src/test/java/backend/accelerator/buffer/AcceleratorBufferLayoutClassifierTest.java"
      provides: "Pure classifier tests for ABI-03"
      contains: "BROADCAST_ZERO_STRIDE_VIEW"
    - path: "src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java"
      provides: "Metal preflight/fallback tests for layout classes and reason details"
      contains: "layoutClass="
    - path: "src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java"
      provides: "CUDA required buffer unavailable reason coverage"
      contains: "REQUIRED_BUFFER_EXECUTION_UNAVAILABLE"
    - path: "docs/compute-flow.md"
      provides: "Reason-code documentation for trace/report consumers"
      contains: "INPUT_LAYOUT_UNSUPPORTED"
  key_links:
    - from: "src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java"
      to: "src/main/java/backend/metal/buffer/MetalAcceleratorBufferBinder.java"
      via: "asserts exact decision reason and layoutClass detail"
      pattern: "layoutClass="
    - from: "src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java"
      to: "src/main/java/backend/cuda/exec/PreparedCudaExecutable.java"
      via: "asserts required-unavailable reason code"
      pattern: "REQUIRED_BUFFER_EXECUTION_UNAVAILABLE"
    - from: "docs/compute-flow.md"
      to: "src/main/java/backend/accelerator/buffer/AcceleratorBufferReasonCode.java"
      via: "documents trace reason names"
      pattern: "INPUT_LAYOUT_UNSUPPORTED"
---

<objective>
Close Phase 1 with targeted tests and trace/report documentation for the layout ABI and fallback reason taxonomy.

Purpose: The ABI is only useful if tests prove each required layout class and fallback reason remains visible, stable, and conservative.
Output: Focused JUnit updates plus compute-flow documentation for new accelerator buffer layout metadata and reason codes.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/001-accelerator-buffer-layout-abi/001-RESEARCH.md
@.planning/phases/001-accelerator-buffer-layout-abi/001-VALIDATION.md
@.planning/phases/001-accelerator-buffer-layout-abi/001-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@AGENTS.md
@.planning/phases/001-accelerator-buffer-layout-abi/001-01-SUMMARY.md
@.planning/phases/001-accelerator-buffer-layout-abi/001-02-SUMMARY.md

<interfaces>
Expected production contracts from Plans 01 and 02:
```java
AcceleratorBufferLayout layout = AcceleratorBufferLayout.fromTensor(tensor);
AcceleratorBufferLayoutClass layoutClass = layout.layoutClass();
AcceleratorBufferReasonCode.INPUT_LAYOUT_UNSUPPORTED;
AcceleratorBufferReasonCode.OUTPUT_LAYOUT_UNSUPPORTED;
AcceleratorBufferReasonCode.REQUIRED_BUFFER_EXECUTION_UNAVAILABLE;
```

Validation command from 001-VALIDATION.md:
```bash
./gradlew test --tests backend.accelerator.buffer.AcceleratorBufferLayoutClassifierTest --tests graph.execution.ExecutionStateResidencyTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest
```
</interfaces>
</context>

<source_audit>
GOAL: Covered by tests and docs proving shared runtime device buffer metadata supports logical views and backend-neutral fallback diagnostics.
REQ ABI-01: Covered by binding tests asserting dtype, shape, strides, storage offset, element count, byte length, access mode, backend id, and native identity.
REQ ABI-02: Covered by tests/docs confirming common ABI is backend-neutral and CUDA consumes reason taxonomy without native handle leakage.
REQ ABI-03: Covered by classifier tests and Metal fallback tests for dense, zero-offset, non-zero-offset, permuted/strided, broadcast zero-stride, and unsupported layouts.
REQ ABI-04: Covered by Metal/CUDA tests and docs for success, fallback, dtype/layout unsupported, native ABI unavailable, and required unavailable reason codes.
RESEARCH: Implements Wave 0 gaps from `001-VALIDATION.md` and exact test files from `001-PATTERNS.md`.
CONTEXT: Prompt priorities honored: preserve conservative fallback, keep CPU hot path untouched, no `.planning/tmp/` or local profile artifacts.
Deferred ideas excluded: native Metal strided/view execution and native CUDA buffer implementation.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete shared ABI and binding unit coverage</name>
  <files>src/test/java/backend/accelerator/buffer/AcceleratorBufferLayoutClassifierTest.java, src/test/java/backend/metal/buffer/MetalBufferBindingTest.java, src/test/java/backend/metal/buffer/MetalBufferAllocatorTest.java</files>
  <read_first>
    src/test/java/backend/accelerator/buffer/AcceleratorBufferLayoutClassifierTest.java
    src/test/java/backend/metal/buffer/MetalBufferBindingTest.java
    src/test/java/backend/metal/buffer/MetalBufferAllocatorTest.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferLayout.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferLayoutClass.java
    src/main/java/backend/metal/buffer/MetalBufferBinding.java
    src/main/java/backend/metal/buffer/MetalBufferAllocator.java
  </read_first>
  <action>
    Ensure `AcceleratorBufferLayoutClassifierTest` covers:
    - `denseContiguousLayoutClassifiesAsDenseContiguous`
    - `zeroOffsetPaddedViewClassifiesAsZeroOffsetView`
    - `nonZeroOffsetDenseViewClassifiesAsNonZeroOffsetView`
    - `permutedLayoutClassifiesAsPermutedOrStridedView`
    - `broadcastZeroStrideClassifiesAsBroadcastZeroStrideView`
    - `unsupportedNegativeStrideClassifiesAsUnsupported`
    - `layoutAccessorsReturnDefensiveCopies`
    - byte-size assertions for `FLOAT32`, `FLOAT64`, `BFLOAT16`, `INT32`, and `BOOL`

    Update `MetalBufferBindingTest` to assert `layout().layoutClass()`, `layout().strides()`, `layout().storageOffset()`, `accessMode()`, `nativeHandleIdentity()`, defensive shape/stride copies through `layout()`, and `describe()` containing `layoutClass=`, `storageOffset=`, `strides=`, and `handleBytes=`. Do not assert or introduce shortcut accessors on `MetalBufferBinding`; layout facts must be accessed through `binding.layout().*`.

    Update `MetalBufferAllocatorTest` to build bindings with `AcceleratorBufferLayout` and assert readback rejects non-contiguous or non-zero-offset destinations before native `readBuffer(...)` is called.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.accelerator.buffer.AcceleratorBufferLayoutClassifierTest --tests backend.metal.buffer.MetalBufferBindingTest --tests backend.metal.buffer.MetalBufferAllocatorTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "zeroOffsetPaddedViewClassifiesAsZeroOffsetView|nonZeroOffsetDenseViewClassifiesAsNonZeroOffsetView|broadcastZeroStrideClassifiesAsBroadcastZeroStrideView|unsupportedNegativeStrideClassifiesAsUnsupported" src/test/java/backend/accelerator/buffer/AcceleratorBufferLayoutClassifierTest.java` finds the named tests; `rg -n "layout\\(\\)\\.layoutClass\\(\\)|layout\\(\\)\\.storageOffset\\(\\)|layout\\(\\)\\.strides\\(\\)|nativeHandleIdentity|layoutClass=|storageOffset=|strides=" src/test/java/backend/metal/buffer/MetalBufferBindingTest.java` finds binding metadata assertions; the targeted unit command passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Metal and CUDA decision regression coverage</name>
  <files>src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java, src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java</files>
  <read_first>
    src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java
    src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
    src/main/java/backend/metal/buffer/MetalAcceleratorBufferBinder.java
    src/main/java/backend/metal/exec/PreparedMetalExecutable.java
    src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferReasonCode.java
  </read_first>
  <action>
    Update Metal executable tests so conservative fallback is asserted for every unsupported layout class:
    - Zero-offset non-dense view output falls back with reason containing `layoutClass=ZERO_OFFSET_VIEW`.
    - Non-contiguous/permuted output falls back before output reservation and the decision has `OUTPUT_LAYOUT_UNSUPPORTED` with reason containing `layoutClass=PERMUTED_OR_STRIDED_VIEW`.
    - Non-zero-offset dense output falls back with reason containing `layoutClass=NON_ZERO_OFFSET_VIEW`.
    - Broadcast/zero-stride output falls back with reason containing `layoutClass=BROADCAST_ZERO_STRIDE_VIEW`.
    - Invalid classifier input used through a focused helper or fake layout falls back with reason containing `layoutClass=UNSUPPORTED`, or, if no legal runtime tensor can carry `UNSUPPORTED`, classifier-only coverage must be explicit in `AcceleratorBufferLayoutClassifierTest` and Metal tests must state that `UNSUPPORTED` is not constructible through normal tensor metadata.
    - Existing pre-attached non-dense `MetalBufferBinding` is rejected unless its layout exactly matches the runtime/request layout and is `DENSE_CONTIGUOUS`.
    - Required buffer mode converts any unsupported layout into `AcceleratorBufferExecutionPath.UNAVAILABLE` before tensor-array execution.
    - When `FakeBridge.supportsBufferBindings()` is false, optional Metal buffer mode reports `NATIVE_BUFFER_ABI_UNAVAILABLE` with reason `native Metal buffer ABI unavailable: bridge does not support buffer bindings`; required Metal buffer mode reports the same reason code and throws with that reason before tensor-array execution.

    Preserve tests that prove dense shared input/output bindings use `BUFFER_BINDING`, adjacent dense Metal executions reuse intermediate buffers without CPU materialization, and failed native buffer execution does not promote reserved outputs.

    Update CUDA policy test to expect `REQUIRED_BUFFER_EXECUTION_UNAVAILABLE` and the exact failure message `Accelerator buffer path is required for GPU_CUDA but unavailable: REQUIRED_BUFFER_EXECUTION_UNAVAILABLE: CUDA bridge does not support required buffer bindings`.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "ZERO_OFFSET_VIEW|PERMUTED_OR_STRIDED_VIEW|NON_ZERO_OFFSET_VIEW|BROADCAST_ZERO_STRIDE_VIEW|UNSUPPORTED|OUTPUT_LAYOUT_UNSUPPORTED|INPUT_LAYOUT_UNSUPPORTED|NATIVE_BUFFER_ABI_UNAVAILABLE" src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java src/test/java/backend/accelerator/buffer/AcceleratorBufferLayoutClassifierTest.java` finds exact layout/reason assertions; `rg -n "REQUIRED_BUFFER_EXECUTION_UNAVAILABLE" src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java` finds CUDA assertion; the targeted Metal/CUDA command passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update trace/report reason documentation and run phase gate</name>
  <files>docs/compute-flow.md</files>
  <read_first>
    docs/compute-flow.md
    src/main/java/backend/accelerator/buffer/AcceleratorBufferReasonCode.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferLayoutClass.java
    .planning/phases/001-accelerator-buffer-layout-abi/001-VALIDATION.md
    AGENTS.md
  </read_first>
  <action>
    Update the accelerator buffer decision/trace section in `docs/compute-flow.md` so `acceleratorBufferReasonCode` examples include `INPUT_LAYOUT_UNSUPPORTED`, `OUTPUT_LAYOUT_UNSUPPORTED`, `NATIVE_BUFFER_ABI_UNAVAILABLE`, `REQUIRED_BUFFER_EXECUTION_UNAVAILABLE`, `BUFFER_BINDING_AVAILABLE`, `TENSOR_ARRAY_SELECTED`, and `CPU_FALLBACK_SELECTED`. Add one concise sentence that layout diagnostics include `layoutClass`, `shape`, `strides`, and `storageOffset` when layout compatibility drives fallback.

    Run the phase targeted suite from `001-VALIDATION.md`, then run `./gradlew classes`. Do not run or commit benchmark/calibration profile outputs. Do not commit `.planning/tmp/`.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.accelerator.buffer.AcceleratorBufferLayoutClassifierTest --tests graph.execution.ExecutionStateResidencyTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.metal.buffer.MetalBufferAllocatorTest --tests backend.metal.buffer.MetalBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest</automated>
    <automated>./gradlew classes</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "INPUT_LAYOUT_UNSUPPORTED|OUTPUT_LAYOUT_UNSUPPORTED|NATIVE_BUFFER_ABI_UNAVAILABLE|REQUIRED_BUFFER_EXECUTION_UNAVAILABLE|layoutClass|storageOffset" docs/compute-flow.md` finds the documented trace/report wording; the full targeted phase test command passes; `./gradlew classes` passes; `git status --short` shows no staged or unstaged `.planning/tmp/` files and no newly staged `profiles/platform` tuning outputs.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests -> native-capable runtime seams | Tests exercise fake bridge/native handle paths and must prevent unsafe promotion or silent fallback. |
| Documentation -> trace/report users | Docs describe stable reason names used by humans and tooling. |
| Local verification -> repository state | Gradle/test runs can produce local artifacts that must not be committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-001-03-01 | Repudiation | `PreparedMetalExecutableBufferBindingTest` | mitigate | Assert exact `AcceleratorBufferReasonCode` names and `layoutClass=` details for layout fallback. |
| T-001-03-02 | Tampering | Output binding promotion tests | mitigate | Preserve tests proving failed native buffer execution and unsupported layouts do not promote reserved device buffers. |
| T-001-03-03 | Information Disclosure | `docs/compute-flow.md` | accept | Documentation lists diagnostic fields but not native handle values; no sensitive runtime data is introduced. |
| T-001-03-04 | Denial of Service | Verification commands | mitigate | Use targeted Gradle filters from validation strategy instead of full default `test`, which can include long benchmark/debug tests. |
</threat_model>

<verification>
Run:
```bash
./gradlew test --tests backend.accelerator.buffer.AcceleratorBufferLayoutClassifierTest --tests graph.execution.ExecutionStateResidencyTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.metal.buffer.MetalBufferAllocatorTest --tests backend.metal.buffer.MetalBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest
./gradlew classes
```
Optionally run `./gradlew metalTest` after these pass if the executor has the local Metal shim prerequisites available.
</verification>

<success_criteria>
Plan 03 is complete when all focused ABI tests pass, docs expose the stable reason-code and layout diagnostic contract, conservative fallback behavior is proven for unsupported layout classes, CUDA required-buffer mode remains explicitly unavailable, and no local tuning/profile or `.planning/tmp/` artifacts are staged.
</success_criteria>

<output>
After completion, create `.planning/phases/001-accelerator-buffer-layout-abi/001-03-SUMMARY.md`.
</output>
