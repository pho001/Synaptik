---
phase: 05-accelerator-verification-and-documentation-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/test/java/StandardWorkloadsTest.java
  - src/test/java/BenchmarkSessionTest.java
  - src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java
  - src/test/java/backend/metal/MetalBufferTraceSmokeTest.java
  - src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java
  - src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
autonomous: true
requirements:
  - OBS-02
  - OBS-03
  - OBS-04
user_setup:
  - "For full native evidence, run `./gradlew metalTest` on macOS with the Metal shim build available. JUnit assumptions may skip native-only assertions when the shim is unavailable."
must_haves:
  truths:
    - "D-04: Metal-specific meaningful values are verified through capability-gated metalTest coverage."
    - "Closure benchmark workload stresses matmul/linear, view/layout transforms, elementwise fusion, reductions, and backward/gradient publication."
    - "Adjacent accelerator buffer handoff is proven without Java array round trips when layout and capability contracts allow it."
    - "CUDA native production support remains out of scope; required CUDA buffer execution stays visibly unavailable without a native shim."
  artifacts:
    - path: "src/test/java/StandardWorkloadsTest.java"
      provides: "closure workload shape and gradient coverage assertions"
      contains: "transformerBlockClosureWorkloadCoversAcceleratorEvidenceFamilies"
    - path: "src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java"
      provides: "CPU-vs-Metal forward and forward-backward parity coverage"
      contains: "forwardBackwardLayoutAwareGraphPublishesGradientsWithCpuParity"
    - path: "src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java"
      provides: "deterministic adjacent device handoff/buffer binding unit coverage"
      contains: "adjacentDeviceOwnedInputUsesBufferBindingWithoutCpuMaterialization"
  key_links:
    - from: "src/main/java/tuning/workload/TransformerBlockHotPathWorkloadSpec.java"
      to: "src/test/java/StandardWorkloadsTest.java"
      via: "closure benchmark workload contract"
      pattern: "TransformerBlockHotPathWorkloadSpec"
    - from: "src/test/java/backend/metal/MetalBufferTraceSmokeTest.java"
      to: "src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java"
      via: "capability-gated native Metal evidence"
      pattern: "assumeNativeBufferBridge"
---

<objective>
Prove the closure workload, adjacent device-buffer handoff, and CPU-vs-Metal correctness coverage required for accelerator milestone closure.

Output: tests demonstrate that the transformer-block workload covers the required benchmark stressors, report-contract benchmark execution can run without persisting measured output, adjacent accelerator regions can reuse device buffers without Java array round trips when legal, and Metal forward/forward-backward parity remains capability-gated through `metalTest`.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-CONTEXT.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-RESEARCH.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-VALIDATION.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-PATTERNS.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-01-PLAN.md
@AGENTS.md
</context>

<source_audit>
GOAL: Covered by workload, Metal correctness, and adjacent buffer-handoff tests.
REQ OBS-02: Covered by transformer-block closure workload assertions and benchmark session report contract.
REQ OBS-03: Covered by fake-bridge deterministic adjacent device-buffer handoff tests and native Metal trace smoke tests.
REQ OBS-04: Covered by CPU-vs-Metal forward and forward-backward parity tests.
CONTEXT D-04: Covered by capability-gated `metalTest` native checks.
AGENTS: Keeps CPU oracle and backend-neutral accelerator abstractions; no public Tensor device API.
</source_audit>

<threat_model>
T-5-04: Closure workload could under-cover the architecture shift and miss gradients or layout transforms. Mitigation: tests assert the workload uses transformer block operations and forward-backward gradient validation.
T-5-05: Device buffer handoff could regress to Java array copies while still producing correct results. Mitigation: fake bridge tests assert buffer execution count and zero tensor-array execution for legal handoff.
T-5-06: Native Metal tests could become mandatory on machines without the shim. Mitigation: keep Metal-specific tests under `metalTest` with existing JUnit assumptions.
T-5-07: CUDA support could be overclaimed. Mitigation: retain CUDA required-unavailable tests and docs/plan text that native CUDA remains future work.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove transformer-block is the closure benchmark workload</name>
  <files>src/test/java/StandardWorkloadsTest.java, src/test/java/BenchmarkSessionTest.java</files>
  <read_first>
    src/main/java/tuning/workload/TransformerBlockHotPathWorkloadSpec.java
    src/main/java/tuning/workload/StandardWorkloads.java
    src/test/java/StandardWorkloadsTest.java
    src/test/java/BenchmarkSessionTest.java
    src/main/java/tuning/benchmark/BenchmarkSession.java
  </read_first>
  <action>
    Add `StandardWorkloadsTest.transformerBlockClosureWorkloadCoversAcceleratorEvidenceFamilies`.

    The test must instantiate `StandardWorkloads.transformerBlockHotPath("accelerator_closure_transformer_block")` with an `ExecutionProfile` using:
    - `DataType.FLOAT32`
    - `ExecutionMode.FORWARD_BACKWARD`
    - `OptimizerConfig.trainingDefaults()`
    - `RuntimeConfig.trainingDefaults()`
    - `StandardWorkloads.transformerHotPathDefaults()`

    Assert:
    - metadata name is `accelerator_closure_transformer_block`
    - metadata kind is `TRANSFORMER_HOT_PATH`
    - validation reference kind is `BASELINE_PROFILE`
    - validation target kind is `ROOT`
    - graph root shape length is `1`
    - source file `TransformerBlockHotPathWorkloadSpec.java` contains the operation stressors `linear`, `reshape`, `permute`, `scaledDotProductAttention`, `tanh`, `mean`, and `gradientLabels`

    Add or strengthen a `BenchmarkSessionTest` case that benchmarks this workload with a quick in-memory `BenchmarkRequest` and asserts:
    - report workload name is `accelerator_closure_transformer_block`
    - candidate success is true
    - rendered text contains `cpuMaterializationCount=`
    - JSON contains `"trace": {`

    Use `new tuning.measure.MeasurementPolicy(0, 1, 1, true, true, true, true, false)` so the test remains focused and does not become a performance benchmark.
  </action>
  <verify>
    <automated>./gradlew test --tests StandardWorkloadsTest --tests BenchmarkSessionTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "transformerBlockClosureWorkloadCoversAcceleratorEvidenceFamilies|accelerator_closure_transformer_block|scaledDotProductAttention|gradientLabels" src/test/java/StandardWorkloadsTest.java src/test/java/BenchmarkSessionTest.java` finds workload coverage. Targeted Gradle command passes.
  </acceptance_criteria>
  <done>Closure benchmark workload coverage is explicit and report-contract only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen Metal correctness and adjacent device-buffer handoff evidence</name>
  <files>src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java, src/test/java/backend/metal/MetalBufferTraceSmokeTest.java, src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java, src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java</files>
  <read_first>
    src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java
    src/test/java/backend/metal/MetalBufferTraceSmokeTest.java
    src/test/java/backend/metal/exec/PreparedMetalExecutableBufferBindingTest.java
    src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
    src/main/java/graph/execution/PreparedExecution.java
    src/main/java/backend/metal/exec/PreparedMetalExecutable.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferDecision.java
  </read_first>
  <action>
    In `MetalLayoutAwareDeviceFlowTest`, keep existing CPU parity tests and add assertions to native/capability-gated trace tests that the first Metal step attributes contain:
    - `acceleratorBufferMode`
    - `acceleratorBufferExecutionPath`
    - `acceleratorBufferReasonCode`
    - `storageResidency`
    - `storageDeviceCurrent`
    - `metalNativeDeviceCopyNs`

    In `MetalBufferTraceSmokeTest`, strengthen native smoke assertions so a buffer-binding trace proves:
    - `acceleratorBufferExecutionPath=BUFFER_BINDING`
    - `acceleratorBufferReasonCode=BUFFER_BINDING_AVAILABLE`
    - `acceleratorBufferPreparedInputUsed` is present
    - CPU materialization reason is `GRAPH_OUTPUT` or `CPU_CONSUMER`

    In `PreparedMetalExecutableBufferBindingTest`, add deterministic fake-bridge coverage named `adjacentDeviceOwnedInputUsesBufferBindingWithoutCpuMaterialization`.
    The test should:
    - create a fixture with an existing input `DeviceBufferBinding`
    - execute a legal Metal buffer path with bridge buffer support
    - assert `bridge.bufferExecutions == 1`
    - assert `bridge.tensorExecutions == 0`
    - assert `lastAcceleratorBufferDecision().path()` is `BUFFER_BINDING`
    - assert output residency is `DEVICE_OWNED`
    - assert no CPU-readable array materialization was required before graph output publication

    In `PreparedCudaExecutableBufferPolicyTest`, add or keep an assertion that CUDA `REQUIRE` buffer mode remains `REQUIRED_BUFFER_EXECUTION_UNAVAILABLE` and does not claim native CUDA buffer execution.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest</automated>
    <automated>./gradlew metalTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "adjacentDeviceOwnedInputUsesBufferBindingWithoutCpuMaterialization|acceleratorBufferExecutionPath|acceleratorBufferPreparedInputUsed|metalNativeDeviceCopyNs|REQUIRED_BUFFER_EXECUTION_UNAVAILABLE" src/test/java/backend/metal src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java` finds assertions. Targeted Java tests pass. `./gradlew metalTest` passes or native-only tests skip through documented assumptions when the local shim is unavailable.
  </acceptance_criteria>
  <done>Metal correctness and adjacent device-buffer handoff evidence is strict where capabilities allow it.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew test --tests StandardWorkloadsTest --tests BenchmarkSessionTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
`./gradlew metalTest`
</verification>

<success_criteria>
- At least one benchmark workload explicitly covers matmul/linear, view/layout transforms, elementwise chains, reductions, and backward/gradient publication.
- Adjacent legal accelerator work can pass device buffers without Java array round trips in deterministic tests.
- CPU-vs-Metal forward and forward-backward coverage remains present and native capability-gated.
- CUDA native execution is not overclaimed.
</success_criteria>
