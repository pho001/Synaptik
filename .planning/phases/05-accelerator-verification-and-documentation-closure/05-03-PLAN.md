---
phase: 05-accelerator-verification-and-documentation-closure
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - docs/architecture.md
  - docs/metal-backend.md
  - docs/calibration-autotune.md
  - docs/testing.md
  - docs/troubleshooting.md
  - .gitignore
  - build.gradle
  - src/test/java/SourceTreeHygieneTest.java
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/05-accelerator-verification-and-documentation-closure/05-03-SUMMARY.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
user_setup:
  - "Do not stage local `profiles/platform/.../tuning/abc/*` changes or `.planning/tmp/` scratch files."
must_haves:
  truths:
    - "Docs explain accelerator ABI, device-owned flow, layout/view handling, CPU materialization boundaries, tuning ownership, and fallback diagnostics."
    - "Benchmark reports are explain artifacts and report-contract evidence, not runtime profile sources of truth."
    - "Hygiene checks prevent accidental commits of root generated classes, `.planning/tmp/`, and unintentional local calibration/benchmark outputs."
    - "Final verification records targeted Java tests, `metalTest`, and artifact hygiene status."
  artifacts:
    - path: "docs/metal-backend.md"
      provides: "accelerator ABI and fallback diagnostic documentation"
      contains: "device-owned"
    - path: "docs/calibration-autotune.md"
      provides: "benchmark report and tuning ownership documentation"
      contains: "report-contract"
    - path: "src/test/java/SourceTreeHygieneTest.java"
      provides: "repository hygiene regression checks"
      contains: "planningTmpScratchIsIgnored"
    - path: ".gitignore"
      provides: "local scratch artifact ignore rule"
      contains: ".planning/tmp/"
  key_links:
    - from: "docs/testing.md"
      to: "build.gradle"
      via: "documented verification commands"
      pattern: "metalTest"
    - from: "src/test/java/SourceTreeHygieneTest.java"
      to: ".gitignore"
      via: "source-control hygiene contract"
      pattern: ".planning/tmp/"
---

<objective>
Close Phase 5 with documentation, hygiene enforcement, final verification, and planning state updates.

Output: docs explain how to inspect accelerator evidence and where CPU materialization boundaries occur; hygiene tests and ignore rules prevent scratch/profile artifacts from entering commits; final summary records verification commands and confirms no local profile or `.planning/tmp/` artifacts were staged.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-CONTEXT.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-RESEARCH.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-VALIDATION.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-PATTERNS.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-01-PLAN.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-02-PLAN.md
@docs/architecture.md
@docs/metal-backend.md
@docs/calibration-autotune.md
@docs/testing.md
@docs/troubleshooting.md
@AGENTS.md
</context>

<source_audit>
GOAL: Covered by final docs, hygiene rules, and verification evidence.
REQ DOC-01: Covered by accelerator ABI, layout/view, and Metal/CUDA implementation responsibility docs.
REQ DOC-02: Covered by tuning ownership docs and benchmark report boundary docs.
REQ DOC-03: Covered by fallback diagnostic and report interpretation docs.
REQ DOC-04: Covered by `.gitignore`, source hygiene tests, and final staging checks.
REQ OBS-01 through OBS-04: Covered by final verification commands from Plans 05-01 and 05-02.
AGENTS: Explicitly protects local benchmark/calibration artifacts and `.planning/tmp/`.
</source_audit>

<threat_model>
T-5-08: Documentation could imply benchmark reports or local profiles are runtime sources of truth. Mitigation: docs state benchmark reports are report-contract evidence and explain artifacts only.
T-5-09: Docs could overclaim CUDA native execution. Mitigation: docs state CUDA remains capability/policy gated until a native shim exists.
T-5-10: Scratch files or machine-local profile artifacts could be staged during closure. Mitigation: `.gitignore`, hygiene tests, and final `git status --short` checks explicitly reject these paths.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update accelerator observability and tuning documentation</name>
  <files>docs/architecture.md, docs/metal-backend.md, docs/calibration-autotune.md, docs/testing.md, docs/troubleshooting.md</files>
  <read_first>
    docs/architecture.md
    docs/metal-backend.md
    docs/calibration-autotune.md
    docs/testing.md
    docs/troubleshooting.md
    .planning/phases/05-accelerator-verification-and-documentation-closure/05-CONTEXT.md
    .planning/phases/05-accelerator-verification-and-documentation-closure/05-RESEARCH.md
  </read_first>
  <action>
    Update docs with exact phrases and concepts:

    In `docs/metal-backend.md`, add or update a section that includes:
    - `device-owned`
    - `acceleratorBufferExecutionPath`
    - `acceleratorBufferReasonCode`
    - `CPU materialization boundary`
    - `storageResidency`
    - `nativeDeviceCopyNs`
    - `CUDA remains capability-gated until a native shim exists`

    In `docs/calibration-autotune.md`, add or update benchmark/report ownership text that includes:
    - `report-contract`
    - `Benchmark reports are explain artifacts, not runtime sources of truth.`
    - `selected accelerator candidate`
    - `rejectedFinalists`
    - `boundaryCount`
    - `Autotune and calibration own persistence; benchmark commands remain read-only.`

    In `docs/testing.md`, add Phase 5 verification commands:
    - `./gradlew test --tests BenchmarkSessionTest --tests PreparedExecutionBuildTest`
    - `./gradlew test --tests StandardWorkloadsTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
    - `./gradlew metalTest`
    - `./gradlew test --tests SourceTreeHygieneTest`

    In `docs/troubleshooting.md`, add diagnostic guidance for:
    - missing `acceleratorBufferReasonCode`
    - unexpected `cpuMaterializationCount`
    - nonzero `nativeToJavaCopyNs`
    - benchmark reports missing `backendSelectionCost`

    In `docs/architecture.md`, add a short cross-reference to Metal/backend docs for accelerator ABI, runtime residency, and materialization boundaries.
  </action>
  <verify>
    <automated>rg -n "device-owned|acceleratorBufferExecutionPath|acceleratorBufferReasonCode|CPU materialization boundary|storageResidency|nativeDeviceCopyNs|CUDA remains capability-gated" docs/metal-backend.md</automated>
    <automated>rg -n "report-contract|Benchmark reports are explain artifacts|selected accelerator candidate|rejectedFinalists|boundaryCount|benchmark commands remain read-only" docs/calibration-autotune.md</automated>
    <automated>rg -n "BenchmarkSessionTest|PreparedExecutionBuildTest|StandardWorkloadsTest|PreparedMetalExecutableBufferBindingTest|metalTest|SourceTreeHygieneTest" docs/testing.md</automated>
  </verify>
  <acceptance_criteria>
    All `rg` commands in the verify block exit 0. Docs do not claim CUDA native execution is production-ready. Docs do not say benchmark reports update profiles or best-profile records.
  </acceptance_criteria>
  <done>Docs explain accelerator evidence, report interpretation, tuning ownership, and fallback diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add local artifact hygiene checks and ignore rules</name>
  <files>.gitignore, build.gradle, src/test/java/SourceTreeHygieneTest.java</files>
  <read_first>
    .gitignore
    build.gradle
    src/test/java/SourceTreeHygieneTest.java
    docs/testing.md
    AGENTS.md
  </read_first>
  <action>
    Add `.planning/tmp/` to `.gitignore` under local agent/workspace metadata.

    Extend `SourceTreeHygieneTest` with tests:
    - `planningTmpScratchIsIgnored`
    - `rootGeneratedClassArtifactsAreIgnored`
    - `trackedLocalTuningArtifactsStayExplicit`

    Required assertions:
    - `.gitignore` contains `.planning/tmp/`
    - `.gitignore` contains `/*.class`
    - `.gitignore` contains `**/*.class`
    - `git ls-files .planning/tmp` returns no tracked files when run through `ProcessBuilder`
    - tracked `profiles/platform/.../tuning/abc/*` files, if present, are treated as explicit canonical fixtures and the test message tells developers not to stage local changes accidentally

    If adding a Gradle task is useful, add `verifyLocalArtifactHygiene` that depends on `test --tests SourceTreeHygieneTest` behavior only if it can be done without breaking existing Gradle task configuration. Otherwise keep hygiene in JUnit and `verifySourceTreeClean`.
  </action>
  <verify>
    <automated>./gradlew test --tests SourceTreeHygieneTest</automated>
    <automated>./gradlew verifySourceTreeClean</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "\\.planning/tmp/|/\\*\\.class|\\*\\*/\\*\\.class" .gitignore` finds ignore rules. `rg -n "planningTmpScratchIsIgnored|rootGeneratedClassArtifactsAreIgnored|trackedLocalTuningArtifactsStayExplicit" src/test/java/SourceTreeHygieneTest.java` finds tests. `./gradlew test --tests SourceTreeHygieneTest` passes. `./gradlew verifySourceTreeClean` passes.
  </acceptance_criteria>
  <done>Local planning scratch and generated artifact hygiene is enforced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run final closure verification and update planning state</name>
  <files>.planning/phases/05-accelerator-verification-and-documentation-closure/05-03-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    .planning/phases/05-accelerator-verification-and-documentation-closure/05-01-PLAN.md
    .planning/phases/05-accelerator-verification-and-documentation-closure/05-02-PLAN.md
    .planning/phases/05-accelerator-verification-and-documentation-closure/05-03-PLAN.md
    .planning/phases/05-accelerator-verification-and-documentation-closure/05-VALIDATION.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </read_first>
  <action>
    Run final targeted verification:
    - `./gradlew classes`
    - `./gradlew test --tests BenchmarkSessionTest --tests PreparedExecutionBuildTest --tests StandardWorkloadsTest --tests SourceTreeHygieneTest --tests graph.execution.ExecutionStateResidencyTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
    - `./gradlew metalTest`

    Create `05-03-SUMMARY.md` with:
    - changed files grouped by trace/report, workload/Metal tests, docs, and hygiene,
    - exact verification commands and pass/fail/skip results,
    - explicit line showing `git status --short`,
    - explicit note that no `profiles/platform/.../tuning/abc/*` files and no `.planning/tmp/` files were staged.

    Update `.planning/ROADMAP.md` Phase 5 plan list to include:
    - `05-01-PLAN.md - Lock trace and benchmark report evidence contract.`
    - `05-02-PLAN.md - Prove closure workload, Metal correctness, and buffer handoff.`
    - `05-03-PLAN.md - Close docs, hygiene, and final verification.`

    Update `.planning/STATE.md` through `gsd-sdk query state.planned-phase --phase 05 --plans 3` or the available state handler. If the handler would corrupt progress, leave `STATE.md` unchanged and record that limitation in `05-03-SUMMARY.md`.
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests BenchmarkSessionTest --tests PreparedExecutionBuildTest --tests StandardWorkloadsTest --tests SourceTreeHygieneTest --tests graph.execution.ExecutionStateResidencyTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest</automated>
    <automated>./gradlew metalTest</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/05-accelerator-verification-and-documentation-closure/05-03-SUMMARY.md` exists and contains `./gradlew classes`, `BenchmarkSessionTest`, `StandardWorkloadsTest`, `SourceTreeHygieneTest`, `metalTest`, `profiles/platform`, and `.planning/tmp`. `rg -n "05-01-PLAN.md|05-02-PLAN.md|05-03-PLAN.md" .planning/ROADMAP.md` exits 0. `git status --short` has no staged `profiles/platform/.../tuning/abc/*` or staged `.planning/tmp/` entries.
  </acceptance_criteria>
  <done>Phase 5 has final verification evidence, roadmap linkage, and clean artifact staging.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew classes`
`./gradlew test --tests BenchmarkSessionTest --tests PreparedExecutionBuildTest --tests StandardWorkloadsTest --tests SourceTreeHygieneTest --tests graph.execution.ExecutionStateResidencyTest --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
`./gradlew metalTest`
</verification>

<success_criteria>
- Documentation covers accelerator ABI, device-owned execution, layout/view handling, materialization boundaries, tuning ownership, fallback diagnostics, and benchmark report interpretation.
- `.planning/tmp/` and generated class artifacts are ignored or tested through hygiene checks.
- Final verification records portable and Metal-specific test results.
- No local profile tuning outputs or `.planning/tmp/` artifacts are staged.
</success_criteria>
