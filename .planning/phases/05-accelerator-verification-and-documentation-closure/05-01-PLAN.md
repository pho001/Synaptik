---
phase: 05-accelerator-verification-and-documentation-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java
  - src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java
  - src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java
  - src/test/java/BenchmarkSessionTest.java
  - src/test/java/PreparedExecutionBuildTest.java
autonomous: true
requirements:
  - OBS-01
  - OBS-02
user_setup: []
must_haves:
  truths:
    - "D-01: Phase 5 tests assert core accelerator evidence fields, not just renderer presence."
    - "D-02: Benchmark evidence is report-contract only; no measured local benchmark output is committed."
    - "D-03: Evidence includes ROADMAP minimum plus planner context: selected accelerator candidate, top rejected finalist, cost summary, and region boundary counts."
    - "Java-side report contract tests remain portable and do not require Metal native runtime."
  artifacts:
    - path: "src/test/java/BenchmarkSessionTest.java"
      provides: "portable benchmark report evidence contract tests"
      contains: "renderersExposeAcceleratorEvidenceContract"
    - path: "src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java"
      provides: "backend-neutral accelerator evidence aggregation"
      contains: "fallbackReasons"
    - path: "src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java"
      provides: "machine-readable accelerator evidence fields"
      contains: "\"fallbackReasons\""
  key_links:
    - from: "src/main/java/graph/execution/PreparedExecution.java"
      to: "src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java"
      via: "acceleratorBuffer and Metal step attributes"
      pattern: "acceleratorBufferReasonCode"
    - from: "src/main/java/tuning/benchmark/report/BenchmarkReport.java"
      to: "src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java"
      via: "report contract rendering"
      pattern: "appendAcceleratorSummary"
---

<objective>
Lock the portable trace and benchmark report evidence contract for accelerator execution.

Output: benchmark report tests assert accelerator path, buffer mode, fallback reason, materialization count/reason, copy-time fields, storage residency, selected backend candidate, top rejected finalist, cost summary, and region boundary counts without committing measured machine-local output.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-CONTEXT.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-RESEARCH.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-VALIDATION.md
@.planning/phases/05-accelerator-verification-and-documentation-closure/05-PATTERNS.md
@AGENTS.md
</context>

<source_audit>
GOAL: Covered by making accelerator evidence testable in trace and benchmark report contracts.
REQ OBS-01: Covered by report and trace assertions for accelerator backend, buffer path, fallback, materialization, copy time, and residency.
REQ OBS-02: Covered partially by ensuring benchmark reports expose the evidence fields that closure workloads will use in Plan 05-02.
CONTEXT D-01/D-02/D-03: Covered directly in must_haves and tasks.
AGENTS: Keeps fallback visible and avoids committing local benchmark/calibration artifacts.
</source_audit>

<threat_model>
T-5-01: Accelerator execution could silently fall back or materialize through CPU while reports still look like GPU execution. Mitigation: renderer tests assert fallback reason, buffer path, materialization count/reason, and residency fields.
T-5-02: Planner cost context could disappear from benchmark reports and hide why a candidate was selected. Mitigation: report tests assert selected candidate, rejected finalists, cost summary, and boundary counts.
T-5-03: Benchmark evidence could become machine-local committed output. Mitigation: tests construct in-memory report fixtures and do not write measured reports to the repository.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend accelerator report summary with fallback reason contract fields</name>
  <files>src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java, src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java, src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java, src/test/java/BenchmarkSessionTest.java</files>
  <read_first>
    src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java
    src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java
    src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java
    src/main/java/graph/execution/PreparedExecution.java
    src/test/java/BenchmarkSessionTest.java
  </read_first>
  <action>
    Update `AcceleratorTraceSummary.BackendSummary` to include a stable fallback/reason summary:
    - `java.util.List<String> fallbackReasons`
    - `java.util.List<String> reasonCodes`

    In `AcceleratorTraceSummary.fromSteps(...)`, collect unique nonblank values from:
    - `acceleratorBufferReasonCode` into `reasonCodes`
    - `acceleratorBufferReason` into `fallbackReasons`
    - `metalFallbackReason` into `fallbackReasons` when nonblank

    Keep the existing counters and copy fields unchanged:
    - `steps`
    - `bufferBindingSteps`
    - `tensorArraySteps`
    - `cpuFallbackSteps`
    - `unavailableSteps`
    - `preparedInputSteps`
    - `inputBytes`
    - `outputBytes`
    - `javaToNativeCopyNs`
    - `nativeToJavaCopyNs`
    - `nativeDeviceCopyNs`

    Update text renderer accelerator summary lines to include:
    - `reasonCodes=<list>`
    - `fallbackReasons=<list>`

    Update JSON renderer accelerator summary objects to include:
    - `"reasonCodes": [...]`
    - `"fallbackReasons": [...]`

    Add `BenchmarkSessionTest.renderersExposeAcceleratorEvidenceContract` using an in-memory `ExecutionTrace` with one `GPU_METAL` step whose attributes include:
    - `acceleratorBufferBackend=GPU_METAL`
    - `acceleratorBufferMode=AUTO`
    - `acceleratorBufferExecutionPath=BUFFER_BINDING`
    - `acceleratorBufferReasonCode=BUFFER_BINDING_AVAILABLE`
    - `acceleratorBufferReason=using native buffer bindings`
    - `acceleratorBufferPreparedInputUsed=true`
    - `metalJavaToNativeCopyNs=100000`
    - `metalNativeToJavaCopyNs=0`
    - `metalNativeDeviceCopyNs=25000`
    - `storageResidency=DEVICE_OWNED`
    - `storageCpuCurrent=false`
    - `storageDeviceCurrent=true`

    The test must assert text and JSON contain `accelerator`, `bufferBindingSteps`, `preparedInputSteps`, `reasonCodes`, `fallbackReasons`, `javaToNativeCopyNs`, `nativeDeviceCopyNs`, `storageResidency`, and `DEVICE_OWNED`.
  </action>
  <verify>
    <automated>./gradlew test --tests BenchmarkSessionTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "fallbackReasons|reasonCodes" src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java` finds the new fields. `rg -n "renderersExposeAcceleratorEvidenceContract|acceleratorBufferMode|BUFFER_BINDING_AVAILABLE|DEVICE_OWNED|nativeDeviceCopyNs" src/test/java/BenchmarkSessionTest.java` finds the contract test. Targeted Gradle command passes.
  </acceptance_criteria>
  <done>Benchmark report renderers expose stable accelerator fallback/reason evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert planner context and materialization evidence in report fixtures</name>
  <files>src/test/java/BenchmarkSessionTest.java, src/test/java/PreparedExecutionBuildTest.java</files>
  <read_first>
    src/test/java/BenchmarkSessionTest.java
    src/test/java/PreparedExecutionBuildTest.java
    src/main/java/graph/execution/trace/BackendSelectionDecisionTrace.java
    src/main/java/graph/execution/trace/PartitionDecisionTrace.java
    src/main/java/tuning/benchmark/report/TextBenchmarkReportRenderer.java
    src/main/java/tuning/benchmark/report/JsonBenchmarkReportRenderer.java
  </read_first>
  <action>
    Strengthen `BenchmarkSessionTest.renderersExposeBackendSelectionCostDiagnostics` so it asserts both text and JSON include:
    - selected backend candidate: `selectedBackend=GPU_METAL` and `"selectedBackend": "GPU_METAL"`
    - top rejected finalist: `rejectedFinalists:` and `"rejectedFinalists": [`
    - cost summary fields: `finalScore`, `estimatedTransferBytes`, `estimatedComputeWork`, `preset`
    - region boundary count: `boundaryCount=2` and `"boundaryCount": 2`

    Strengthen or add a report fixture test so CPU materialization evidence asserts:
    - text contains `cpuMaterializationCount=1`
    - text contains `reason=GRAPH_OUTPUT`
    - text contains `residency=DEVICE_OWNED`
    - JSON contains `"cpuMaterializationCount": 1`
    - JSON contains `"reason": "GRAPH_OUTPUT"`
    - JSON contains `"sourceResidency": "DEVICE_OWNED"`
    - JSON contains `"durationNs"` or `"durationMs"`

    Add `PreparedExecutionBuildTest.prepareTraceSelectedAcceleratorDecisionCarriesPlannerEvidence` if no equivalent exists. It must compile a small FLOAT32 matmul+relu graph with `OffloadConfig.acceleratorGreedy()`, prepare with inference defaults, find the selected `GPU_METAL` backend selection decision, and assert:
    - `decision.costSummary() != null`
    - `decision.costSummary().boundaryCount() >= 0`
    - `decision.costSummary().estimatedTransferBytes() >= 0`
    - `decision.costSummary().estimatedComputeWork() >= 0`
    - `decision.costSummary().preset()` is not blank
  </action>
  <verify>
    <automated>./gradlew test --tests BenchmarkSessionTest --tests PreparedExecutionBuildTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "boundaryCount=2|rejectedFinalists|cpuMaterializationCount=1|sourceResidency|durationNs|prepareTraceSelectedAcceleratorDecisionCarriesPlannerEvidence" src/test/java/BenchmarkSessionTest.java src/test/java/PreparedExecutionBuildTest.java` finds assertions. Targeted Gradle command passes.
  </acceptance_criteria>
  <done>Report and prepare-trace tests enforce planner context and materialization evidence.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew test --tests BenchmarkSessionTest --tests PreparedExecutionBuildTest`
</verification>

<success_criteria>
- Benchmark reports expose accelerator path, buffer mode, fallback reason, materialization count/reason, copy timing, storage residency, selected candidate, rejected finalists, cost summary, and boundary count.
- Tests use in-memory report fixtures and do not write measured benchmark output to source-controlled paths.
- Portable Java-side tests pass without requiring native Metal.
</success_criteria>
