# Plan 38-04: Final Training Parity Closure

**Phase:** 38 Metal Training Backward Coverage
**Requirements:** METALTRAIN-01, METALTRAIN-02, METALTRAIN-03
**Status:** Planned
**Wave:** 4

## Objective

Close Phase 38 with representative parity, report, docs, and verification evidence for supported and unsupported Metal training/backward paths.

## Tasks

1. Run focused CPU-vs-Metal parity tests for every supported backward family admitted in 38-02.
2. Run trace/report gates for training hot-path targets added in 38-03.
3. Update docs:
   - `docs/gpu-lowering-coverage.md`
   - `docs/metal-backend.md`
   - benchmark/coverage troubleshooting where needed
4. Write `38-VERIFICATION.md` mapping `METALTRAIN-01..03` to source files, tests, traces, report gates, and residual scope.
5. Keep local tuning/profile artifacts unstaged unless intentionally promoting canonical evidence.

## Acceptance Criteria

- Supported backward claims have parity and trace evidence.
- Unsupported backward variants remain visible with stable reason codes.
- Phase 38 requirements are complete only after report gates and docs distinguish gradient publication from hidden CPU exits.

## Verification

```bash
./gradlew test --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest
./gradlew test --tests GpuCoverageSummaryTest --tests GpuCoverageRegressionGateTest --tests GpuHotPathCoverageTargetsTest --tests StandardWorkloadsTest
./gradlew test --tests SourceTreeHygieneTest
./gradlew classes
./gradlew metalTest
git diff --check
```

## Threat Model

- **Threat:** Phase closure overstates "training on Metal" while leaving key backward families on CPU.
  **Mitigation:** Verification must list supported, unsupported, and residual backward families explicitly.
