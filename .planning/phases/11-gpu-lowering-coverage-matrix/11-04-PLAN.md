---
phase: 11-gpu-lowering-coverage-matrix
plan: "04"
type: execute
wave: 4
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - src/test/java/CompiledGraphTraceTest.java
  - src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java
  - src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java
  - docs/gpu-lowering-coverage.md
  - docs/development.md
  - .planning/phases/11-gpu-lowering-coverage-matrix/11-04-SUMMARY.md
autonomous: true
requirements: [GPULOWER-01, GPULOWER-02, GPULOWER-03]
must_haves:
  truths:
    - "D-13: Focused portable JUnit gates are the primary verification path."
    - "D-14: Native Metal/CUDA tests remain capability-gated and local benchmark/profile artifacts are not committed."
    - "D-15: Final closure proves both selected and rejected candidates."
---

<objective>
Close Phase 11 with trace-visible selected/rejected lowering coverage, docs, verification commands, and hygiene evidence proving the coverage matrix is auditable before Phase 12 fusion work begins.
</objective>

<source_audit>
GOAL: Provides Phase 11 closure evidence across matrix, lowering, backend selection, trace, docs, and hygiene.
REQ GPULOWER-01: Docs and tests prove matrix coverage remains checked in.
REQ GPULOWER-02: E2E/trace tests cover supported high-value GPU paths.
REQ GPULOWER-03: Unsupported operation, dtype, layout, and capability gaps reject cleanly without CPU/Metal/CUDA fallback safeguards regressing.
D-13: Covered by final focused JUnit gates for coverage matrix, legality adapters, lowering, backend selection, and trace/reason-code behavior.
D-14: Covered by optional capability-gated Metal/CUDA native checks and explicit local profile artifact hygiene.
D-15: Covered by closure tests for both selected supported candidates and visibly rejected unsupported families.
</source_audit>

<threat_model>
T-11-07: Trace evidence could omit lowering/fallback reasons and make unsupported GPU exits invisible. Mitigation: add trace assertions and docs that name selected and rejected coverage reason fields.
T-11-08: Final verification could accidentally stage local tuning/profile artifacts. Mitigation: final summary must record `git status --short` evidence and explicitly exclude `profiles/platform/.../tuning/abc/*`.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add trace tests for lowering coverage reason visibility</name>
  <files>src/test/java/CompiledGraphTraceTest.java</files>
  <read_first>
    src/test/java/CompiledGraphTraceTest.java
    src/main/java/graph/execution/trace/BackendSelectionDecisionTrace.java
    src/main/java/graph/execution/trace/PartitionDecisionTrace.java
    src/main/java/graph/execution/PreparedExecution.java
    docs/gpu-lowering-coverage.md
  </read_first>
  <action>
    Extend `CompiledGraphTraceTest` with tests named:
    - `gpuLoweringCoverageSelectionTraceNamesSupportedLogSoftmaxRegion`
    - `gpuLoweringCoverageRejectionTraceNamesUnsupportedReduction`

    The selected test must:
    - build a FLOAT32 graph with `linear(...).logSoftmax(1)`
    - assign either Metal or CUDA backend to the linear and logSoftmax nodes
    - prepare and execute traced forward execution when local capability allows, or inspect prepare trace when runtime execution is capability-skipped
    - assert a selected backend decision contains reason `selected`
    - assert selected node operation names include `LINEAR` or `MATMUL` and `LOG_SOFTMAX`

    The rejected test must:
    - build a graph containing `sum` or `layerNorm`
    - assign accelerator backend intent
    - assert no hidden selected accelerator execution for the unsupported node
    - assert trace, compile trace, or planner diagnostic contains `UNSUPPORTED_OPERATION`
  </action>
  <verify>
    <automated>./gradlew test --tests CompiledGraphTraceTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "gpuLoweringCoverageSelectionTraceNamesSupportedLogSoftmaxRegion|gpuLoweringCoverageRejectionTraceNamesUnsupportedReduction|LOG_SOFTMAX|UNSUPPORTED_OPERATION" src/test/java/CompiledGraphTraceTest.java` exits 0.
  </acceptance_criteria>
  <done>Trace tests prove supported and rejected Phase 11 coverage is visible.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend layout-heavy device-flow tests with supported lowering path</name>
  <files>src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java, src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java</files>
  <read_first>
    src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java
    src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java
    src/main/java/graph/execution/DeviceLayoutViewPropagator.java
    src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java
  </read_first>
  <action>
    Add tests named:
    - Metal: `layoutViewThenLogSoftmaxStaysDeviceOwnedUntilOutputBoundary`
    - CUDA: `layoutViewThenLogSoftmaxStaysDeviceOwnedUntilOutputBoundary`

    Each test must build a supported path such as:
    - `input.linear(weight).reshape(...).permute(...).contiguous().logSoftmax(lastDimension)`

    Assertions:
    - CPU parity for graph output
    - no `CpuMaterializationReason.CPU_CONSUMER` before graph output for supported local capability paths
    - trace attributes or prepare trace include `LOG_SOFTMAX`, `GPU_LAYOUT_VIEW_BINDING_AVAILABLE` or `GPU_LAYOUT_DENSE_MATERIALIZATION_AVAILABLE`, and selected GPU backend
    - CUDA still accepts only layouts made legal by metadata-only view propagation or dense materialization
  </action>
  <verify>
    <automated>./gradlew test --tests backend.metal.MetalLayoutAwareDeviceFlowTest --tests backend.cuda.exec.CudaLayoutTransformDeviceFlowTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "layoutViewThenLogSoftmaxStaysDeviceOwnedUntilOutputBoundary|LOG_SOFTMAX|GPU_LAYOUT_VIEW_BINDING_AVAILABLE|GPU_LAYOUT_DENSE_MATERIALIZATION_AVAILABLE" src/test/java/backend/metal/MetalLayoutAwareDeviceFlowTest.java src/test/java/backend/cuda/exec/CudaLayoutTransformDeviceFlowTest.java` exits 0.
  </acceptance_criteria>
  <done>Phase 10 layout residency and Phase 11 lowering coverage are proven together.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document developer extension workflow and verification commands</name>
  <files>docs/gpu-lowering-coverage.md, docs/development.md</files>
  <read_first>
    docs/gpu-lowering-coverage.md
    docs/development.md
    docs/adding-tensor-operation.md
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
    src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java
  </read_first>
  <action>
    Add a section named `Adding A GPU-Lowerable Operation Family` to `docs/gpu-lowering-coverage.md`.

    The section must list these steps:
    - add or update the semantic `Operation.OpType`
    - add a `GpuLoweringCoverageMatrix` entry for Metal and CUDA
    - add or update `AcceleratorDagNodeType` only when a native ABI op is truly required
    - prefer decomposition through existing DAG primitives where safe
    - update `AcceleratorSubgraphLowerer`
    - update Metal/CUDA planner tests
    - keep backend-specific native capability checks backend-owned
    - add selected and rejected candidate tests

    Update `docs/development.md` with a subsection named `GPU lowering coverage checks` containing these commands:
    - `./gradlew classes`
    - `./gradlew test --tests backend.accelerator.lowering.* --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest`
    - `./gradlew test --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest`
    - `./gradlew metalTest`
    - `./gradlew buildCudaGraphShim cudaTest`
  </action>
  <verify>
    <automated>rg -n "Adding A GPU-Lowerable Operation Family|GPU lowering coverage checks|backend.accelerator.lowering|buildCudaGraphShim cudaTest|prefer decomposition through existing DAG primitives" docs/gpu-lowering-coverage.md docs/development.md</automated>
  </verify>
  <acceptance_criteria>
    `docs/gpu-lowering-coverage.md` contains `Adding A GPU-Lowerable Operation Family` and `docs/development.md` contains `GPU lowering coverage checks`.
  </acceptance_criteria>
  <done>Developers have a documented extension workflow without duplicating backend-neutral logic.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run final focused Phase 11 verification and record summary</name>
  <files>.planning/phases/11-gpu-lowering-coverage-matrix/11-04-SUMMARY.md</files>
  <read_first>
    .planning/phases/11-gpu-lowering-coverage-matrix/11-VALIDATION.md
    .planning/phases/11-gpu-lowering-coverage-matrix/11-01-PLAN.md
    .planning/phases/11-gpu-lowering-coverage-matrix/11-02-PLAN.md
    .planning/phases/11-gpu-lowering-coverage-matrix/11-03-PLAN.md
    docs/development.md
  </read_first>
  <action>
    Run and record these commands in `11-04-SUMMARY.md`:
    - `./gradlew classes`
    - `./gradlew test --tests backend.accelerator.lowering.* --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest`
    - `./gradlew test --tests backend.metal.MetalLayoutAwareDeviceFlowTest --tests backend.cuda.exec.CudaLayoutTransformDeviceFlowTest`
    - optional native: `./gradlew metalTest`
    - optional native: `./gradlew buildCudaGraphShim cudaTest`
    - `git status --short`

    The summary must include:
    - `Phase 11 final verification`
    - whether native Metal/CUDA checks passed or were capability-skipped
    - selected supported coverage: `LOG_SOFTMAX lowered as SOFTMAX followed by LOG`
    - rejected unsupported families: reductions, normalization, and loss-adjacent
    - confirmation that local profile tuning files under `profiles/platform/.../tuning/abc/*` remained unstaged
  </action>
  <verify>
    <automated>test -f .planning/phases/11-gpu-lowering-coverage-matrix/11-04-SUMMARY.md</automated>
    <automated>rg -n "Phase 11 final verification|LOG_SOFTMAX lowered as SOFTMAX followed by LOG|reductions|normalization|loss-adjacent|profiles/platform" .planning/phases/11-gpu-lowering-coverage-matrix/11-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/11-gpu-lowering-coverage-matrix/11-04-SUMMARY.md` contains `Phase 11 final verification`, `LOG_SOFTMAX lowered as SOFTMAX followed by LOG`, `metalTest`, `buildCudaGraphShim cudaTest`, and `profiles/platform`.
  </acceptance_criteria>
  <done>Phase 11 has final verification evidence ready for UAT, security, validation, and verify-work.</done>
</task>

</tasks>

<verification>
Run:
- `./gradlew classes`
- `./gradlew test --tests backend.accelerator.lowering.* --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest`
- `./gradlew test --tests backend.metal.MetalLayoutAwareDeviceFlowTest --tests backend.cuda.exec.CudaLayoutTransformDeviceFlowTest`
- Optional native: `./gradlew metalTest`
- Optional native: `./gradlew buildCudaGraphShim cudaTest`
</verification>

<success_criteria>
- Trace/tests show one supported softmax-ish GPU path selected.
- Trace/tests show unsupported reductions, normalization, and loss-adjacent families rejected visibly.
- Documentation explains how to add new GPU-lowerable operation families.
- Final summary records focused verification and local artifact hygiene.
</success_criteria>
