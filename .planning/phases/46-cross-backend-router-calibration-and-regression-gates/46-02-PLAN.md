---
phase: 46-cross-backend-router-calibration-and-regression-gates
plan: 46-02-representative-workload-gates
status: completed
requirements:
  - BACKENDROUTE-02
---

# 46-02 Representative Workload Gates

## Objective

Add gate policies that fail representative workload evidence on hidden CPU exits, tensor-array replay, unsupported route overclaims, and native-copy/write-status regressions.

## Tasks

1. Add a gate evaluator over the Phase 46 router evidence model.
2. Support requirements for selected routes, rejected reasons, copy strategy, output write status, native buffer execution, and maximum hidden exits.
3. Add tests for transformer/MLP/conv/index/loss/training-style synthetic traces.
4. Preserve existing `GpuCoverageRegressionGate` behavior.

## Verification

- `./gradlew test --tests CrossBackendRouterEvidenceTest --tests GpuCoverageRegressionGateTest`

## Completion Criteria

- A benchmark suite can fail fast when a hot path silently leaves the intended backend or overclaims support.
