---
phase: 43
plan: 43-04
type: execute
wave: 4
depends_on:
  - 43-01
  - 43-02
  - 43-03
files_modified:
  - docs/cuda-backend.md
  - docs/gpu-lowering-coverage.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CUDATRAIN-01
  - CUDATRAIN-02
  - CUDATRAIN-03
requirements_addressed:
  - CUDATRAIN-01
  - CUDATRAIN-02
  - CUDATRAIN-03
---

# Plan 43-04: Final Parity, Docs, And Validation Closure

**Phase:** 43 CUDA Training And Index Semantics
**Requirements:** CUDATRAIN-01, CUDATRAIN-02, CUDATRAIN-03
**Status:** Planned
**Wave:** 4

## Objective

Close Phase 43 with reviewable CUDA training/index evidence, docs, and verification artifacts that prevent unsupported CUDA backward/index paths from being counted as native support.

## Tasks

1. Update CUDA and GPU lowering docs with final Phase 43 status:
   - CUDA backward truth scope,
   - CUDA scatter/index-gradient semantic rejection,
   - CUDA training hot-path blocker interpretation,
   - native execution evidence still required before support promotion.
2. Add `43-01-SUMMARY.md` through `43-04-SUMMARY.md` and `43-VERIFICATION.md`.
3. Update `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`.
4. Run the focused Phase 43 regression gate.
5. Confirm local tuning/profile artifacts remain unstaged.

## Acceptance Criteria

- Every `CUDATRAIN` requirement maps to source, test, docs, and verification evidence.
- Docs distinguish CUDA support, matrix support, capability-missing rows, and unsupported semantic rows.
- No local benchmark/profile artifacts are committed as canonical evidence.

## Verification

```bash
./gradlew test --tests backend.cuda.lowering.CudaRegionLowererTest --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest --tests GpuHotPathCoverageTargetsTest --tests GpuCoverageRegressionGateTest --tests GpuCoverageSummaryTest --tests SourceTreeHygieneTest
./gradlew classes
git diff --check
```

## Threat Model

- **T43-04:** Documentation or planning artifacts imply CUDA training support that implementation does not prove.
  **Mitigation:** Verification must state support versus visible rejection explicitly and keep native support claims evidence-gated.

