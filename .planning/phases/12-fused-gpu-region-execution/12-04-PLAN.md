---
phase: 12-fused-gpu-region-execution
plan: "04"
type: execute
wave: 4
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - src/main/java/backend/accelerator/lowering/GpuCompoundPatternDetector.java
  - src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
  - src/main/java/backend/metal/lowering/MetalPartitionSupport.java
  - src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java
  - src/test/java/backend/accelerator/lowering/GpuCompoundPatternDetectorTest.java
  - src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
  - src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
  - src/test/java/CompiledGraphTraceTest.java
  - src/test/java/PreparedExecutionBuildTest.java
  - docs/gpu-lowering-coverage.md
  - docs/graph-optimizer.md
  - docs/compute-flow.md
  - docs/development.md
  - .planning/phases/12-fused-gpu-region-execution/12-04-SUMMARY.md
autonomous: true
requirements: [GPUFUSE-01, GPUFUSE-02, GPUFUSE-03, GPUFUSE-04]
must_haves:
  truths:
    - "D-06: Unsupported compound candidates reject with stable reasons tied to the pattern summary."
    - "D-07: `Operation.OpType.FUSED` remains CPU-only for Phase 12."
    - "D-08: GPU path encountering `Operation.OpType.FUSED` rejects explicitly with stable unsupported reason."
    - "D-09: CPU fused paths remain protected by existing tests and metadata contracts."
    - "D-11: Reduction-adjacent candidates are third priority and may be narrow support or explicit stable rejection."
    - "D-14: Backend-specific coverage must remain visible and must not claim equal backend coverage when CUDA is narrower."
    - "D-18: Unsupported or out-of-scope compound candidates may fallback only with stable reason codes and visible trace/report evidence."
---

<objective>
Close Phase 12 by making reduction-adjacent candidates and CPU `FUSED` handling explicit, documenting the GPU compound lowering model, and recording focused verification without committing local tuning artifacts.
</objective>

<source_audit>
GOAL: Completes Phase 12 with explicit support/reject status for reduction-adjacent candidates, docs, trace evidence, CPU fused regression coverage, and source hygiene.
REQ GPUFUSE-01: Final closure keeps linear+bias+activation tests in the focused gate.
REQ GPUFUSE-02: Final closure keeps elementwise-chain tests in the focused gate.
REQ GPUFUSE-03: Docs and regression tests prove GPU compound DAG execution remains independent of CPU fused ASM/vector internals.
REQ GPUFUSE-04: Reduction-adjacent candidates are either supported with parity tests or explicitly rejected with stable coverage matrix entries and reasons.
D-06 through D-18: Covered by final matrix/docs/trace checks and summary evidence.
</source_audit>

<threat_model>
T-12-09: Reduction-adjacent candidates may stay ambiguous and later appear as hidden fallback. Mitigation: matrix, planner diagnostics, and trace tests must name exact stable reasons.
T-12-10: CPU fused ASM/vector paths may regress while GPU compound work passes. Mitigation: run focused CPU fused metadata/trace tests and assert accelerator packages do not import `backend.cpu.fused`.
T-12-11: Documentation may imply broad CUDA parity or public GPU tensor residency. Mitigation: docs must state CUDA minimum subset/capability gating and public `Tensor` remains logical.
T-12-12: Local profile artifacts may be accidentally staged. Mitigation: final summary records `git status --short` and confirms `profiles/platform/.../tuning/abc/*` are unstaged.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reduction-adjacent and CPU FUSED rejection tests</name>
  <files>src/test/java/backend/accelerator/lowering/GpuCompoundPatternDetectorTest.java, src/test/java/backend/metal/lowering/MetalRegionLowererTest.java, src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java, src/test/java/CompiledGraphTraceTest.java</files>
  <read_first>
    src/test/java/backend/accelerator/lowering/GpuCompoundPatternDetectorTest.java
    src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
    src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
    src/test/java/CompiledGraphTraceTest.java
    src/main/java/backend/accelerator/lowering/GpuCompoundPatternDetector.java
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
    src/main/java/backend/metal/lowering/MetalPartitionSupport.java
    src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java
  </read_first>
  <action>
    Add tests named:
    - `reductionAdjacentLayerNormRejectsWithStableCompoundReason`
    - `metalGpuFusedOpTypeRejectsWithStableCompoundReason`
    - `cudaGpuFusedOpTypeRejectsWithStableCompoundReason`
    - `gpuCompoundRejectionTraceNamesReductionAdjacentReason`

    The tests must assert:
    - `LAYER_NORM` and `RMS_NORM` remain `REDUCTION_ADJACENT` unless a narrow implementation is added in this plan
    - unsupported reduction-adjacent candidates use a stable reason such as `COMPOUND_PATTERN_UNSUPPORTED` or existing `DEFERRED_FUSED_REGION`
    - `Operation.OpType.FUSED` never returns a supported GPU row
    - Metal/CUDA planner diagnostics contain the stable reason string
    - trace or planner diagnostic output contains `REDUCTION_ADJACENT`
  </action>
  <verify>
    <automated>./gradlew test --tests backend.accelerator.lowering.GpuCompoundPatternDetectorTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests CompiledGraphTraceTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "reductionAdjacentLayerNormRejectsWithStableCompoundReason|metalGpuFusedOpTypeRejectsWithStableCompoundReason|cudaGpuFusedOpTypeRejectsWithStableCompoundReason|gpuCompoundRejectionTraceNamesReductionAdjacentReason|REDUCTION_ADJACENT|CPU_FUSED" src/test/java/backend/accelerator/lowering/GpuCompoundPatternDetectorTest.java src/test/java/backend/metal/lowering/MetalRegionLowererTest.java src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java src/test/java/CompiledGraphTraceTest.java` exits 0.
  </acceptance_criteria>
  <done>Reduction-adjacent and CPU FUSED rejection behavior is specified by tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement explicit reduction-adjacent support or stable rejection closure</name>
  <files>src/main/java/backend/accelerator/lowering/GpuCompoundPatternDetector.java, src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java, src/main/java/backend/metal/lowering/MetalPartitionSupport.java, src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java</files>
  <read_first>
    src/main/java/backend/accelerator/lowering/GpuCompoundPatternDetector.java
    src/main/java/backend/accelerator/lowering/GpuLoweringCoverageMatrix.java
    src/main/java/backend/metal/lowering/MetalPartitionSupport.java
    src/main/java/backend/cuda/lowering/CudaGpuRegionLegalityAdapter.java
    src/main/java/operations/Operation.java
    src/test/java/backend/accelerator/lowering/GpuCompoundPatternDetectorTest.java
  </read_first>
  <action>
    Choose the conservative default unless a safe implementation already exists after Plans 12-01 through 12-03:
    - keep `SUM`, `MEAN`, `REDUCE_MIN`, and `REDUCE_MAX` as explicit fallback rows
    - keep `LAYER_NORM` and `RMS_NORM` as reduction-adjacent compound candidates that reject with stable reason `COMPOUND_PATTERN_UNSUPPORTED` or `DEFERRED_FUSED_REGION`
    - include `REDUCTION_ADJACENT` in the detector summary detail and planner diagnostic reason
    - ensure Metal and CUDA diagnostics do not differ except where backend-specific capability details are required

    If implementing a narrow supported reduction-adjacent subset instead, it must include:
    - one exact supported graph shape
    - CPU parity test
    - Metal and CUDA capability gates
    - trace attributes proving the full region stayed on GPU
    - docs matrix update naming the subset and unsupported reasons for the rest
  </action>
  <verify>
    <automated>./gradlew test --tests backend.accelerator.lowering.GpuCompoundPatternDetectorTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests CompiledGraphTraceTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "REDUCTION_ADJACENT|COMPOUND_PATTERN_UNSUPPORTED|DEFERRED_FUSED_REGION|LAYER_NORM|RMS_NORM" src/main/java/backend/accelerator/lowering src/main/java/backend/metal/lowering src/main/java/backend/cuda/lowering docs/gpu-lowering-coverage.md` exits 0.
  </acceptance_criteria>
  <done>Reduction-adjacent candidates are no longer ambiguous for Phase 12.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document GPU compound lowering and verification workflow</name>
  <files>docs/gpu-lowering-coverage.md, docs/graph-optimizer.md, docs/compute-flow.md, docs/development.md</files>
  <read_first>
    docs/gpu-lowering-coverage.md
    docs/graph-optimizer.md
    docs/compute-flow.md
    docs/development.md
    docs/mechanisms.md
    src/main/java/backend/accelerator/lowering/GpuCompoundRegionSummary.java
    src/main/java/graph/execution/PreparedExecution.java
  </read_first>
  <action>
    Update docs with these exact concepts:
    - `GPU compound region lowering`
    - `LINEAR_BIAS_ACTIVATION`
    - `ELEMENTWISE_CHAIN`
    - `REDUCTION_ADJACENT`
    - `Operation.OpType.FUSED remains CPU-only`
    - `public Tensor remains logical`
    - `device residency stays in ExecutionState and DeviceBufferBinding`
    - `Metal and CUDA coverage is backend-specific`

    In `docs/development.md`, add a subsection named `GPU compound region checks` with commands:
    - `./gradlew classes`
    - `./gradlew test --tests backend.accelerator.lowering.* --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest`
    - `./gradlew test --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest`
    - `./gradlew test --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
    - `./gradlew metalTest`
    - `./gradlew buildCudaGraphShim cudaTest`
  </action>
  <verify>
    <automated>rg -n "GPU compound region lowering|LINEAR_BIAS_ACTIVATION|ELEMENTWISE_CHAIN|REDUCTION_ADJACENT|Operation.OpType.FUSED remains CPU-only|public Tensor remains logical|device residency stays in ExecutionState|GPU compound region checks" docs/gpu-lowering-coverage.md docs/graph-optimizer.md docs/compute-flow.md docs/development.md</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "GPU compound region lowering|GPU compound region checks|Operation.OpType.FUSED remains CPU-only" docs/gpu-lowering-coverage.md docs/graph-optimizer.md docs/compute-flow.md docs/development.md` exits 0.
  </acceptance_criteria>
  <done>Developers can understand and verify GPU compound lowering without confusing it with CPU fused ASM.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run focused Phase 12 verification and record summary</name>
  <files>.planning/phases/12-fused-gpu-region-execution/12-04-SUMMARY.md</files>
  <read_first>
    .planning/phases/12-fused-gpu-region-execution/12-01-PLAN.md
    .planning/phases/12-fused-gpu-region-execution/12-02-PLAN.md
    .planning/phases/12-fused-gpu-region-execution/12-03-PLAN.md
    .planning/phases/12-fused-gpu-region-execution/12-VALIDATION.md
    docs/development.md
  </read_first>
  <action>
    Run and record these commands in `12-04-SUMMARY.md`:
    - `./gradlew classes`
    - `./gradlew test --tests backend.accelerator.lowering.* --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest`
    - `./gradlew test --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest`
    - `./gradlew test --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
    - optional native: `./gradlew metalTest`
    - optional native: `./gradlew buildCudaGraphShim cudaTest`
    - `rg -n "import backend.cpu.fused" src/main/java/backend/accelerator src/main/java/backend/metal src/main/java/backend/cuda`
    - `git status --short`

    The summary must contain:
    - `Phase 12 final verification`
    - `LINEAR_BIAS_ACTIVATION`
    - `ELEMENTWISE_CHAIN`
    - `REDUCTION_ADJACENT`
    - `Operation.OpType.FUSED remains CPU-only`
    - native Metal/CUDA status: passed, unavailable, or capability-skipped
    - confirmation that local `profiles/platform/.../tuning/abc/*` files remained unstaged
  </action>
  <verify>
    <automated>test -f .planning/phases/12-fused-gpu-region-execution/12-04-SUMMARY.md</automated>
    <automated>rg -n "Phase 12 final verification|LINEAR_BIAS_ACTIVATION|ELEMENTWISE_CHAIN|REDUCTION_ADJACENT|Operation.OpType.FUSED remains CPU-only|profiles/platform" .planning/phases/12-fused-gpu-region-execution/12-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/12-fused-gpu-region-execution/12-04-SUMMARY.md` contains the required final verification strings and source hygiene note.
  </acceptance_criteria>
  <done>Phase 12 is ready for security, validation, and verify-work.</done>
</task>

</tasks>

<verification>
Run:
- `./gradlew classes`
- `./gradlew test --tests backend.accelerator.lowering.* --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest`
- `./gradlew test --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest`
- `./gradlew test --tests backend.metal.exec.PreparedMetalExecutableBufferBindingTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
- Optional native: `./gradlew metalTest`
- Optional native: `./gradlew buildCudaGraphShim cudaTest`
</verification>

<success_criteria>
- Reduction-adjacent candidates are implemented with tests or rejected with stable reason codes and matrix/docs rows.
- CPU `Operation.OpType.FUSED` is explicitly CPU-only for GPU paths.
- CPU fused execution tests and metadata contracts remain covered.
- Docs explain GPU compound lowering, trace attributes, and backend-specific Metal/CUDA coverage.
- Final summary records verification and local profile artifact hygiene.
</success_criteria>

