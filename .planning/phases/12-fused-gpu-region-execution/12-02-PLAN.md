---
phase: 12-fused-gpu-region-execution
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java
  - src/main/java/backend/metal/lowering/MetalPartitionPlan.java
  - src/main/java/backend/cuda/lowering/CudaGpuPartitionPlan.java
  - src/main/java/backend/metal/lowering/MetalRegionLowerer.java
  - src/main/java/backend/cuda/lowering/CudaRegionLowerer.java
  - src/test/java/backend/accelerator/lowering/AcceleratorSubgraphLowererTest.java
  - src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
  - src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
  - src/test/java/PreparedExecutionBuildTest.java
autonomous: true
requirements: [GPUFUSE-01, GPUFUSE-03]
must_haves:
  truths:
    - "D-01: Extend existing lowering pipeline and backend region lowerers rather than adding a new CPU-style fusion system."
    - "D-02: Recognize a safe compound pattern, then lower to accelerator DAG primitives and backend-specific Metal/CUDA execution primitives."
    - "D-04: Pattern summary for this plan must expose `LINEAR_BIAS_ACTIVATION`."
    - "D-05: Summary must not bypass legality, dtype, layout, or capability checks."
    - "D-10: Implement the `linear + bias + activation` target for `GPUFUSE-01`."
    - "D-13: Metal may be broader, but CUDA must provide a stable minimum subset."
    - "D-15: CUDA must prove at least one minimal useful compound pattern or capability-gate it with stable fallback behavior."
    - "D-16: For supported fused target patterns, region shortening is failure."
    - "D-17: CPU materialization or tensor-array fallback between operations of a supported fused pattern is failure."
---

<objective>
Make Metal and CUDA recognize, lower, prepare, and test one supported `LINEAR_BIAS_ACTIVATION` compound GPU region without shortening the selected region or relying on CPU fused internals.
</objective>

<source_audit>
GOAL: Delivers the first explicit compound GPU target pattern.
REQ GPUFUSE-01: Metal and CUDA execute or required-mode validate at least one linear+bias+activation fused GPU region without Java array round trips between fused operations.
REQ GPUFUSE-03: Uses accelerator DAG and backend plans, not CPU fused ASM/vector internals.
D-10, D-13, D-15, D-16, D-17: Covered by exact target graph tests, backend-specific summaries, and required-mode fallback guards.
</source_audit>

<threat_model>
T-12-04: Backend selection may select only `LINEAR` or `MATMUL` and drop the activation from the GPU region. Mitigation: tests assert selected decisions and partition plans contain all compute, bias, and activation node ids.
T-12-05: AUTO mode may hide CPU fallback while parity still passes. Mitigation: add a REQUIRE buffer-mode test that throws before CPU fallback when native execution cannot satisfy the supported compound path.
T-12-06: CUDA support may be overclaimed. Mitigation: CUDA tests either prove the minimal subset through prepared DAG metadata or assert a capability-gated stable rejection before execution.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing-first linear+bias+activation lowering tests for Metal and CUDA</name>
  <files>src/test/java/backend/accelerator/lowering/AcceleratorSubgraphLowererTest.java, src/test/java/backend/metal/lowering/MetalRegionLowererTest.java, src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java</files>
  <read_first>
    src/test/java/backend/accelerator/lowering/AcceleratorSubgraphLowererTest.java
    src/test/java/backend/metal/lowering/MetalRegionLowererTest.java
    src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java
    src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java
    src/main/java/backend/accelerator/lowering/AcceleratorMatMulSpec.java
    src/main/java/backend/metal/lowering/MetalPartitionPlan.java
    src/main/java/backend/cuda/lowering/CudaGpuPartitionPlan.java
  </read_first>
  <action>
    Add tests named:
    - `linearBiasReluProducesLinearBiasActivationSummary`
    - `metalLowersLinearBiasReluAsLinearBiasActivationCompoundRegion`
    - `cudaLowersLinearBiasReluAsLinearBiasActivationCompoundRegion`

    Test graph:
    - FLOAT32 input shape `[2, 3]`
    - FLOAT32 weight shape `[3, 4]`
    - FLOAT32 bias shape `[4]`
    - output graph `input.linear(weight, bias).relu()`

    Assertions:
    - summary pattern equals `LINEAR_BIAS_ACTIVATION`
    - summary supported is `true`
    - `dagSpec.nodes()` contains `LINEAR` and `RELU` or equivalent existing node types
    - `AcceleratorMatMulSpec.biasInputNodeId() >= 0`
    - `AcceleratorMatMulSpec.postOps()` contains `RELU`
    - Metal and CUDA partition/lowering plans include node ids for `LINEAR` and `RELU`
    - no assertion imports any `backend.cpu.fused` class
  </action>
  <verify>
    <automated>./gradlew test --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "linearBiasReluProducesLinearBiasActivationSummary|metalLowersLinearBiasReluAsLinearBiasActivationCompoundRegion|cudaLowersLinearBiasReluAsLinearBiasActivationCompoundRegion|LINEAR_BIAS_ACTIVATION" src/test/java/backend/accelerator/lowering/AcceleratorSubgraphLowererTest.java src/test/java/backend/metal/lowering/MetalRegionLowererTest.java src/test/java/backend/cuda/lowering/CudaRegionLowererTest.java` exits 0.
  </acceptance_criteria>
  <done>Tests define the minimal linear+bias+activation contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire compound summary through accelerator lowering and backend partition plans</name>
  <files>src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java, src/main/java/backend/metal/lowering/MetalPartitionPlan.java, src/main/java/backend/cuda/lowering/CudaGpuPartitionPlan.java, src/main/java/backend/metal/lowering/MetalRegionLowerer.java, src/main/java/backend/cuda/lowering/CudaRegionLowerer.java</files>
  <read_first>
    src/main/java/backend/accelerator/lowering/AcceleratorSubgraphLowerer.java
    src/main/java/backend/accelerator/lowering/GpuCompoundPatternDetector.java
    src/main/java/backend/accelerator/lowering/GpuCompoundLoweringArtifact.java
    src/main/java/backend/metal/lowering/MetalPartitionPlan.java
    src/main/java/backend/cuda/lowering/CudaGpuPartitionPlan.java
    src/main/java/backend/lowering/LoweredExecutionUnit.java
    src/main/java/backend/metal/lowering/MetalRegionLowerer.java
    src/main/java/backend/cuda/lowering/CudaRegionLowerer.java
  </read_first>
  <action>
    Update `AcceleratorSubgraphLowerer.tryLower(...)` so after `dagSpec`, `matMulSpec`, and `estimatedWork` are computed, it calls:
    `GpuCompoundPatternDetector.detect(backend, subgraph, context, dagSpec, matMulSpec)`
    or equivalent backend-aware detector entry point.

    Ensure Metal and CUDA partition plan creation has access to the resulting summary:
    - `MetalPartitionPlan` may continue reading `lowering().compoundSummary()`
    - `CudaGpuPartitionPlan` must expose `compoundSummary()` even though it currently stores only `dagSpec`

    Update `MetalRegionLowerer` and `CudaRegionLowerer` so the first `LoweredExecutionUnit` carries `new GpuCompoundLoweringArtifact(summary)` when the selected plan has a non-`NONE` summary.

    Preserve current behavior:
    - graph regions still use `METAL_GRAPH_REGION` or `CUDA_GRAPH_REGION` unless they are one fused elementwise execution unit
    - existing constructor call sites for `AcceleratorSubgraphLoweringResult` continue to compile
    - no accelerator package imports `backend.cpu.fused`
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "compoundSummary\\(|GpuCompoundPatternDetector|GpuCompoundLoweringArtifact|LINEAR_BIAS_ACTIVATION" src/main/java/backend/accelerator/lowering src/main/java/backend/metal/lowering src/main/java/backend/cuda/lowering` exits 0.
  </acceptance_criteria>
  <done>Backend partition/lowering artifacts carry the supported linear+bias+activation summary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add prepared execution parity and required-mode no-hidden-fallback checks</name>
  <files>src/test/java/PreparedExecutionBuildTest.java</files>
  <read_first>
    src/test/java/PreparedExecutionBuildTest.java
    src/main/java/backend/metal/exec/PreparedMetalExecutable.java
    src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
    src/main/java/config/runtime/AcceleratorBufferBindingMode.java
    src/main/java/config/runtime/RuntimeConfig.java
  </read_first>
  <action>
    Add tests named:
    - `gpuMetalLinearBiasReluCompilesAsOneCompoundRegion`
    - `gpuCudaLinearBiasReluCompilesAsOneCompoundRegion`
    - `gpuLinearBiasReluRequiredBufferModeFailsBeforeHiddenCpuFallbackWhenUnavailable`

    The Metal and CUDA compile tests must:
    - build a CPU baseline for `input.linear(weight, bias).relu()`
    - build the same graph with backend intent on the linear and relu nodes
    - prepare with `OptimizerConfig.inferenceDefaults()`
    - assert exactly one forward step has backend `GPU_METAL` or `GPU_CUDA`
    - assert the accelerator executable summary pattern is `LINEAR_BIAS_ACTIVATION`
    - assert the selected plan node ids contain both `LINEAR` and `RELU`
    - execute forward when local backend can execute, otherwise rely on prepared metadata parity/fallback checks already present in the executable
    - assert output parity with CPU baseline when execution completes

    The required-mode test must configure accelerator buffer binding mode `REQUIRE` for the selected backend and assert unsupported native execution throws an `IllegalStateException` containing `Accelerator buffer path is required` before CPU fallback can hide the supported compound path.
  </action>
  <verify>
    <automated>./gradlew test --tests PreparedExecutionBuildTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "gpuMetalLinearBiasReluCompilesAsOneCompoundRegion|gpuCudaLinearBiasReluCompilesAsOneCompoundRegion|gpuLinearBiasReluRequiredBufferModeFailsBeforeHiddenCpuFallbackWhenUnavailable|LINEAR_BIAS_ACTIVATION|Accelerator buffer path is required" src/test/java/PreparedExecutionBuildTest.java` exits 0.
  </acceptance_criteria>
  <done>Linear+bias+activation has prepared execution evidence and hidden fallback is blocked in required mode.</done>
</task>

</tasks>

<verification>
Run:
- `./gradlew classes`
- `./gradlew test --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest`
- `./gradlew test --tests PreparedExecutionBuildTest`
</verification>

<success_criteria>
- Metal and CUDA expose `LINEAR_BIAS_ACTIVATION` summaries for the target graph.
- Selected backend plans include compute, bias, and activation nodes.
- Required-mode tests prevent hidden CPU fallback from counting as successful supported fusion.
- No accelerator code depends on CPU fused ASM/vector packages.
</success_criteria>

