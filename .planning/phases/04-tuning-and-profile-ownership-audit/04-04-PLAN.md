---
phase: 04-tuning-and-profile-ownership-audit
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - src/main/java/synaptik/app/TuningCli.java
  - src/main/java/tuning/benchmark/BenchmarkRequest.java
  - src/main/java/tuning/ARCHITECTURE.md
  - src/main/java/tuning/PERSISTENCE.md
  - docs/architecture.md
  - src/test/java/synaptik/app/TuningCliParsingTest.java
  - src/test/java/TuningStoreTest.java
  - src/test/java/BenchmarkSessionTest.java
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TUNE-01
  - TUNE-02
  - TUNE-03
  - TUNE-04
user_setup: []
must_haves:
  truths:
    - "Benchmark commands are read-only for profile, best-profile, history, and calibration artifacts."
    - "Autotune and calibration are the only CLI flows that intentionally persist profile results."
    - "Docs distinguish graph autotune, platform calibration, profile IO, cost model, and benchmark/report artifact ownership."
    - "No Phase 4 task stages local profiles/platform/.../tuning/abc/* or .planning/tmp artifacts."
  artifacts:
    - path: "src/main/java/synaptik/app/TuningCli.java"
      provides: "testable CLI command persistence role"
      contains: "writesProfileArtifacts"
    - path: "src/main/java/tuning/PERSISTENCE.md"
      provides: "documented benchmark read-only profile boundary"
      contains: "Benchmark commands are profile-read-only"
  key_links:
    - from: "src/main/java/synaptik/app/TuningCli.java"
      to: "src/test/java/synaptik/app/TuningCliParsingTest.java"
      via: "command kind persistence assertions"
      pattern: "benchmarkCommandsAreProfileReadOnly"
---

<objective>
Lock the benchmark/profile persistence boundary and close Phase 4 documentation and planning state.

Output: benchmark CLI command kinds are explicitly profile-read-only, docs explain ownership and profile-derived cost behavior, and final targeted verification proves all TUNE requirements are covered without staging local profile artifacts.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-tuning-and-profile-ownership-audit/04-CONTEXT.md
@.planning/phases/04-tuning-and-profile-ownership-audit/04-RESEARCH.md
@.planning/phases/04-tuning-and-profile-ownership-audit/04-VALIDATION.md
@.planning/phases/04-tuning-and-profile-ownership-audit/04-01-PLAN.md
@.planning/phases/04-tuning-and-profile-ownership-audit/04-02-PLAN.md
@.planning/phases/04-tuning-and-profile-ownership-audit/04-03-PLAN.md
@src/main/java/tuning/ARCHITECTURE.md
@src/main/java/tuning/PERSISTENCE.md
@src/main/java/tuning/README.md
@AGENTS.md
</context>

<source_audit>
GOAL: Covered by final ownership docs and benchmark write-boundary enforcement.
REQ TUNE-01/TUNE-02: Covered by docs and prior ownership/cost plans.
REQ TUNE-03: Covered by docs referencing strict platform profile IO behavior.
REQ TUNE-04: Covered by CLI command persistence role and tests.
AGENTS: Explicitly protects local benchmark/calibration artifacts from accidental commits.
</source_audit>

<threat_model>
T-4-10: Benchmark commands could start writing best-profile/history artifacts and make performance experiments mutate project state. Mitigation: command-kind persistence role test asserts benchmark commands are profile-read-only.
T-4-11: Docs could imply benchmark reports are runtime sources of truth. Mitigation: docs state reports are explain artifacts only.
T-4-12: Final planning commit could include local profile or `.planning/tmp/` artifacts. Mitigation: final task includes `git status --short` check and stages only intended files.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add testable CLI profile persistence roles</name>
  <files>src/main/java/synaptik/app/TuningCli.java, src/main/java/tuning/benchmark/BenchmarkRequest.java, src/test/java/synaptik/app/TuningCliParsingTest.java, src/test/java/TuningStoreTest.java, src/test/java/BenchmarkSessionTest.java</files>
  <read_first>
    src/main/java/synaptik/app/TuningCli.java
    src/main/java/tuning/benchmark/BenchmarkRequest.java
    src/main/java/tuning/store/PersistencePolicy.java
    src/test/java/synaptik/app/TuningCliParsingTest.java
    src/test/java/TuningStoreTest.java
    src/test/java/BenchmarkSessionTest.java
  </read_first>
  <action>
    Add method to `TuningCli.CommandKind`:
    - `boolean writesProfileArtifacts()`

    Required return values:
    - `HELP` -> `false`
    - `FULL` -> `true`
    - `CALIBRATION` -> `true`
    - `AUTOTUNE` -> `true`
    - `BENCHMARK_WINNER` -> `false`
    - `BENCHMARK_GRAPH_SPACE` -> `false`

    Add tests in `TuningCliParsingTest`:
    - `benchmarkCommandsAreProfileReadOnly`
    - `autotuneAndCalibrationCommandsWriteProfileArtifacts`

    Keep `BenchmarkRequest` documentation explicit: benchmark validates/measures supplied entries and never creates, searches, or persists profiles, best records, history entries, calibration artifacts, or benchmark reports. Do not make `BenchmarkSession` call any store.

    Add a regression assertion in `BenchmarkSessionTest` or `TuningStoreTest` that `BenchmarkSession.create(request).run()` produces a report while no `PersistencePolicy` or profile store is involved.
  </action>
  <verify>
    <automated>./gradlew test --tests synaptik.app.TuningCliParsingTest --tests TuningStoreTest --tests BenchmarkSessionTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "writesProfileArtifacts" src/main/java/synaptik/app/TuningCli.java src/test/java/synaptik/app/TuningCliParsingTest.java` finds method and tests. `rg -n "benchmarkCommandsAreProfileReadOnly|autotuneAndCalibrationCommandsWriteProfileArtifacts" src/test/java/synaptik/app/TuningCliParsingTest.java` finds tests. `rg -n "new JsonFileBestProfileStore|new JsonFileTuningHistoryStore|PersistencePolicy" src/main/java/tuning/benchmark src/main/java/tuning/benchmark/report` returns no profile persistence use. Targeted Gradle command passes.
  </acceptance_criteria>
  <done>Benchmark command kinds are explicitly profile-read-only and covered by tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ownership documentation and roadmap plan list</name>
  <files>src/main/java/tuning/ARCHITECTURE.md, src/main/java/tuning/PERSISTENCE.md, docs/architecture.md, .planning/ROADMAP.md</files>
  <read_first>
    src/main/java/tuning/ARCHITECTURE.md
    src/main/java/tuning/PERSISTENCE.md
    src/main/java/tuning/README.md
    docs/architecture.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Update `src/main/java/tuning/ARCHITECTURE.md` with a section titled `Tuning knob ownership matrix` containing exact phrases:
    - `Graph/workload-owned`
    - `Platform/dtype-owned`
    - `Obsolete`
    - `ACCELERATOR_BUFFER_MODE`
    - `METAL_SELECTION`
    - `MetalTransferModel`
    - `Profile-derived accelerator costs enter through RuntimeConfig, not profile file reads.`

    Update `src/main/java/tuning/PERSISTENCE.md` with a section titled `Benchmark commands are profile-read-only` containing exact phrases:
    - `benchmark-winner`
    - `benchmark-graph-space`
    - `Autotune and calibration are the only profile-writing CLI flows.`
    - `Benchmark reports are explain artifacts, not runtime sources of truth.`

    Update `docs/architecture.md` with a short tuning ownership subsection pointing to `src/main/java/tuning/ARCHITECTURE.md` and `src/main/java/tuning/PERSISTENCE.md`.

    Update `.planning/ROADMAP.md` Phase 4 plan list to include:
    - `04-01-PLAN.md - Establish test-enforced knob ownership matrix.`
    - `04-02-PLAN.md - Harden platform profile IO and schema validation.`
    - `04-03-PLAN.md - Wire audited runtime/profile-derived accelerator costs.`
    - `04-04-PLAN.md - Lock benchmark read-only profile boundary and docs.`
  </action>
  <verify>
    <automated>rg -n "Tuning knob ownership matrix|Graph/workload-owned|Platform/dtype-owned|ACCELERATOR_BUFFER_MODE|METAL_SELECTION|MetalTransferModel|Profile-derived accelerator costs enter through RuntimeConfig" src/main/java/tuning/ARCHITECTURE.md</automated>
    <automated>rg -n "Benchmark commands are profile-read-only|benchmark-winner|benchmark-graph-space|Autotune and calibration are the only profile-writing CLI flows|Benchmark reports are explain artifacts" src/main/java/tuning/PERSISTENCE.md</automated>
    <automated>rg -n "04-01-PLAN.md|04-02-PLAN.md|04-03-PLAN.md|04-04-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    All three `rg` commands in verify exit 0. Documentation does not claim benchmark reports are executable profiles. `.planning/ROADMAP.md` lists all four Phase 4 plan files.
  </acceptance_criteria>
  <done>Phase 4 ownership and persistence boundaries are documented and roadmap-linked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run final targeted verification and preserve local artifact hygiene</name>
  <files>.planning/phases/04-tuning-and-profile-ownership-audit/04-04-SUMMARY.md, .planning/STATE.md</files>
  <read_first>
    .planning/phases/04-tuning-and-profile-ownership-audit/04-VALIDATION.md
    .planning/phases/04-tuning-and-profile-ownership-audit/04-01-PLAN.md
    .planning/phases/04-tuning-and-profile-ownership-audit/04-02-PLAN.md
    .planning/phases/04-tuning-and-profile-ownership-audit/04-03-PLAN.md
    .planning/phases/04-tuning-and-profile-ownership-audit/04-04-PLAN.md
    .planning/STATE.md
  </read_first>
  <action>
    Run final targeted verification:
    - `./gradlew classes`
    - `./gradlew test --tests CalibrationCandidateOwnershipTest --tests GraphAutotuneCandidateSpaceTest --tests TuningKnobOwnershipTest --tests PlatformCalibrationSessionTest --tests ExecutionProfileIoTest --tests graph.optimizer.partition.cost.AcceleratorPartitionScoreModelTest --tests PreparedExecutionBuildTest --tests synaptik.app.TuningCliParsingTest --tests TuningStoreTest --tests BenchmarkSessionTest`

    Create `04-04-SUMMARY.md` with:
    - changed files grouped by ownership, profile IO, cost model, benchmark/docs,
    - exact verification commands and pass/fail results,
    - explicit note from `git status --short` that no `profiles/platform/.../tuning/abc/*` or `.planning/tmp/` files were staged.

    Update `.planning/STATE.md` through the GSD state handler if available; if the handler would corrupt progress, leave STATE unchanged and record the limitation in the summary.
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests CalibrationCandidateOwnershipTest --tests GraphAutotuneCandidateSpaceTest --tests TuningKnobOwnershipTest --tests PlatformCalibrationSessionTest --tests ExecutionProfileIoTest --tests graph.optimizer.partition.cost.AcceleratorPartitionScoreModelTest --tests PreparedExecutionBuildTest --tests synaptik.app.TuningCliParsingTest --tests TuningStoreTest --tests BenchmarkSessionTest</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/04-tuning-and-profile-ownership-audit/04-04-SUMMARY.md` exists and contains `./gradlew classes`, `TuningKnobOwnershipTest`, `PlatformCalibrationSessionTest`, `PreparedExecutionBuildTest`, and `profiles/platform`. `git status --short` has no staged `profiles/platform/.../tuning/abc/*` or staged `.planning/tmp/` entries.
  </acceptance_criteria>
  <done>Phase 4 has final targeted verification evidence and local generated artifacts are not staged.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew classes`
`./gradlew test --tests CalibrationCandidateOwnershipTest --tests GraphAutotuneCandidateSpaceTest --tests TuningKnobOwnershipTest --tests PlatformCalibrationSessionTest --tests ExecutionProfileIoTest --tests graph.optimizer.partition.cost.AcceleratorPartitionScoreModelTest --tests PreparedExecutionBuildTest --tests synaptik.app.TuningCliParsingTest --tests TuningStoreTest --tests BenchmarkSessionTest`
</verification>

<success_criteria>
- Benchmark CLI commands are profile-read-only.
- Autotune/calibration/full are the only profile-writing CLI flows.
- Tuning docs explain ownership matrix, strict IO, runtime-derived cost inputs, and report artifact meaning.
- Roadmap lists all four Phase 4 plans.
- No local profile or `.planning/tmp/` artifacts are staged.
</success_criteria>
