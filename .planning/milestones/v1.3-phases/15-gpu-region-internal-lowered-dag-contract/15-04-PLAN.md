---
phase: 15-gpu-region-internal-lowered-dag-contract
plan: "04"
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - docs/gpu-lowered-region-manifest.md
  - docs/gpu-lowering-coverage.md
  - docs/development.md
  - docs/compute-flow.md
  - .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-VALIDATION.md
  - .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-04-SUMMARY.md
autonomous: true
requirements: [GPUDAG-01, GPUDAG-02, GPUDAG-03]
must_haves:
  truths:
    - "GPUDAG-01/GPUDAG-02/GPUDAG-03: Final docs and validation prove selected GPU regions are described as internal lowered DAG manifests with stable rejection metadata."
    - "D-09/D-10/D-11/D-12: Documentation names structured prepare trace, compact text summary, run trace region-id reference, and stable field names as the trace contract."
    - "D-13/D-14/D-15/D-16: Documentation states the manifest is Java-side, backend-neutral with backend extension map, no native ABI change, no public GPU Tensor API, and no CPU fusion contract change."
    - "Local tuning profile artifacts under profiles/platform/.../tuning/abc/* remain unstaged unless intentionally promoted to canonical fixtures."
---

<objective>
Close Phase 15 with developer documentation, validation evidence, CPU/public API guardrails, and artifact hygiene.
</objective>

<source_audit>
GOAL: Documents and verifies the lowered-region manifest contract as the handoff to Phase 16 and later v1.3 work.
REQ GPUDAG-01: Covered by docs explaining multi-op selected region manifests.
REQ GPUDAG-02: Covered by field-level docs for original ops, primitives, assumptions, fused summaries, backend id, and region length.
REQ GPUDAG-03: Covered by docs and validation for stable rejection, fallback, materialization, and shortening reason metadata.
CONTEXT D-09, D-10, D-11, D-12, D-13, D-14, D-15, and D-16 are closed by documentation and validation.
</source_audit>

<threat_model>
T-15-10: Future phases could treat the manifest as permission to add public GPU tensor residency. Mitigation: docs explicitly state public `Tensor` remains logical and residency lives in prepare/execute runtime state.
T-15-11: Future backend work could assume Phase 15 changed native ABI. Mitigation: docs explicitly state Phase 15 is Java-side only and native Metal/CUDA bridge contracts are unchanged.
T-15-12: Local tuning artifacts could be accidentally committed during validation. Mitigation: final summary and validation require `git status --short` evidence and the exact hygiene note.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document lowered-region manifest contract</name>
  <files>docs/gpu-lowered-region-manifest.md, docs/gpu-lowering-coverage.md</files>
  <read_first>
    .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-CONTEXT.md
    .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-RESEARCH.md
    docs/gpu-lowering-coverage.md
    docs/compute-flow.md
    src/main/java/backend/accelerator/lowering/GpuLoweredRegionManifest.java
    src/main/java/backend/accelerator/lowering/GpuLoweredRegionManifestRenderer.java
  </read_first>
  <action>
    Create `docs/gpu-lowered-region-manifest.md` with exact sections:
    - `# GPU Lowered Region Manifest`
    - `## Purpose`
    - `## Manifest Fields`
    - `## Original Ops And Lowered Primitives`
    - `## Value Assumptions`
    - `## Fused Subpattern Placeholder`
    - `## Rejection And Shortening Reasons`
    - `## Trace Contract`
    - `## Backend Boundaries`
    - `## Phase Handoff`

    Include exact statements:
    - `GpuLoweredRegionManifest is Java-side metadata and does not change the Metal or CUDA native ABI.`
    - `Public Tensor remains logical; device residency lives in compile/prepare/execute runtime state.`
    - `GPU fusion metadata is region-internal lowering/fusion metadata, not CPU Operation.OpType.FUSED.`
    - `Prepare/backend-selection trace is the source of truth for the structured manifest.`
    - `Run trace references the region id and runtime outcome without duplicating the full manifest.`

    Update `docs/gpu-lowering-coverage.md` with a short Phase 15 section linking the manifest docs and naming the exact reason constants:
    - `DAG_PRIMITIVE_UNSUPPORTED`
    - `DAG_REGION_BOUNDARY_MATERIALIZATION`
    - `DAG_CANDIDATE_SHORTENED`
    - `DAG_FUSED_SUBPATTERN_REJECTED`
  </action>
  <verify>
    <automated>rg -n "# GPU Lowered Region Manifest|Manifest Fields|Original Ops And Lowered Primitives|Value Assumptions|Fused Subpattern Placeholder|Rejection And Shortening Reasons|Trace Contract|Backend Boundaries|Phase Handoff|Java-side metadata|Public Tensor remains logical|not CPU Operation.OpType.FUSED|Prepare/backend-selection trace|Run trace references the region id" docs/gpu-lowered-region-manifest.md</automated>
    <automated>rg -n "GPU Lowered Region Manifest|DAG_PRIMITIVE_UNSUPPORTED|DAG_REGION_BOUNDARY_MATERIALIZATION|DAG_CANDIDATE_SHORTENED|DAG_FUSED_SUBPATTERN_REJECTED" docs/gpu-lowering-coverage.md</automated>
  </verify>
  <acceptance_criteria>
    `docs/gpu-lowered-region-manifest.md` contains all exact section headings and boundary statements listed in the action.
  </acceptance_criteria>
  <done>Developer docs define the manifest contract and phase boundaries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update development and compute-flow docs</name>
  <files>docs/development.md, docs/compute-flow.md</files>
  <read_first>
    docs/development.md
    docs/compute-flow.md
    docs/gpu-lowered-region-manifest.md
    docs/gpu-coverage-triage.md
    src/test/java/SourceTreeHygieneTest.java
  </read_first>
  <action>
    Update `docs/development.md` with a focused Phase 15 verification command block containing:
    - `./gradlew test --tests backend.accelerator.lowering.GpuLoweredRegionManifestTest --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests CompiledGraphTraceTest`
    - `./gradlew test --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest`
    - `./gradlew test --tests BenchmarkSessionTest --tests GpuCoverageSummaryTest`
    - `./gradlew test --tests SourceTreeHygieneTest`

    Update `docs/compute-flow.md` with a subsection titled `GPU lowered-region manifest` that states:
    - selected GPU regions can be described as lowered DAG manifests,
    - the manifest is attached during prepare/backend selection,
    - run trace keeps only compact region id/runtime evidence,
    - CPU `Operation.OpType.FUSED` remains CPU-only,
    - native Metal/CUDA ABI is unchanged by Phase 15.
  </action>
  <verify>
    <automated>rg -n "GpuLoweredRegionManifestTest|AcceleratorSubgraphLowererTest|CompiledGraphTraceTest|MetalRegionLowererTest|CudaRegionLowererTest|BenchmarkSessionTest|GpuCoverageSummaryTest|SourceTreeHygieneTest" docs/development.md</automated>
    <automated>rg -n "GPU lowered-region manifest|prepare/backend selection|region id/runtime evidence|Operation.OpType.FUSED remains CPU-only|native Metal/CUDA ABI is unchanged" docs/compute-flow.md</automated>
  </verify>
  <acceptance_criteria>
    `docs/development.md` contains all focused Phase 15 verification commands, and `docs/compute-flow.md` contains the `GPU lowered-region manifest` subsection with the exact boundary statements.
  </acceptance_criteria>
  <done>Operational docs tell future developers how to verify and interpret the manifest contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run final focused verification and update validation evidence</name>
  <files>.planning/phases/15-gpu-region-internal-lowered-dag-contract/15-VALIDATION.md</files>
  <read_first>
    .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-VALIDATION.md
    .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-01-SUMMARY.md
    .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-02-SUMMARY.md
    .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-03-SUMMARY.md
  </read_first>
  <action>
    Run final focused verification:
    - `./gradlew classes`
    - `./gradlew test --tests backend.accelerator.lowering.GpuLoweredRegionManifestTest --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests CompiledGraphTraceTest --tests BenchmarkSessionTest --tests GpuCoverageSummaryTest --tests SourceTreeHygieneTest`
    - `git status --short`

    Update `15-VALIDATION.md`:
    - set frontmatter `status: verified`
    - keep `nyquist_compliant: true`
    - add section `## Execution Evidence`
    - list every final command above
    - list covered test classes `GpuLoweredRegionManifestTest`, `AcceleratorSubgraphLowererTest`, `MetalRegionLowererTest`, `CudaRegionLowererTest`, `CompiledGraphTraceTest`, `BenchmarkSessionTest`, `GpuCoverageSummaryTest`, and `SourceTreeHygieneTest`
    - add exact text `profiles/platform/.../tuning/abc/* remained unstaged`
    - set `**Approval:** verified`
  </action>
  <verify>
    <automated>rg -n "status: verified|nyquist_compliant: true|Execution Evidence|GpuLoweredRegionManifestTest|AcceleratorSubgraphLowererTest|MetalRegionLowererTest|CudaRegionLowererTest|CompiledGraphTraceTest|BenchmarkSessionTest|GpuCoverageSummaryTest|SourceTreeHygieneTest|profiles/platform/.../tuning/abc/\\* remained unstaged|Approval: verified" .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/15-gpu-region-internal-lowered-dag-contract/15-VALIDATION.md` contains `status: verified`, `Execution Evidence`, all covered test class names, and `profiles/platform/.../tuning/abc/* remained unstaged`.
  </acceptance_criteria>
  <done>Validation evidence closes Nyquist sampling for Phase 15.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Record final phase summary and source hygiene evidence</name>
  <files>.planning/phases/15-gpu-region-internal-lowered-dag-contract/15-04-SUMMARY.md</files>
  <read_first>
    .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-04-PLAN.md
    .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-VALIDATION.md
    docs/gpu-lowered-region-manifest.md
    docs/development.md
    docs/compute-flow.md
  </read_first>
  <action>
    Create `15-04-SUMMARY.md` with:
    - frontmatter containing `phase: 15-gpu-region-internal-lowered-dag-contract`, `plan: "04"`, and `status: complete`
    - a section `Phase 15 final verification`
    - a section `Requirement Closure` naming `GPUDAG-01`, `GPUDAG-02`, and `GPUDAG-03`
    - a section `Decision Closure` naming `D-01` through `D-16`
    - a section `Downstream Handoff` naming Phase 16, Phase 17, Phase 18, Phase 19, and Phase 20 consumers
    - a section `Artifact Hygiene`
    - exact text `profiles/platform/.../tuning/abc/* remained unstaged`
    - exact text `Native Metal/CUDA ABI unchanged`
    - exact text `Public Tensor API unchanged`
    - exact text `CPU Operation.OpType.FUSED remains CPU-only`
    - a `Self-Check: PASSED` line
  </action>
  <verify>
    <automated>test -f .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-04-SUMMARY.md</automated>
    <automated>rg -n "Phase 15 final verification|Requirement Closure|Decision Closure|D-01|D-16|Downstream Handoff|Phase 16|Phase 20|Artifact Hygiene|profiles/platform/.../tuning/abc/\\* remained unstaged|Native Metal/CUDA ABI unchanged|Public Tensor API unchanged|CPU Operation.OpType.FUSED remains CPU-only|Self-Check: PASSED" .planning/phases/15-gpu-region-internal-lowered-dag-contract/15-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/15-gpu-region-internal-lowered-dag-contract/15-04-SUMMARY.md` contains all closure sections and exact boundary/hygiene statements from the action.
  </acceptance_criteria>
  <done>Phase 15 closure evidence is ready for `$gsd-verify-work`.</done>
</task>

</tasks>

<verification>
Run:
- `./gradlew classes`
- `./gradlew test --tests backend.accelerator.lowering.GpuLoweredRegionManifestTest --tests backend.accelerator.lowering.AcceleratorSubgraphLowererTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests CompiledGraphTraceTest --tests BenchmarkSessionTest --tests GpuCoverageSummaryTest --tests SourceTreeHygieneTest`
- `rg -n "GPU Lowered Region Manifest|Java-side metadata|Public Tensor remains logical|Operation.OpType.FUSED remains CPU-only|native Metal/CUDA ABI is unchanged" docs/gpu-lowered-region-manifest.md docs/compute-flow.md docs/development.md`
- `git status --short`
</verification>

<success_criteria>
- Docs explain the manifest fields, trace contract, reason codes, and backend boundaries.
- Validation evidence records final focused test coverage.
- Public `Tensor`, CPU fusion, and native Metal/CUDA ABI boundaries are explicitly protected.
- Local tuning profile artifacts remain unstaged.
- Phase 15 is ready for `$gsd-verify-work` after execution.
</success_criteria>
