---
phase: 20-coverage-regression-hardening
plan: "04"
type: execute
wave: 4
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - docs/testing.md
  - docs/development.md
  - docs/compute-flow.md
  - docs/gpu-lowering-coverage.md
  - src/test/java/SourceTreeHygieneTest.java
  - .planning/phases/20-coverage-regression-hardening/20-04-SUMMARY.md
autonomous: true
requirements: [GPUHARDEN-01, GPUHARDEN-02, GPUHARDEN-03]
must_haves:
  truths:
    - "GPUHARDEN-01/02/03: Phase closure documents the v1.3 hard coverage evidence contract and records final focused verification."
    - "Local profiles under profiles/platform/.../tuning/abc/* remain unstaged unless intentionally promoted."
    - "Docs distinguish portable Java proof, capability-gated native pass/skip evidence, and timing-only non-proof."
    - "D-16: Docs explain the v1.3 evidence contract, coverage failure interpretation, native skip evidence, and non-canonical profile artifacts."
    - "D-17: Local `profiles/platform/.../tuning/abc/*` artifacts remain unstaged unless explicitly promoted as stable fixtures."
---

<objective>
Close Phase 20 with docs, source hygiene, final validation evidence, and a summary suitable for v1.3 milestone audit.
</objective>

<source_audit>
REQ GPUHARDEN-01: Covered by docs and summary listing required report fields.
REQ GPUHARDEN-02: Covered by docs and final verification of hard gate failure semantics.
REQ GPUHARDEN-03: Covered by docs and native capability-gated evidence language.
D-16: Docs explain the v1.3 evidence contract, coverage failure interpretation, native skip evidence, and non-canonical profile artifacts.
D-17: Local `profiles/platform/.../tuning/abc/*` artifacts remain unstaged unless explicitly promoted as stable fixtures.
</source_audit>

<threat_model>
T-20-07: Docs could imply timing-only benchmark wins prove GPU residency. Mitigation: include exact language that hot-path GPU proof is trace/report based, not timing-only.
T-20-08: Local tuning outputs could be staged as closure evidence. Mitigation: add source hygiene/doc checks and final `git status --short` review.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 20 source hygiene checks</name>
  <files>src/test/java/SourceTreeHygieneTest.java</files>
  <read_first>
    src/test/java/SourceTreeHygieneTest.java
    src/main/java/tuning/benchmark/report/GpuCoverageRegressionGate.java
    src/main/java/tuning/benchmark/report/GpuCoverageGatePolicy.java
    src/main/java/tuning/benchmark/report/GpuHotPathCoverageTargets.java
  </read_first>
  <action>
    Add or extend tests named:
    - `phaseTwentyCoverageGatesDoNotDependOnTimingOnlyMetrics`
    - `phaseTwentyHotPathTargetsRemainSourceOfTruth`
    - `phaseTwentyLocalTuningArtifactsRemainNonCanonical`

    The timing-only check must scan `src/main/java/tuning/benchmark/report/GpuCoverageRegressionGate.java` and fail if the gate implementation uses measurement median names such as `median`, `medianNs`, or `durationNs` for pass/fail.

    The target source-of-truth check must assert `GpuHotPathCoverageTargets.java` contains all exact target names:
    - `transformer_block_hot_path`
    - `mlp_classifier_small`
    - `conv2d_resnet_3x3`
    - `layer_norm_small`

    The local tuning artifact check must preserve the existing policy that `profiles/platform/.../tuning/abc/*` changes are not staged as Phase 20 evidence.
  </action>
  <verify>
    <automated>./gradlew test --tests SourceTreeHygieneTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "phaseTwentyCoverageGatesDoNotDependOnTimingOnlyMetrics|phaseTwentyHotPathTargetsRemainSourceOfTruth|phaseTwentyLocalTuningArtifactsRemainNonCanonical|transformer_block_hot_path|mlp_classifier_small|conv2d_resnet_3x3|layer_norm_small" src/test/java/SourceTreeHygieneTest.java` exits 0.
  </acceptance_criteria>
  <done>Source hygiene protects Phase 20 evidence boundaries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document the hard GPU coverage evidence contract</name>
  <files>docs/testing.md, docs/development.md, docs/compute-flow.md, docs/gpu-lowering-coverage.md</files>
  <read_first>
    docs/testing.md
    docs/development.md
    docs/compute-flow.md
    docs/gpu-lowering-coverage.md
    .planning/phases/20-coverage-regression-hardening/20-CONTEXT.md
    .planning/phases/20-coverage-regression-hardening/20-VALIDATION.md
  </read_first>
  <action>
    Add or update documentation with exact strings:
    - `Phase 20 coverage regression hardening`
    - `hot path stayed on GPU is trace/report evidence, not timing-only`
    - `targetCoverageGates`
    - `nativeEvidence`
    - `capabilitySkipped`
    - `tensor-array bridge execution is not native buffer GPU coverage`
    - `profiles/platform/.../tuning/abc/* remained unstaged`

    Docs must distinguish:
    - portable Java gate proof,
    - capability-gated native Metal/CUDA pass/skip evidence,
    - local benchmark/profile artifacts that are not canonical proof.
  </action>
  <verify>
    <automated>rg -n "Phase 20 coverage regression hardening|hot path stayed on GPU is trace/report evidence, not timing-only|targetCoverageGates|nativeEvidence|capabilitySkipped|tensor-array bridge execution is not native buffer GPU coverage|profiles/platform/.../tuning/abc/\\* remained unstaged" docs/testing.md docs/development.md docs/compute-flow.md docs/gpu-lowering-coverage.md</automated>
  </verify>
  <acceptance_criteria>
    `docs/testing.md`, `docs/development.md`, `docs/compute-flow.md`, and `docs/gpu-lowering-coverage.md` contain the Phase 20 evidence strings above.
  </acceptance_criteria>
  <done>Docs explain the v1.3 hard coverage evidence contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run final Phase 20 focused verification and write summary</name>
  <files>.planning/phases/20-coverage-regression-hardening/20-04-SUMMARY.md</files>
  <read_first>
    .planning/phases/20-coverage-regression-hardening/20-01-SUMMARY.md
    .planning/phases/20-coverage-regression-hardening/20-02-SUMMARY.md
    .planning/phases/20-coverage-regression-hardening/20-03-SUMMARY.md
    .planning/phases/20-coverage-regression-hardening/20-VALIDATION.md
  </read_first>
  <action>
    Run final verification:
    - `./gradlew classes`
    - `./gradlew test --tests GpuCoverageSummaryTest --tests GpuCoverageRegressionGateTest --tests GpuHotPathCoverageTargetsTest --tests BenchmarkSessionTest --tests BenchmarkSuiteSessionTest --tests CompiledGraphTraceTest --tests SourceTreeHygieneTest`
    - `git status --short`

    Create `20-04-SUMMARY.md` with:
    - frontmatter containing `phase: 20-coverage-regression-hardening`, `plan: "04"`, and `status: complete`
    - section `Phase 20 coverage regression hardening closure`
    - requirement coverage notes for `GPUHARDEN-01`, `GPUHARDEN-02`, and `GPUHARDEN-03`
    - exact text `hot path stayed on GPU is trace/report evidence, not timing-only`
    - exact text `tensor-array bridge execution is not native buffer GPU coverage`
    - exact text `capabilitySkipped`
    - final verification command table
    - hygiene note `profiles/platform/.../tuning/abc/* remained unstaged`
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests GpuCoverageSummaryTest --tests GpuCoverageRegressionGateTest --tests GpuHotPathCoverageTargetsTest --tests BenchmarkSessionTest --tests BenchmarkSuiteSessionTest --tests CompiledGraphTraceTest --tests SourceTreeHygieneTest</automated>
    <automated>git status --short</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/20-coverage-regression-hardening/20-04-SUMMARY.md` contains `Phase 20 coverage regression hardening closure`, all three `GPUHARDEN` ids, `hot path stayed on GPU is trace/report evidence, not timing-only`, `tensor-array bridge execution is not native buffer GPU coverage`, and `profiles/platform/.../tuning/abc/* remained unstaged`.
  </acceptance_criteria>
  <done>Phase 20 closure evidence is recorded for milestone audit.</done>
</task>

</tasks>

<verification>
Run:

```bash
./gradlew classes
./gradlew test --tests GpuCoverageSummaryTest --tests GpuCoverageRegressionGateTest --tests GpuHotPathCoverageTargetsTest --tests BenchmarkSessionTest --tests BenchmarkSuiteSessionTest --tests CompiledGraphTraceTest --tests SourceTreeHygieneTest
git status --short
```
</verification>

<success_criteria>
- Docs explain hard coverage gates and native capability skip evidence.
- Source hygiene prevents timing-only gates and target list drift.
- Final summary records Phase 20 verification and profile artifact hygiene.
</success_criteria>
