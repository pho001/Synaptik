---
phase: 16-dtype-and-storage-residency-expansion
plan: "04"
type: execute
wave: 4
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - docs/compute-flow.md
  - docs/gpu-lowering-coverage.md
  - docs/development.md
  - .planning/phases/16-dtype-and-storage-residency-expansion/16-VALIDATION.md
  - .planning/phases/16-dtype-and-storage-residency-expansion/16-04-SUMMARY.md
autonomous: true
requirements: [GPUSTORAGE-01, GPUSTORAGE-02, GPUSTORAGE-03]
must_haves:
  truths:
    - "GPUSTORAGE-01/GPUSTORAGE-02/GPUSTORAGE-03: Phase closure must prove typed residency, capability diagnostics, and no hidden CPU materialization for supported internal values."
    - "D-01/D-02/D-03/D-04/D-05/D-06/D-07/D-08/D-09/D-10/D-11/D-12/D-13/D-14: All Phase 16 decisions are documented, tested, and reflected in validation evidence."
    - "Local tuning profiles remain local artifacts and are not staged or committed."
---

<objective>
Close Phase 16 with docs, focused verification, validation evidence, and artifact hygiene.
</objective>

<source_audit>
GOAL: Turns implementation evidence into durable developer guidance and validation artifacts.
REQ GPUSTORAGE-01: Covered by runtime typed slot binding docs and tests.
REQ GPUSTORAGE-02: Covered by backend capability/rejection docs and tests.
REQ GPUSTORAGE-03: Covered by trace/report verification and final focused test slice.
CONTEXT D-01 through D-14 are covered by this closure wave.
</source_audit>

<threat_model>
T-16-07: Closure could over-document unsupported native dtype compute or accidentally commit machine-local profile artifacts. Mitigation: docs must explicitly distinguish residency from compute support, and final hygiene requires `git status --short` review before staging.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document dtype residency contract</name>
  <files>docs/compute-flow.md, docs/gpu-lowering-coverage.md, docs/development.md</files>
  <read_first>
    docs/compute-flow.md
    docs/gpu-lowering-coverage.md
    docs/development.md
    .planning/phases/16-dtype-and-storage-residency-expansion/16-CONTEXT.md
    .planning/phases/16-dtype-and-storage-residency-expansion/16-01-SUMMARY.md
    .planning/phases/16-dtype-and-storage-residency-expansion/16-02-SUMMARY.md
    .planning/phases/16-dtype-and-storage-residency-expansion/16-03-SUMMARY.md
  </read_first>
  <action>
    Update docs with exact concepts:
    - `docs/compute-flow.md`: add a subsection `DType residency and materialization boundaries` explaining that `BFLOAT16`, `INT32`, and `BOOL` can be represented in runtime storage residency, but true CPU consumers, graph outputs, and gradient publication can still materialize.
    - `docs/gpu-lowering-coverage.md`: add a subsection `DType residency is not native dtype compute` explaining Metal/CUDA role gates and `UNSUPPORTED_DTYPE` diagnostics.
    - `docs/development.md`: add Phase 16 focused verification commands:
      `./gradlew test --tests graph.execution.RuntimeMemoryBinderTest --tests backend.accelerator.residency.AcceleratorDTypeResidencyPolicyTest --tests GpuCoverageSummaryTest`

    Include exact phrase `dtype residency is not native dtype compute` in docs.
  </action>
  <verify>
    <automated>rg -n "DType residency and materialization boundaries|DType residency is not native dtype compute|dtype residency is not native dtype compute|RuntimeMemoryBinderTest|AcceleratorDTypeResidencyPolicyTest|GpuCoverageSummaryTest" docs/compute-flow.md docs/gpu-lowering-coverage.md docs/development.md</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "dtype residency is not native dtype compute|BFLOAT16|INT32|BOOL|UNSUPPORTED_DTYPE" docs/compute-flow.md docs/gpu-lowering-coverage.md docs/development.md` exits 0.
  </acceptance_criteria>
  <done>Developer docs describe residency, capability, and materialization boundaries accurately.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run final focused verification and update validation evidence</name>
  <files>.planning/phases/16-dtype-and-storage-residency-expansion/16-VALIDATION.md</files>
  <read_first>
    .planning/phases/16-dtype-and-storage-residency-expansion/16-VALIDATION.md
    .planning/phases/16-dtype-and-storage-residency-expansion/16-01-SUMMARY.md
    .planning/phases/16-dtype-and-storage-residency-expansion/16-02-SUMMARY.md
    .planning/phases/16-dtype-and-storage-residency-expansion/16-03-SUMMARY.md
  </read_first>
  <action>
    Run:
    `./gradlew classes`

    Then run:
    `./gradlew test --tests graph.execution.RuntimeMemoryBinderTest --tests backend.accelerator.buffer.AcceleratorBufferLayoutClassifierTest --tests backend.accelerator.residency.AcceleratorDTypeResidencyPolicyTest --tests backend.metal.MetalMpsCapabilitiesTest --tests backend.cuda.buffer.CudaBufferAllocatorTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests SourceTreeHygieneTest`

    Update `16-VALIDATION.md`:
    - set `status: verified` after commands pass
    - mark all per-task rows `passed`
    - add section `Execution Evidence`
    - add a table row for `./gradlew classes`
    - add a table row for the focused test command
    - add a table row for `git status --short`
    - add exact text `profiles/platform/.../tuning/abc/* remained unstaged`
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests graph.execution.RuntimeMemoryBinderTest --tests backend.accelerator.buffer.AcceleratorBufferLayoutClassifierTest --tests backend.accelerator.residency.AcceleratorDTypeResidencyPolicyTest --tests backend.metal.MetalMpsCapabilitiesTest --tests backend.cuda.buffer.CudaBufferAllocatorTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests SourceTreeHygieneTest</automated>
    <automated>rg -n "status: verified|Execution Evidence|RuntimeMemoryBinderTest|AcceleratorDTypeResidencyPolicyTest|SourceTreeHygieneTest|profiles/platform/.../tuning/abc/\\* remained unstaged" .planning/phases/16-dtype-and-storage-residency-expansion/16-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/16-dtype-and-storage-residency-expansion/16-VALIDATION.md` contains `status: verified`, `Execution Evidence`, and `profiles/platform/.../tuning/abc/* remained unstaged`.
  </acceptance_criteria>
  <done>Phase 16 validation evidence is complete and Nyquist-compliant.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record closure summary and source hygiene</name>
  <files>.planning/phases/16-dtype-and-storage-residency-expansion/16-04-SUMMARY.md</files>
  <read_first>
    .planning/phases/16-dtype-and-storage-residency-expansion/16-04-PLAN.md
    .planning/phases/16-dtype-and-storage-residency-expansion/16-VALIDATION.md
    docs/compute-flow.md
    docs/gpu-lowering-coverage.md
    docs/development.md
  </read_first>
  <action>
    Create `16-04-SUMMARY.md` with:
    - frontmatter containing `phase: 16-dtype-and-storage-residency-expansion`, `plan: "04"`, and `status: complete`
    - a section `DType storage residency closure`
    - a checklist for `GPUSTORAGE-01`, `GPUSTORAGE-02`, and `GPUSTORAGE-03`
    - a verification table listing final commands
    - a docs note containing exact text `dtype residency is not native dtype compute`
    - a hygiene note containing exact text `profiles/platform/.../tuning/abc/* remained unstaged`

    Before staging, run `git status --short` and verify local profile files are not included in the intended commit list.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-dtype-and-storage-residency-expansion/16-04-SUMMARY.md</automated>
    <automated>rg -n "DType storage residency closure|GPUSTORAGE-01|GPUSTORAGE-02|GPUSTORAGE-03|dtype residency is not native dtype compute|profiles/platform/.../tuning/abc/\\* remained unstaged" .planning/phases/16-dtype-and-storage-residency-expansion/16-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/16-dtype-and-storage-residency-expansion/16-04-SUMMARY.md` contains `DType storage residency closure`, all three `GPUSTORAGE` IDs, and the profile artifact hygiene phrase.
  </acceptance_criteria>
  <done>Phase 16 is ready for security, validation, and work verification.</done>
</task>

</tasks>

<verification>
Run:

```bash
./gradlew classes
./gradlew test --tests graph.execution.RuntimeMemoryBinderTest --tests backend.accelerator.buffer.AcceleratorBufferLayoutClassifierTest --tests backend.accelerator.residency.AcceleratorDTypeResidencyPolicyTest --tests backend.metal.MetalMpsCapabilitiesTest --tests backend.cuda.buffer.CudaBufferAllocatorTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests SourceTreeHygieneTest
git status --short
```
</verification>

<success_criteria>
- Docs distinguish dtype residency from native dtype compute.
- Validation evidence covers runtime binding, capability diagnostics, trace/report visibility, and hygiene.
- No local tuning profile artifacts are staged or committed.
</success_criteria>
