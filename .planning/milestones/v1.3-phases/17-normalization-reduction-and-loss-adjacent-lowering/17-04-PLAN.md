---
phase: 17-normalization-reduction-and-loss-adjacent-lowering
plan: "04"
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - docs/gpu-lowering-coverage.md
  - docs/compute-flow.md
  - docs/development.md
  - .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-VALIDATION.md
  - .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-04-SUMMARY.md
autonomous: true
requirements: [GPUNORM-01, GPUNORM-02, GPUNORM-03]
must_haves:
  truths:
    - "GPUNORM-01/GPUNORM-02/GPUNORM-03: Phase closure must prove matrix coverage, stable rejection or supported lowering, CPU parity, trace/report evidence, and artifact hygiene."
    - "D-01 through D-15: All Phase 17 decisions are documented, tested, and reflected in validation evidence."
    - "Local tuning profiles remain local artifacts and are not staged or committed."
---

<objective>
Close Phase 17 with docs, focused verification, validation evidence, and artifact hygiene.
</objective>

<source_audit>
GOAL: Turns Phase 17 implementation evidence into durable developer guidance and validation artifacts.
REQ GPUNORM-01: Covered by docs and validation evidence for shared matrix coverage.
REQ GPUNORM-02: Covered by docs and validation evidence for selected/rejected candidates tied to hot-path gaps.
REQ GPUNORM-03: Covered by CPU parity, dtype/layout legality, trace/report, and final focused test evidence.
CONTEXT D-01 through D-15 are covered by this closure wave.
</source_audit>

<threat_model>
T-17-07: Closure could overstate native reduction/normalization support or commit machine-local profile artifacts. Mitigation: docs must state support vs visible rejection precisely, and final hygiene requires `git status --short` review before staging.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document Phase 17 lowering and fallback contract</name>
  <files>docs/gpu-lowering-coverage.md, docs/compute-flow.md, docs/development.md</files>
  <read_first>
    docs/gpu-lowering-coverage.md
    docs/compute-flow.md
    docs/development.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-CONTEXT.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-01-SUMMARY.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-02-SUMMARY.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-03-SUMMARY.md
  </read_first>
  <action>
    Update docs with exact concepts:
    - `docs/gpu-lowering-coverage.md`: finalize the section `Phase 17 normalization, reduction, and loss-adjacent contract`.
    - `docs/compute-flow.md`: add a subsection `Visible fallback for numerically sensitive GPU gaps`.
    - `docs/development.md`: add Phase 17 focused verification command:
      `./gradlew test --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest`

    Include exact phrases:
    - `LOG_SOFTMAX remains lowered as SOFTMAX followed by LOG`
    - `loss-adjacent fallback remained visible`
    - `CPU parity remained the correctness oracle`
    - `native reduction and normalization support is not implied by a fallback row`
  </action>
  <verify>
    <automated>rg -n "Phase 17 normalization, reduction, and loss-adjacent contract|Visible fallback for numerically sensitive GPU gaps|LOG_SOFTMAX remains lowered as SOFTMAX followed by LOG|loss-adjacent fallback remained visible|CPU parity remained the correctness oracle|native reduction and normalization support is not implied by a fallback row|GpuLoweringCoverageMatrixTest|MetalRegionLowererTest|CudaRegionLowererTest" docs/gpu-lowering-coverage.md docs/compute-flow.md docs/development.md</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "native reduction and normalization support is not implied by a fallback row|loss-adjacent fallback remained visible|CPU parity remained the correctness oracle" docs/gpu-lowering-coverage.md docs/compute-flow.md docs/development.md` exits 0.
  </acceptance_criteria>
  <done>Developer docs describe support, fallback, and verification boundaries accurately.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run final focused verification and update validation evidence</name>
  <files>.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-VALIDATION.md</files>
  <read_first>
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-VALIDATION.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-01-SUMMARY.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-02-SUMMARY.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-03-SUMMARY.md
  </read_first>
  <action>
    Run:
    `./gradlew classes`

    Then run:
    `./gradlew test --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests SourceTreeHygieneTest`

    Update `17-VALIDATION.md`:
    - set `status: verified` after commands pass
    - mark all per-task rows `passed`
    - add section `Execution Evidence`
    - add a table row for `./gradlew classes`
    - add a table row for the focused test command
    - add a table row for `git status --short`
    - add exact text `profiles/platform/.../tuning/abc/* remained unstaged`
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests SourceTreeHygieneTest</automated>
    <automated>rg -n "status: verified|Execution Evidence|GpuLoweringCoverageMatrixTest|MetalRegionLowererTest|CudaRegionLowererTest|PreparedExecutionBuildTest|SourceTreeHygieneTest|profiles/platform/.../tuning/abc/\\* remained unstaged" .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-VALIDATION.md` contains `status: verified`, `Execution Evidence`, and `profiles/platform/.../tuning/abc/* remained unstaged`.
  </acceptance_criteria>
  <done>Phase 17 validation evidence is complete and Nyquist-compliant.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record closure summary and source hygiene</name>
  <files>.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-04-SUMMARY.md</files>
  <read_first>
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-04-PLAN.md
    .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-VALIDATION.md
    docs/gpu-lowering-coverage.md
    docs/compute-flow.md
    docs/development.md
  </read_first>
  <action>
    Create `17-04-SUMMARY.md` with:
    - frontmatter containing `phase: 17-normalization-reduction-and-loss-adjacent-lowering`, `plan: "04"`, and `status: complete`
    - a section `Normalization reduction and loss-adjacent closure`
    - a checklist for `GPUNORM-01`, `GPUNORM-02`, and `GPUNORM-03`
    - a verification table listing final commands
    - a docs note containing exact text `native reduction and normalization support is not implied by a fallback row`
    - a hygiene note containing exact text `profiles/platform/.../tuning/abc/* remained unstaged`

    Before staging, run `git status --short` and verify local profile files are not included in the intended commit list.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-04-SUMMARY.md</automated>
    <automated>rg -n "Normalization reduction and loss-adjacent closure|GPUNORM-01|GPUNORM-02|GPUNORM-03|native reduction and normalization support is not implied by a fallback row|profiles/platform/.../tuning/abc/\\* remained unstaged" .planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/17-normalization-reduction-and-loss-adjacent-lowering/17-04-SUMMARY.md` contains `Normalization reduction and loss-adjacent closure`, all three `GPUNORM` IDs, and the profile artifact hygiene phrase.
  </acceptance_criteria>
  <done>Phase 17 is ready for security, validation, and work verification.</done>
</task>

</tasks>

<verification>
Run:

```bash
./gradlew classes
./gradlew test --tests backend.accelerator.lowering.GpuLoweringCoverageMatrixTest --tests backend.metal.lowering.MetalRegionLowererTest --tests backend.cuda.lowering.CudaRegionLowererTest --tests PreparedExecutionBuildTest --tests CompiledGraphTraceTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests SourceTreeHygieneTest
git status --short
```
</verification>

<success_criteria>
- Docs accurately describe Phase 17 support and visible fallback.
- Validation evidence covers matrix, backend legality, trace/report, CPU parity, and hygiene.
- No local tuning profile artifacts are staged or committed.
</success_criteria>
