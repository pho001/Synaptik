---
phase: 08-cuda-observability-and-documentation-closure
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - docs/architecture.md
  - docs/compute-flow.md
  - docs/development.md
  - docs/configuration.md
  - docs/testing.md
  - docs/troubleshooting.md
  - .planning/phases/08-cuda-observability-and-documentation-closure/08-03-SUMMARY.md
autonomous: true
requirements:
  - CUDADOC-02
  - CUDADOC-01
  - CUDA-06
user_setup:
  - "Do not commit measured local CUDA benchmark output as documentation evidence."
must_haves:
  truths:
    - "D-24: Docs state CUDA trace/report parity is implemented only for the narrow dense FLOAT32 buffer path proven by v1.1."
    - "D-25: Docs explain CUDA build prerequisites, capability probing, fallback interpretation, and troubleshooting."
    - "D-26: Docs do not overclaim broad CUDA operation coverage or real-hardware CUDA performance on machines where native CUDA checks skip."
  artifacts:
    - path: "docs/compute-flow.md"
      provides: "trace and fallback interpretation"
      contains: "cudaExecutionPath"
    - path: "docs/testing.md"
      provides: "CUDA verification command guidance"
      contains: "./gradlew buildCudaGraphShim cudaTest"
---

<objective>
Update developer documentation so CUDA build/probe setup, trace/report evidence, fallback interpretation, troubleshooting, and Metal/CUDA ABI symmetry are accurate after Phase 8.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cuda-observability-and-documentation-closure/08-CONTEXT.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-RESEARCH.md
@docs/architecture.md
@docs/compute-flow.md
@docs/development.md
@docs/configuration.md
@docs/testing.md
@docs/troubleshooting.md
@AGENTS.md
</context>

<source_audit>
GOAL: Covers developer docs closure and fallback interpretation.
REQ CUDADOC-02: Covered by build/probe/troubleshooting/fallback docs.
REQ CUDADOC-01/CUDA-06: Covered by docs explaining the trace/report fields and reason-code matrix implemented by Plans 08-01 and 08-02.
</source_audit>

<threat_model>
T-8-05: Documentation could imply broad CUDA coverage or real-hardware performance evidence that was not proven. Mitigation: docs explicitly scope support to narrow dense FLOAT32 native-buffer execution and mark optional native CUDA checks as pass-or-skip by capability.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document CUDA trace and benchmark report fields</name>
  <files>docs/architecture.md, docs/compute-flow.md, docs/configuration.md</files>
  <read_first>
    docs/architecture.md
    docs/compute-flow.md
    docs/configuration.md
    src/main/java/graph/execution/PreparedExecution.java
    src/main/java/tuning/benchmark/report/AcceleratorTraceSummary.java
  </read_first>
  <action>
    Update docs with exact strings:
    - `CUDA trace and benchmark reports`
    - `GPU_CUDA`
    - `cudaExecutionPath`
    - `cudaFallbackReason`
    - `acceleratorBufferReasonCode`
    - `acceleratorInputBytes`
    - `acceleratorNativeDeviceCopyNs`
    - `cpuMaterializationCount`
    - `StorageResidency.DEVICE_OWNED`

    Explain:
    - CUDA report evidence uses the same accelerator summary contract as Metal.
    - CUDA copy timing fields are Java-observed boundary timings; native device sub-timers can be `0` when the shim does not expose them.
    - `RunTrace.cpuMaterializations()` remains the source for graph-output and CPU-consumer materialization reasons.
  </action>
  <verify>
    <automated>rg -n "CUDA trace and benchmark reports|cudaExecutionPath|cudaFallbackReason|acceleratorBufferReasonCode|acceleratorInputBytes|acceleratorNativeDeviceCopyNs|cpuMaterializationCount" docs/architecture.md docs/compute-flow.md docs/configuration.md</automated>
  </verify>
  <acceptance_criteria>
    The grep command in verify exits 0. `docs/configuration.md` no longer says CUDA trace/report parity remains a later observability task.
  </acceptance_criteria>
  <done>Docs describe CUDA trace/report parity with concrete field names.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document CUDA build, probe, fallback, and troubleshooting</name>
  <files>docs/development.md, docs/testing.md, docs/troubleshooting.md, docs/configuration.md</files>
  <read_first>
    docs/development.md
    docs/testing.md
    docs/troubleshooting.md
    docs/configuration.md
    build.gradle
    scripts/build-cuda-graph-shim.sh
  </read_first>
  <action>
    Update docs with exact strings:
    - `SYNAPTIK_CUDA_GRAPH_LIB`
    - `-Dsynaptik.cuda.graph.lib=`
    - `./gradlew buildCudaGraphShim cudaTest`
    - `Native CUDA tests skip when nvcc or CUDA hardware is unavailable`
    - `CUDA fallback interpretation`
    - `NATIVE_BUFFER_ABI_UNAVAILABLE`
    - `REQUIRED_BUFFER_EXECUTION_UNAVAILABLE`
    - `NATIVE_BUFFER_EXECUTION_FAILED`

    Remove or replace stale wording in `docs/testing.md` that says CUDA shim build instructions are not present.

    Explain troubleshooting steps:
    - check whether `nvcc` is on `PATH`
    - set `SYNAPTIK_CUDA_GRAPH_LIB` or `-Dsynaptik.cuda.graph.lib=<path>`
    - inspect `acceleratorBufferReasonCode` and `cudaFallbackReason`
    - treat optional native CUDA skip as acceptable only when portable Java gates pass
  </action>
  <verify>
    <automated>rg -n "SYNAPTIK_CUDA_GRAPH_LIB|-Dsynaptik\\.cuda\\.graph\\.lib=|\\.\\/gradlew buildCudaGraphShim cudaTest|Native CUDA tests skip when nvcc or CUDA hardware is unavailable|CUDA fallback interpretation|NATIVE_BUFFER_ABI_UNAVAILABLE|REQUIRED_BUFFER_EXECUTION_UNAVAILABLE|NATIVE_BUFFER_EXECUTION_FAILED" docs/development.md docs/testing.md docs/troubleshooting.md docs/configuration.md</automated>
  </verify>
  <acceptance_criteria>
    The grep command in verify exits 0. `rg -n "CUDA shim build instructions are not present" docs/testing.md` exits 1.
  </acceptance_criteria>
  <done>Developers have current CUDA setup and fallback troubleshooting docs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document Metal/CUDA ABI symmetry and narrow CUDA scope</name>
  <files>docs/architecture.md, docs/compute-flow.md, docs/development.md</files>
  <read_first>
    docs/architecture.md
    docs/compute-flow.md
    docs/development.md
    .planning/phases/07-cuda-buffer-execution-and-materialization/07-VERIFICATION.md
  </read_first>
  <action>
    Add documentation statements with exact strings:
    - `shared accelerator buffer ABI`
    - `Metal and CUDA`
    - `dense FLOAT32`
    - `unsupported CUDA buffer layouts and dtypes fall back visibly`
    - `CPU remains the correctness oracle`

    State that Phase 8 closes observability and documentation parity, not broad CUDA op coverage, public device tensors, large fused CUDA kernels, or CUDA profile calibration churn.
  </action>
  <verify>
    <automated>rg -n "shared accelerator buffer ABI|Metal and CUDA|dense FLOAT32|unsupported CUDA buffer layouts and dtypes fall back visibly|CPU remains the correctness oracle" docs/architecture.md docs/compute-flow.md docs/development.md</automated>
  </verify>
  <acceptance_criteria>
    Documentation contains all exact strings. `rg -n "broad CUDA operation coverage is complete|full production CUDA coverage" docs` exits 1.
  </acceptance_criteria>
  <done>Docs clearly state the v1.1 CUDA scope and backend symmetry.</done>
</task>

</tasks>

<verification>
Run:
`rg -n "CUDA trace and benchmark reports|cudaExecutionPath|SYNAPTIK_CUDA_GRAPH_LIB|Native CUDA tests skip|shared accelerator buffer ABI" docs`
</verification>

<success_criteria>
- Docs explain CUDA trace/report fields and fallback interpretation.
- Docs explain build/probe/troubleshooting.
- Docs avoid broad CUDA coverage overclaims.
</success_criteria>
