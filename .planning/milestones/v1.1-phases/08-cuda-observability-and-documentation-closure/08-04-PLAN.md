---
phase: 08-cuda-observability-and-documentation-closure
plan: "04"
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - src/test/java/SourceTreeHygieneTest.java
  - .gitignore
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/08-cuda-observability-and-documentation-closure/08-04-SUMMARY.md
autonomous: true
requirements:
  - CUDADOC-03
  - CUDA-06
  - CUDADOC-01
  - CUDADOC-02
user_setup:
  - "Run optional native CUDA verification only as capability-gated; skipped native checks are acceptable when portable gates pass and the skip reason is recorded."
must_haves:
  truths:
    - "D-27: Source hygiene prevents accidental tracking of CUDA native outputs, `.planning/tmp`, generated scratch files, and local benchmark/profile artifacts."
    - "D-28: Final verification covers portable Java tests, docs checks, source hygiene, and optional `buildCudaGraphShim cudaTest` pass-or-skip status."
    - "D-29: Milestone state moves Phase 8 requirements to complete only after verification evidence exists."
  artifacts:
    - path: "src/test/java/SourceTreeHygieneTest.java"
      provides: "source hygiene gate"
      contains: "cuda"
    - path: ".planning/phases/08-cuda-observability-and-documentation-closure/08-04-SUMMARY.md"
      provides: "final Phase 8 verification summary"
      contains: "Verification"
---

<objective>
Close Phase 8 with hygiene gates, final portable verification, optional native CUDA pass-or-skip evidence, and planning state updates so the v1.1 milestone can re-audit cleanly.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cuda-observability-and-documentation-closure/08-CONTEXT.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-RESEARCH.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-VALIDATION.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-01-PLAN.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-02-PLAN.md
@.planning/phases/08-cuda-observability-and-documentation-closure/08-03-PLAN.md
@src/test/java/SourceTreeHygieneTest.java
@.gitignore
@.planning/v1.1-MILESTONE-AUDIT.md
@AGENTS.md
</context>

<source_audit>
GOAL: Covers hygiene and final v1.1 CUDA observability closure verification.
REQ CUDADOC-03: Covered by source hygiene tests and final verification.
REQ CUDA-06/CUDADOC-01/CUDADOC-02: Covered by final gate over Plans 08-01, 08-02, and 08-03.
</source_audit>

<threat_model>
T-8-06: Local CUDA build outputs or profile artifacts could be committed as canonical project state. Mitigation: source hygiene checks and final git-status review reject `build/native/cuda`, `.planning/tmp`, and local `profiles/platform/.../tuning/abc/*` staging.
T-8-07: Phase 8 could close without a full portable verification pass. Mitigation: final task runs classes, focused CUDA/report/hygiene tests, docs grep checks, and optional native CUDA gate.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen CUDA source hygiene gates</name>
  <files>src/test/java/SourceTreeHygieneTest.java, .gitignore</files>
  <read_first>
    src/test/java/SourceTreeHygieneTest.java
    .gitignore
    build.gradle
  </read_first>
  <action>
    Extend `SourceTreeHygieneTest` with checks that:
    - `.gitignore` contains `build/`
    - `.gitignore` contains `.planning/tmp/`
    - `git ls-files build/native/cuda` is blank
    - `git ls-files .planning/tmp` is blank
    - no tracked file path matches `build/native/cuda/`
    - tracked `profiles/platform/.../tuning/abc/*` files are treated as explicit canonical fixtures and the test message says `do not stage local profile tuning changes accidentally`

    If `.gitignore` lacks a needed ignore entry, add it. Do not ignore canonical committed fixtures unless the repository already treats them as local-only.
  </action>
  <verify>
    <automated>./gradlew test --tests SourceTreeHygieneTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "build/native/cuda|\\.planning/tmp|do not stage local profile tuning changes accidentally|gitOutput\\(\"ls-files\"" src/test/java/SourceTreeHygieneTest.java .gitignore` exits 0. `./gradlew test --tests SourceTreeHygieneTest` exits 0.
  </acceptance_criteria>
  <done>Source hygiene prevents accidental CUDA/native/profile artifact pollution.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run final Phase 8 portable and optional native gates</name>
  <files>.planning/phases/08-cuda-observability-and-documentation-closure/08-04-SUMMARY.md</files>
  <read_first>
    .planning/phases/08-cuda-observability-and-documentation-closure/08-01-PLAN.md
    .planning/phases/08-cuda-observability-and-documentation-closure/08-02-PLAN.md
    .planning/phases/08-cuda-observability-and-documentation-closure/08-03-PLAN.md
    .planning/phases/08-cuda-observability-and-documentation-closure/08-04-PLAN.md
    build.gradle
    src/test/java/BenchmarkSessionTest.java
    src/test/java/backend/cuda/bridge/CudaFfmBridgeTest.java
    src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java
    src/test/java/backend/cuda/buffer/CudaBufferAllocatorTest.java
    src/test/java/backend/cuda/buffer/CudaDeviceToCpuMaterializerTest.java
    src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
    src/test/java/SourceTreeHygieneTest.java
  </read_first>
  <action>
    Run final commands:
    - `./gradlew classes`
    - `./gradlew test --tests BenchmarkSessionTest --tests backend.cuda.bridge.CudaFfmBridgeTest --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest --tests backend.cuda.buffer.CudaBufferAllocatorTest --tests backend.cuda.buffer.CudaDeviceToCpuMaterializerTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests SourceTreeHygieneTest`
    - `rg -n "GPU_CUDA|cudaExecutionPath|cudaFallbackReason|acceleratorInputBytes|CUDA trace and benchmark reports|Native CUDA tests skip" src/main/java src/test/java docs`
    - `./gradlew buildCudaGraphShim cudaTest`

    If `./gradlew buildCudaGraphShim cudaTest` skips because `nvcc` or CUDA hardware is unavailable, record the skip reason as acceptable. If it fails due Java/native regression, fix the regression.

    Write `08-04-SUMMARY.md` with:
    - commands run and pass/skip status
    - Phase 8 requirement coverage for `CUDA-06`, `CUDADOC-01`, `CUDADOC-02`, `CUDADOC-03`
    - statement that profile tuning files under `profiles/platform/.../tuning/abc/*` were not staged
    - residual risk for real CUDA hardware if native gate skipped
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests BenchmarkSessionTest --tests backend.cuda.bridge.CudaFfmBridgeTest --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest --tests backend.cuda.buffer.CudaBufferAllocatorTest --tests backend.cuda.buffer.CudaDeviceToCpuMaterializerTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests SourceTreeHygieneTest</automated>
    <automated>rg -n "GPU_CUDA|cudaExecutionPath|cudaFallbackReason|acceleratorInputBytes|CUDA trace and benchmark reports|Native CUDA tests skip" src/main/java src/test/java docs</automated>
    <automated>./gradlew buildCudaGraphShim cudaTest</automated>
  </verify>
  <acceptance_criteria>
    `08-04-SUMMARY.md` exists and contains `CUDA-06`, `CUDADOC-01`, `CUDADOC-02`, `CUDADOC-03`, `./gradlew classes`, `BenchmarkSessionTest`, `SourceTreeHygieneTest`, and `cudaTest`. `git status --short` shows no staged `profiles/platform/.../tuning/abc/*` files and no staged `build/native/cuda` artifacts.
  </acceptance_criteria>
  <done>Phase 8 has final verification evidence and optional native CUDA status.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update v1.1 planning state after verification evidence exists</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    .planning/PROJECT.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/08-cuda-observability-and-documentation-closure/08-04-SUMMARY.md
  </read_first>
  <action>
    After Task 2 verification succeeds, update planning state:
    - Mark `CUDA-06`, `CUDADOC-01`, `CUDADOC-02`, and `CUDADOC-03` as `[x]` in `.planning/REQUIREMENTS.md` with text `Validated in Phase 8.`
    - Update `.planning/ROADMAP.md` coverage table so those four requirements are `Complete`.
    - Update `.planning/PROJECT.md` Current State to say Phase 8 closes CUDA trace/report parity, fallback docs, and hygiene gates.
    - Update `.planning/STATE.md` Phase Planning Status so Phase 8 is complete only after `08-VERIFICATION.md` or final verification evidence exists.

    Do not mark the milestone complete; that remains `$gsd-audit-milestone` and `$gsd-complete-milestone`.
  </action>
  <verify>
    <automated>rg -n "CUDA-06.*Validated in Phase 8|CUDADOC-01.*Validated in Phase 8|CUDADOC-02.*Validated in Phase 8|CUDADOC-03.*Validated in Phase 8|Phase 8" .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    Requirements are checked only after verification evidence exists. `.planning/ROADMAP.md` lists `CUDA-06`, `CUDADOC-01`, `CUDADOC-02`, and `CUDADOC-03` as `Complete`. `.planning/STATE.md` does not say the milestone is archived.
  </acceptance_criteria>
  <done>Planning state reflects Phase 8 closure without prematurely archiving v1.1.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew classes`
`./gradlew test --tests BenchmarkSessionTest --tests backend.cuda.bridge.CudaFfmBridgeTest --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest --tests backend.cuda.buffer.CudaBufferAllocatorTest --tests backend.cuda.buffer.CudaDeviceToCpuMaterializerTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests SourceTreeHygieneTest`
`./gradlew buildCudaGraphShim cudaTest`
</verification>

<success_criteria>
- Source hygiene blocks local CUDA/native/profile artifact pollution.
- Portable Phase 8 verification is green.
- Optional native CUDA gate passes or skips with explicit unavailable reason.
- Planning state is updated only after verification evidence exists.
</success_criteria>
