---
phase: 06-cuda-shim-and-capability-probe
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java
  - src/main/java/backend/cuda/bridge/CudaBridgeExecutable.java
  - src/main/java/backend/cuda/bridge/CudaFfmBridge.java
  - src/main/java/backend/cuda/bridge/CudaGraphBridge.java
  - src/main/java/backend/cuda/bridge/UnavailableCudaGraphBridge.java
  - src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
  - src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java
  - src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
  - docs/architecture.md
  - docs/development.md
  - src/test/java/SourceTreeHygieneTest.java
autonomous: true
requirements:
  - CUDA-02
  - CUDA-01
user_setup:
  - "Do not stage local `profiles/platform/.../tuning/abc/*` changes or native outputs under `build/native/cuda/`."
must_haves:
  truths:
    - "D-08: CUDA seams consume `AcceleratorBufferRequest`, `AcceleratorBufferLayout`, `AcceleratorBufferDecision`, and `DeviceBufferBinding` metadata."
    - "D-09: CUDA native handles and lifetimes stay under `backend.cuda.*`; shared accelerator records remain backend-neutral."
    - "D-10: Phase 6 proves only supported dense CUDA layout metadata, primarily dense `FLOAT32`."
    - "D-11: No public user-facing device tensor API is added."
    - "D-12: REQUIRED buffer mode fails before tensor-list execution when CUDA buffer execution is unavailable."
    - "D-13: AUTO/default mode publishes a stable `AcceleratorBufferDecision` for CUDA fallback paths."
    - "D-15: Tests cover unavailable, unsupported dtype/layout, dense layout metadata, and required-buffer-mode behavior without CUDA hardware."
    - "D-16: Optional native CUDA tests remain capability-gated."
  artifacts:
    - path: "src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java"
      provides: "CUDA shared buffer ABI decision seam"
      contains: "AcceleratorBufferRequest"
    - path: "src/main/java/backend/cuda/exec/PreparedCudaExecutable.java"
      provides: "prepared executable CUDA buffer decision publication"
      contains: "lastAcceleratorBufferDecision"
    - path: "src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java"
      provides: "portable CUDA buffer policy coverage"
      contains: "denseFloat32LayoutsAreAcceptedByPolicy"
    - path: "src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java"
      provides: "REQUIRED-mode failure before tensor-list fallback"
      contains: "REQUIRED_BUFFER_EXECUTION_UNAVAILABLE"
  key_links:
    - from: "src/main/java/backend/cuda/exec/PreparedCudaExecutable.java"
      to: "src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java"
      via: "AcceleratorBufferRequest and AcceleratorBufferDecision"
      pattern: "GPU_CUDA"
    - from: "src/main/java/backend/memory/DeviceBufferBinding.java"
      to: "src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java"
      via: "backend-neutral binding boundary"
      pattern: "backendId"
---

<objective>
Wire CUDA prepared execution to the shared accelerator buffer ABI for dense supported layouts while keeping native buffer execution capability-gated and visibly unavailable until Phase 7.

Output: CUDA buffer policy consumes shared layout/access metadata, prepared CUDA execution publishes stable reason-coded buffer decisions, unsupported dtype/layout cases are tested, and REQUIRED mode fails before tensor-list fallback without needing CUDA hardware.
</objective>

<execution_context>
@/Users/phujka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/phujka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cuda-shim-and-capability-probe/06-CONTEXT.md
@.planning/phases/06-cuda-shim-and-capability-probe/06-RESEARCH.md
@.planning/phases/06-cuda-shim-and-capability-probe/06-VALIDATION.md
@.planning/phases/06-cuda-shim-and-capability-probe/06-PATTERNS.md
@.planning/phases/06-cuda-shim-and-capability-probe/06-01-PLAN.md
@.planning/phases/06-cuda-shim-and-capability-probe/06-02-PLAN.md
@src/main/java/backend/metal/buffer/MetalAcceleratorBufferBinder.java
@src/main/java/backend/metal/exec/PreparedMetalExecutable.java
@AGENTS.md
</context>

<source_audit>
GOAL: Covered by Java-side CUDA bridge/prepared executable seam consuming shared buffer layout/access metadata.
REQ CUDA-02: Covered by CUDA buffer binder, executable dtype metadata, prepared decision publication, and unsupported dtype/layout tests.
REQ CUDA-01: Covered by preserving capability-gated unavailable paths and REQUIRED-mode failure behavior from Plans 06-01 and 06-02.
CONTEXT D-08/D-09/D-10/D-11/D-12/D-13/D-15/D-16: Covered in must_haves and tasks.
AGENTS: Keeps public Tensor API logical and fallback visible.
</source_audit>

<threat_model>
T-6-07: CUDA buffer policy could add backend-specific fields to shared contracts. Mitigation: only consume existing `backend.accelerator.buffer` records; CUDA-specific helpers stay under `backend.cuda.*`.
T-6-08: REQUIRED buffer mode could accidentally fall back through tensor arrays. Mitigation: tests assert failure occurs before `CudaGraphBridge.execute(...)` is called.
T-6-09: Unsupported dtype/layout could look like generic native unavailable. Mitigation: policy tests assert `INPUT_DTYPE_UNSUPPORTED`, `OUTPUT_DTYPE_UNSUPPORTED`, `INPUT_LAYOUT_UNSUPPORTED`, and `OUTPUT_LAYOUT_UNSUPPORTED` where applicable.
T-6-10: Phase 6 could overclaim actual CUDA device-buffer execution. Mitigation: docs and decisions state actual buffer execution/materialization/handoff belong to Phase 7.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CUDA buffer policy over shared ABI records</name>
  <files>src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java, src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java</files>
  <read_first>
    src/main/java/backend/metal/buffer/MetalAcceleratorBufferBinder.java
    src/main/java/backend/metal/buffer/MetalLayoutPolicy.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferRequest.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferDecision.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferReasonCode.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferLayout.java
    src/main/java/backend/accelerator/buffer/AcceleratorBufferLayoutClass.java
    src/main/java/backend/cuda/bridge/CudaGraphBridge.java
  </read_first>
  <action>
    Create `src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java`.

    Public constructor:
    - `public CudaAcceleratorBufferBinder(CudaGraphBridge bridge)`

    Public method:
    - `public AcceleratorBufferDecision decide(AcceleratorBufferRequest request, AcceleratorBufferConfig bufferConfig)`

    Implement policy:
    - null `bufferConfig` uses `AcceleratorBufferConfig.defaults()`
    - if mode is `OFF`, return path `TENSOR_ARRAY`, allowed false, reason code `BUFFER_BINDINGS_DISABLED`
    - if `!bridge.supportsBufferBindings()`, return `UNAVAILABLE` for `REQUIRE` mode and `TENSOR_ARRAY` otherwise, reason code `NATIVE_BUFFER_ABI_UNAVAILABLE`, reason `native CUDA buffer ABI unavailable: bridge does not support buffer bindings`
    - only accept `DataType.FLOAT32` for inputs and outputs in Phase 6
    - reject input dtype mismatch with `INPUT_DTYPE_UNSUPPORTED`
    - reject output dtype mismatch with `OUTPUT_DTYPE_UNSUPPORTED`
    - accept only `AcceleratorBufferLayoutClass.DENSE_CONTIGUOUS` for inputs and outputs
    - reject input layout with `INPUT_LAYOUT_UNSUPPORTED`
    - reject output layout with `OUTPUT_LAYOUT_UNSUPPORTED`
    - when all metadata is accepted and bridge supports buffer bindings, return path `BUFFER_BINDING`, allowed true, reason code `BUFFER_BINDING_AVAILABLE`, reason `CUDA dense FLOAT32 buffer metadata accepted`

    Create `src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java` with fake bridges and tests:
    - `denseFloat32LayoutsAreAcceptedByPolicy`
    - `missingNativeBufferAbiFallsBackToTensorArrayInAutoMode`
    - `missingNativeBufferAbiIsUnavailableInRequireMode`
    - `unsupportedInputDTypeIsRejected`
    - `unsupportedOutputDTypeIsRejected`
    - `unsupportedInputLayoutIsRejected`
    - `unsupportedOutputLayoutIsRejected`

    Test requests must build `AcceleratorBufferRequest` with backend `ComputeBackend.GPU_CUDA` and explicit `AcceleratorBufferLayout.of(...)` values.
  </action>
  <verify>
    <automated>./gradlew test --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "class CudaAcceleratorBufferBinder|AcceleratorBufferRequest|DENSE_CONTIGUOUS|INPUT_DTYPE_UNSUPPORTED|OUTPUT_LAYOUT_UNSUPPORTED|BUFFER_BINDING_AVAILABLE" src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java src/test/java/backend/cuda/buffer/CudaAcceleratorBufferBinderTest.java` exits 0. Focused CUDA buffer binder tests pass without CUDA hardware.
  </acceptance_criteria>
  <done>CUDA has a backend-specific policy seam that consumes shared accelerator buffer ABI records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish CUDA buffer decisions from prepared execution</name>
  <files>src/main/java/backend/cuda/bridge/CudaBridgeExecutable.java, src/main/java/backend/cuda/bridge/CudaFfmBridge.java, src/main/java/backend/cuda/bridge/UnavailableCudaGraphBridge.java, src/main/java/backend/cuda/exec/PreparedCudaExecutable.java, src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java</files>
  <read_first>
    src/main/java/backend/cuda/bridge/CudaBridgeExecutable.java
    src/main/java/backend/cuda/bridge/CudaFfmBridge.java
    src/main/java/backend/cuda/exec/PreparedCudaExecutable.java
    src/main/java/backend/metal/exec/PreparedMetalExecutable.java
    src/main/java/backend/cuda/buffer/CudaAcceleratorBufferBinder.java
    src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java
  </read_first>
  <action>
    Extend `CudaBridgeExecutable` with dtype metadata:
    - `List<DataType> externalInputDataTypes`
    - `List<DataType> outputDataTypes`

    Update constructor validation to copy null lists to `List.of()` and require list sizes match `externalInputNodeIds` and `outputNodeIds` when non-empty.

    Update `CudaBridgeExecutable.unavailable(...)` to pass empty dtype lists.

    Update `CudaFfmBridge.compile(...)` to populate:
    - `externalInputDataTypes` from `dagSpec.externalInputs().stream().map(AcceleratorDagInput::dataType).toList()`
    - `outputDataTypes` as `List` of `DataType.FLOAT32` with size `dagSpec.outputNodeIds().size()` for current F32 graph ABI

    Update tests/fakes constructing `CudaBridgeExecutable` to pass the new dtype lists.

    Update `PreparedCudaExecutable`:
    - add field `CudaAcceleratorBufferBinder bufferBinder`
    - construct it from `bridge`
    - before legacy tensor-array bridge execution, build an `AcceleratorBufferRequest` with:
      - backend `ComputeBackend.GPU_CUDA`
      - estimated work from `dagSpec.nodes().size()` or another nonnegative DAG work estimate
      - `bridgeExecutable.externalInputNodeIds()`
      - `bridgeExecutable.externalInputDataTypes()`
      - layouts from `AcceleratorBufferLayout.fromTensor(...)` for resolved input tensors
      - `bridgeExecutable.outputNodeIds()`
      - `bridgeExecutable.outputDataTypes()`
      - layouts from runtime output tensors
      - `context != null && context.runsBackwardPass()`
    - call `bufferBinder.decide(...)` and assign `lastAcceleratorBufferDecision`
    - if `lastAcceleratorBufferDecision.required()` and path is not `BUFFER_BINDING`, throw the existing `IllegalStateException` shape: `Accelerator buffer path is required for GPU_CUDA but unavailable: <reasonCode>: <reason>`
    - if decision path is `BUFFER_BINDING`, do not claim actual execution in Phase 6; throw or convert to `REQUIRED_BUFFER_EXECUTION_UNAVAILABLE` unless native execution is implemented in Phase 7
    - otherwise keep existing tensor-array bridge execution and CPU fallback behavior

    Update `PreparedCudaExecutableBufferPolicyTest`:
    - keep the two existing REQUIRED-mode tests
    - add `autoModePublishesNativeBufferAbiUnavailableDecisionWithoutThrowing` using unavailable/fake bridge and CPU fallback step when possible
    - add `requireModeFailsBeforeTensorListExecutionEvenWhenDenseMetadataAccepted`
    - assert fake bridge `execute(...)` is not called in REQUIRED failure paths
  </action>
  <verify>
    <automated>./gradlew test --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest</automated>
    <automated>./gradlew test --tests backend.cuda.bridge.CudaFfmBridgeTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "externalInputDataTypes|outputDataTypes|CudaAcceleratorBufferBinder|AcceleratorBufferRequest|REQUIRED_BUFFER_EXECUTION_UNAVAILABLE|autoModePublishesNativeBufferAbiUnavailableDecisionWithoutThrowing" src/main/java/backend/cuda src/test/java/backend/cuda/exec/PreparedCudaExecutableBufferPolicyTest.java` exits 0. Focused prepared executable and bridge tests pass without CUDA hardware.
  </acceptance_criteria>
  <done>Prepared CUDA execution publishes shared-ABI buffer decisions and preserves REQUIRED-mode failure semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close docs, hygiene, and final portable verification</name>
  <files>docs/architecture.md, docs/development.md, src/test/java/SourceTreeHygieneTest.java, .planning/phases/06-cuda-shim-and-capability-probe/06-03-SUMMARY.md</files>
  <read_first>
    docs/architecture.md
    docs/development.md
    src/test/java/SourceTreeHygieneTest.java
    .gitignore
    build.gradle
    .planning/phases/06-cuda-shim-and-capability-probe/06-01-PLAN.md
    .planning/phases/06-cuda-shim-and-capability-probe/06-02-PLAN.md
    .planning/phases/06-cuda-shim-and-capability-probe/06-03-PLAN.md
  </read_first>
  <action>
    Update docs:
    - `docs/architecture.md` states CUDA consumes the shared `AcceleratorBufferLayout` / `AcceleratorBufferDecision` ABI for dense `FLOAT32` metadata in Phase 6
    - `docs/architecture.md` states actual CUDA device-buffer execution/materialization/handoff belongs to Phase 7
    - `docs/development.md` lists focused portable commands:
      - `./gradlew classes`
      - `./gradlew test --tests backend.cuda.bridge.CudaFfmBridgeTest`
      - `./gradlew test --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest`
      - `./gradlew test --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest`
      - `./gradlew test --tests SourceTreeHygieneTest`
    - docs include exact reason code strings `NATIVE_BUFFER_ABI_UNAVAILABLE`, `INPUT_DTYPE_UNSUPPORTED`, `OUTPUT_LAYOUT_UNSUPPORTED`, and `REQUIRED_BUFFER_EXECUTION_UNAVAILABLE`

    Extend `SourceTreeHygieneTest` only if needed:
    - assert `.gitignore` or Gradle build location protects `build/native/cuda/` through existing `build/` ignore
    - assert no files under `build/native/cuda/` are tracked using `git ls-files build/native/cuda`
    - do not add rules that fail because canonical profile fixtures are already tracked

    Run final verification:
    - `./gradlew classes`
    - `./gradlew test --tests backend.cuda.bridge.CudaFfmBridgeTest --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests SourceTreeHygieneTest`

    Create `06-03-SUMMARY.md` after execution with:
    - exact commands and pass/skip results
    - note whether optional `./gradlew buildCudaGraphShim` / `./gradlew cudaTest` was run or skipped and why
    - `git status --short` excerpt proving no `build/native/cuda/`, `.planning/tmp/`, or unintended `profiles/platform/.../tuning/abc/*` files were staged by Phase 6
  </action>
  <verify>
    <automated>./gradlew classes</automated>
    <automated>./gradlew test --tests backend.cuda.bridge.CudaFfmBridgeTest --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests SourceTreeHygieneTest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "AcceleratorBufferLayout|AcceleratorBufferDecision|NATIVE_BUFFER_ABI_UNAVAILABLE|REQUIRED_BUFFER_EXECUTION_UNAVAILABLE|Phase 7" docs/architecture.md docs/development.md` exits 0. Source hygiene tests pass. Final summary records portable verification and native CUDA skip/pass status. `git status --short` does not show staged build/native CUDA outputs or staged profile tuning changes.
  </acceptance_criteria>
  <done>Phase 6 docs, hygiene, and portable verification are complete.</done>
</task>

</tasks>

<verification>
Run:
`./gradlew classes`
`./gradlew test --tests backend.cuda.bridge.CudaFfmBridgeTest --tests backend.cuda.buffer.CudaAcceleratorBufferBinderTest --tests backend.cuda.exec.PreparedCudaExecutableBufferPolicyTest --tests SourceTreeHygieneTest`
Optional on CUDA-capable machines:
`./gradlew buildCudaGraphShim`
`./gradlew cudaTest`
</verification>

<success_criteria>
- CUDA bridge/prepared executable seams consume shared accelerator buffer layout/access metadata for supported dense layouts.
- Unsupported dtype/layout and unavailable native buffer ABI paths publish stable reason codes.
- REQUIRED buffer mode fails before tensor-list fallback when CUDA native buffer execution is unavailable.
- Phase 6 does not add public device tensor APIs or CUDA-specific fields to shared accelerator records.
</success_criteria>
