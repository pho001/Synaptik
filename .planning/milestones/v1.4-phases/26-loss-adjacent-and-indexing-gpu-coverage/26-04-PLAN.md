# Plan 26-04: Loss-Adjacent Coverage Gates And Documentation Closure

**Phase:** 26 Loss-Adjacent And Indexing GPU Coverage
**Requirements:** GPULOSSIDX-01, GPULOSSIDX-02, GPULOSSIDX-03
**Status:** Planned
**Wave:** 4

## Objective

Close Phase 26 with coverage gates and docs that show hot-path loss/indexing exits explicitly and preserve adjacent supported GPU regions.

## Tasks

1. Add coverage summary/regression expectations for index-target loss and representative indexing flows.
2. Add benchmark/report assertions for fallback reason, CPU materialization count, selected GPU region length, and lowered primitive count around loss/indexing boundaries.
3. Document current Metal/CUDA status in `docs/gpu-lowering-coverage.md`, `docs/compute-flow.md`, and backend docs if touched.
4. Produce phase summaries and update roadmap/state.
5. Run focused validation gates and record CUDA native toolchain limitation if applicable.

## Acceptance Criteria

- Coverage reports show loss/indexing fallback with stable reasons.
- Hot paths fail if fallback becomes hidden or adjacent GPU region selection regresses.
- Docs explain that GPU fusion/lowering remains region-internal and loss/indexing rows are not CPU fused nodes.
- Profile artifacts under `profiles/platform/...` remain unstaged.

## Verification

```bash
./gradlew test --tests GpuCoverageRegressionGateTest --tests GpuCoverageSummaryTest --tests BenchmarkSessionTest --tests StandardWorkloadsTest
git diff --check
```

## Threat Model

- **Threat:** Reports hide loss/indexing exits behind aggregate fallback counts.
  **Mitigation:** Assert reason strings and family names in text/JSON renderers.
- **Threat:** Local profile files are accidentally committed as evidence.
  **Mitigation:** Check status and keep profile artifacts unstaged unless explicitly promoted.
