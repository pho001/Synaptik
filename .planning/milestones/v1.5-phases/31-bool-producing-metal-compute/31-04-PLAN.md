# Plan 31-04: Docs Parity And Report Closure

**Phase:** 31 BOOL-Producing Metal Compute
**Requirements:** METALBOOL-01, METALBOOL-02, METALBOOL-03
**Status:** Complete
**Wave:** 4

## Objective

Close Phase 31 with docs, verification evidence, and report contracts that clearly distinguish supported BOOL-producing Metal compute from predicate-input residency and unsupported mask semantics.

## Tasks

1. Update `docs/metal-backend.md` with BOOL output support scope, native requirements, and unsupported families.
2. Update `docs/gpu-lowering-coverage.md` with compare/logical/reduction BOOL support or rejection rows.
3. Update troubleshooting guidance for missing BOOL native capability, missing dtype residency evidence, and accidental CPU exits.
4. Write `31-VERIFICATION.md` mapping `METALBOOL-01/02/03` to tests and docs.
5. Run source hygiene, focused Metal capability/lowering/report tests, `metalTest`, and `git diff --check`.
6. Verify local benchmark/profile artifacts remain unstaged.

## Acceptance Criteria

- Docs do not overclaim masked attention or universal BOOL operation support.
- Requirement verification explicitly covers compare/logical output, `WHERE` mask-chain residency, and `REDUCE_ALL/ANY` support-or-reject status.
- Reports distinguish predicate input residency from native BOOL-producing compute.
- Profile artifacts remain unstaged.

## Verification

```bash
./gradlew test --tests SourceTreeHygieneTest
./gradlew test --tests backend.metal.MetalMpsCapabilitiesTest --tests backend.metal.lowering.MetalRegionLowererTest --tests GpuCoverageSummaryTest
./gradlew metalTest
git diff --check
git status --short profiles/platform
```

## Threat Model

- **Threat:** Docs imply all BOOL masks are Metal-supported, including masked SDPA.
  **Mitigation:** Keep masked SDPA language explicitly deferred to Phase 34.
- **Threat:** Local benchmark artifacts are committed accidentally.
  **Mitigation:** Check profile status before staging and commit only source/docs/planning files.
