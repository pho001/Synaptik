---
phase: 13-coverage-benchmark-and-regression-gate
plan: "04"
type: execute
wave: 4
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - docs/compute-flow.md
  - docs/gpu-lowering-coverage.md
  - docs/development.md
  - docs/testing.md
  - .planning/phases/13-coverage-benchmark-and-regression-gate/13-VALIDATION.md
  - .planning/phases/13-coverage-benchmark-and-regression-gate/13-04-SUMMARY.md
autonomous: true
requirements: [GPUCOV-01, GPUCOV-02, GPUCOV-03]
must_haves:
  truths:
    - "D-09: Native Metal and CUDA checks remain capability-gated while portable Java tests prove schema, fallback, and report contracts."
    - "D-10: Do not commit machine-local benchmark/calibration output from build/ or profiles/platform/.../tuning/abc/* unless intentionally creating stable fixtures."
    - "D-11: Stable report fixtures, if needed, live under test resources or docs/test fixture paths, not local tuning output directories."
    - "D-12: Docs must distinguish evidence contracts from local measurement artifacts."
    - "GPUCOV-01, GPUCOV-02, and GPUCOV-03 must all be closed by verification evidence."
---

<objective>
Close Phase 13 with documentation, native capability-gated checks, validation strategy updates, and source hygiene evidence that make GPU coverage regression results auditable.
</objective>

<source_audit>
GOAL: Completes v1.2 coverage work by documenting the evidence contract, proving portable/native gates, and recording local artifact hygiene.
REQ GPUCOV-01: Docs explain trace/report coverage fields.
REQ GPUCOV-02: Docs and summaries identify representative coverage workloads and baseline comparison semantics.
REQ GPUCOV-03: Docs and validation explain regression gate failure behavior.
D-09 through D-12: Covered by capability-gated native checks and artifact hygiene instructions.
</source_audit>

<threat_model>
T-13-11: Documentation could imply native CUDA ran when it was capability-skipped. Mitigation: final summary must record native Metal/CUDA status as passed, unavailable, or capability-skipped.
T-13-12: Local tuning/profile artifacts could be committed with Phase 13 evidence. Mitigation: final summary and validation must record `git status --short` and confirm no staged `profiles/platform/.../tuning/abc/*` files.
T-13-13: Future developers could treat local benchmark output as a regression gate. Mitigation: docs must name checked-in report fields/tests as evidence contracts and local profiles as non-canonical artifacts.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document coverage report fields and gate semantics</name>
  <files>docs/compute-flow.md, docs/gpu-lowering-coverage.md, docs/testing.md</files>
  <read_first>
    docs/compute-flow.md
    docs/gpu-lowering-coverage.md
    docs/testing.md
    src/main/java/tuning/benchmark/report/GpuCoverageSummary.java
    src/main/java/tuning/benchmark/report/GpuCoverageRegressionGate.java
    .planning/phases/13-coverage-benchmark-and-regression-gate/13-CONTEXT.md
  </read_first>
  <action>
    Update docs with these exact terms:
    - `GPU coverage summary`
    - `gpuCoverageRatio`
    - `selectedRegionCount`
    - `maxSelectedRegionLength`
    - `rejectedCandidateReasonCounts`
    - `cpuMaterializationReasonCounts`
    - `deviceHandoffCount`
    - `hidden tensor-array fallback`
    - `coverage/materialization behavior, not raw timing`
    - `portable coverage gate`
    - `native capability-gated evidence`

    `docs/compute-flow.md` must explain that `gpuCoverageRatio` is executed accelerator-buffer steps divided by total traced run steps.

    `docs/gpu-lowering-coverage.md` must link Phase 11/12 lowering and compound coverage to Phase 13 benchmark gates.

    `docs/testing.md` must explain that portable Java tests prove report/gate contracts even when CUDA native tasks are capability-skipped.
  </action>
  <verify>
    <automated>rg -n "GPU coverage summary|gpuCoverageRatio|selectedRegionCount|maxSelectedRegionLength|rejectedCandidateReasonCounts|cpuMaterializationReasonCounts|deviceHandoffCount|hidden tensor-array fallback|coverage/materialization behavior, not raw timing|portable coverage gate|native capability-gated evidence" docs/compute-flow.md docs/gpu-lowering-coverage.md docs/testing.md</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "GPU coverage summary|hidden tensor-array fallback|coverage/materialization behavior, not raw timing|native capability-gated evidence" docs/compute-flow.md docs/gpu-lowering-coverage.md docs/testing.md` exits 0.
  </acceptance_criteria>
  <done>Docs explain how to read and trust coverage evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document focused commands and artifact hygiene</name>
  <files>docs/development.md, docs/testing.md</files>
  <read_first>
    docs/development.md
    docs/testing.md
    .planning/STATE.md
    .planning/phases/13-coverage-benchmark-and-regression-gate/13-VALIDATION.md
  </read_first>
  <action>
    Add a `GPU coverage regression checks` subsection to `docs/development.md` with exact commands:
    - `./gradlew classes`
    - `./gradlew test --tests GpuCoverageSummaryTest --tests GpuCoverageRegressionGateTest --tests BenchmarkSessionTest --tests BenchmarkSuiteSessionTest --tests CompiledGraphTraceTest`
    - `./gradlew metalTest`
    - `./gradlew buildCudaGraphShim cudaTest`
    - `git status --short`

    Add artifact hygiene text with exact strings:
    - `profiles/platform/.../tuning/abc/*`
    - `machine-local benchmark/calibration output`
    - `checked-in evidence contract`
    - `do not commit local tuning artifacts`
  </action>
  <verify>
    <automated>rg -n "GPU coverage regression checks|GpuCoverageSummaryTest|GpuCoverageRegressionGateTest|BenchmarkSuiteSessionTest|buildCudaGraphShim cudaTest|profiles/platform/.../tuning/abc/\\*|machine-local benchmark/calibration output|checked-in evidence contract|do not commit local tuning artifacts" docs/development.md docs/testing.md</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "GPU coverage regression checks|do not commit local tuning artifacts|checked-in evidence contract" docs/development.md docs/testing.md` exits 0.
  </acceptance_criteria>
  <done>Developers have exact commands and artifact hygiene rules for coverage gates.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run final portable and native capability-gated verification</name>
  <files>.planning/phases/13-coverage-benchmark-and-regression-gate/13-04-SUMMARY.md, .planning/phases/13-coverage-benchmark-and-regression-gate/13-VALIDATION.md</files>
  <read_first>
    .planning/phases/13-coverage-benchmark-and-regression-gate/13-01-SUMMARY.md
    .planning/phases/13-coverage-benchmark-and-regression-gate/13-02-SUMMARY.md
    .planning/phases/13-coverage-benchmark-and-regression-gate/13-03-SUMMARY.md
    .planning/phases/13-coverage-benchmark-and-regression-gate/13-VALIDATION.md
    docs/development.md
  </read_first>
  <action>
    Run and record these commands in `13-04-SUMMARY.md`:
    - `./gradlew classes`
    - `./gradlew test --tests GpuCoverageSummaryTest --tests GpuCoverageRegressionGateTest --tests BenchmarkSessionTest --tests BenchmarkSuiteSessionTest --tests CompiledGraphTraceTest`
    - optional native: `./gradlew metalTest`
    - optional native: `./gradlew buildCudaGraphShim cudaTest`
    - `rg -n "GPU coverage summary|gpuCoverageRatio|hidden tensor-array fallback|GPU coverage regression checks" docs/compute-flow.md docs/gpu-lowering-coverage.md docs/development.md docs/testing.md`
    - `git status --short`

    The summary must include:
    - `Phase 13 final verification`
    - `GPUCOV-01`
    - `GPUCOV-02`
    - `GPUCOV-03`
    - `native Metal status`
    - `native CUDA status`
    - `capability-skipped` when a native gate skips
    - `profiles/platform/.../tuning/abc/* remained unstaged`

    Update `13-VALIDATION.md` from draft to validation-audit form:
    - set `status: verified`
    - set `nyquist_compliant: true`
    - mark task rows green when focused commands pass or capability-skip correctly
    - add a `Validation Audit 2026-04-30` section with requirements audited, task rows, gaps found, resolved, and escalated counts
  </action>
  <verify>
    <automated>test -f .planning/phases/13-coverage-benchmark-and-regression-gate/13-04-SUMMARY.md</automated>
    <automated>rg -n "Phase 13 final verification|GPUCOV-01|GPUCOV-02|GPUCOV-03|native Metal status|native CUDA status|profiles/platform/.../tuning/abc/\\* remained unstaged|nyquist_compliant: true|Validation Audit 2026-04-30" .planning/phases/13-coverage-benchmark-and-regression-gate/13-04-SUMMARY.md .planning/phases/13-coverage-benchmark-and-regression-gate/13-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/13-coverage-benchmark-and-regression-gate/13-04-SUMMARY.md` records final verification and native statuses; `13-VALIDATION.md` contains `nyquist_compliant: true` and `Validation Audit 2026-04-30`.
  </acceptance_criteria>
  <done>Phase 13 is ready for security, validation, and milestone audit.</done>
</task>

</tasks>

<verification>
Run:
- `./gradlew classes`
- `./gradlew test --tests GpuCoverageSummaryTest --tests GpuCoverageRegressionGateTest --tests BenchmarkSessionTest --tests BenchmarkSuiteSessionTest --tests CompiledGraphTraceTest`
- Optional native: `./gradlew metalTest`
- Optional native: `./gradlew buildCudaGraphShim cudaTest`
- `rg -n "GPU coverage summary|gpuCoverageRatio|hidden tensor-array fallback|GPU coverage regression checks" docs/compute-flow.md docs/gpu-lowering-coverage.md docs/development.md docs/testing.md`
- `git status --short`
</verification>

<success_criteria>
- Documentation explains GPU coverage summary fields and regression gate failure semantics.
- Portable Java gates prove schema, fallback, report, and hidden-exit contracts.
- Native Metal/CUDA evidence is recorded as passed, unavailable, or capability-skipped.
- Validation strategy is updated after execution with no Nyquist gaps.
- Local `profiles/platform/.../tuning/abc/*` artifacts remain unstaged.
</success_criteria>
